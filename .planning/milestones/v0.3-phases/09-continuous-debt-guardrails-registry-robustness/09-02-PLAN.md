---
phase: 09-continuous-debt-guardrails-registry-robustness
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - scripts/check-no-runtime-deps.cjs
  - tests/check-no-runtime-deps.test.ts
  - Makefile
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "A `make verify-no-runtime-deps` target asserts the root `@dnzn/dxkit` package.json declares no external runtime-visible dependency (empty/absent `dependencies`/`peerDependencies`/`optionalDependencies`) and passes on today's zero-dep root (per revised D-08, core-only)."
    - "The core-only check fails the build if the root package.json declares an external runtime dep."
    - "REQUIREMENTS.md GATE-02 wording and ROADMAP Phase 9 success criterion #2 reflect core-only scope (no longer 'any package')."
  artifacts:
    - "scripts/check-no-runtime-deps.cjs — Node built-in JSON field check (no new deps)"
    - "tests/check-no-runtime-deps.test.ts — fixture unit test + Makefile/CI wiring guard"
    - "Makefile — `verify-no-runtime-deps` target (root package.json only) + `.PHONY` + release/publish prereq"
    - ".github/workflows/ci.yml — named GATE-02 dep-check step"
  key_links:
    - "CI dep-check step → `make verify-no-runtime-deps` → `node scripts/check-no-runtime-deps.cjs package.json` (root only)"
  prohibitions:
    - "MUST NOT check plugin package.json files or loop `PLUGIN_BUILD_ORDER` for this check (revised D-08 / P1 — plugins legitimately declare workspace:* deps and will grow external runtime deps)."
    - "MUST NOT add any npm dependency (dev or runtime); use node built-ins only (P4)."
  backstops:
    - statement: "The dep-check passes on the current root package.json (zero declared deps) — codifies the status quo, does not turn red on arrival."
      verification: backstop
---

<objective>
GATE-02: machine-enforce the zero-runtime-dependency posture of the **core `@dnzn/dxkit` package
only** (revised D-08, 2026-07-18). A Makefile target reads the root package.json and fails the
build if it declares any external runtime-visible dependency. Correct the requirement/roadmap text
to match the core-only scope.

Purpose: Executable documentation of the "zero-runtime-dep microframework" claim, scoped where it
is load-bearing (the framework, not the optional plugins).
Output: A dep-check script + Makefile target + named CI step + fixture/guard tests + doc correction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-CONTEXT.md
@.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core-only dep-check script + fixture unit test (RED→GREEN)</name>
  <files>scripts/check-no-runtime-deps.cjs, tests/check-no-runtime-deps.test.ts</files>
  <read_first>
    - package.json (root — confirm `dependencies`/`peerDependencies`/`optionalDependencies` are empty/absent today)
    - tests/typecheck-config.test.ts (guard-test style: readFileSync + assertions)
    - .planning/phases/09-continuous-debt-guardrails-registry-robustness/09-CONTEXT.md (revised D-08 — core-only, keep it simple, no workspace-carveout logic needed)
  </read_first>
  <behavior>
    - checkNoRuntimeDeps({}) → ok (no runtime fields).
    - checkNoRuntimeDeps({ dependencies: {} }) → ok (empty field).
    - checkNoRuntimeDeps({ dependencies: { lodash: "^4.17.0" } }) → fail, violation names `dependencies.lodash`.
    - Same failing behavior for a non-empty `peerDependencies` and `optionalDependencies`.
  </behavior>
  <action>
    Create `scripts/check-no-runtime-deps.cjs` exporting a pure function `checkNoRuntimeDeps(pkg)` that returns `{ ok, violations }` where `violations` lists every entry found in `dependencies`, `peerDependencies`, or `optionalDependencies`. Because this gate is core-only (revised D-08), keep it simple: any entry in those three fields is a violation — no workspace-carveout logic (the root never declares workspace links). Add a CLI entry: when invoked directly with a package.json path arg, read+parse it with node's `fs`/`JSON`, print `OK: <path> ...` and exit 0 on pass, or print each `FAIL: <path> declares <field>.<name> ...` to stderr and `process.exit(1)` on violation. Use CommonJS `module.exports` and `require('node:fs')` only — no npm packages (P4). Then create `tests/check-no-runtime-deps.test.ts` importing `checkNoRuntimeDeps` and asserting the four behavior cases plus a case loading the actual root `package.json` object and expecting `ok: true`.
  </action>
  <verify>
    <automated>npx vitest run tests/check-no-runtime-deps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/check-no-runtime-deps.test.ts` passes all fixture cases.
    - `node scripts/check-no-runtime-deps.cjs package.json` exits 0 and prints an OK line on the current root package.json.
    - Running the script against a temp fixture declaring `{ "dependencies": { "lodash": "^4" } }` exits 1.
  </acceptance_criteria>
  <done>The core-only dep-check function and its fixture unit test exist and are green on the real root package.json.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `verify-no-runtime-deps` into Makefile + CI + release/publish (+ wiring guard)</name>
  <files>Makefile, .github/workflows/ci.yml, tests/check-no-runtime-deps.test.ts</files>
  <read_first>
    - Makefile (`.PHONY` line 7; `verify-outputs` template lines 77–95; `release`/`publish` prereq chains lines 64/71)
    - .github/workflows/ci.yml (after this phase's 09-01 GATE-01 step; insert the dep-check step after it, before `make test`)
    - tests/check-no-runtime-deps.test.ts (append the wiring-guard describe block here)
  </read_first>
  <action>
    Add a `verify-no-runtime-deps` target to the Makefile that invokes `node ./scripts/check-no-runtime-deps.cjs package.json` on the root package.json ONLY — it MUST NOT loop `$(PLUGIN_BUILD_ORDER)` and MUST NOT reference plugin package.json files (revised D-08 / P1). Add `verify-no-runtime-deps` to the `.PHONY` list, and add it as an explicit prerequisite of the `release` and `publish` targets (mirroring `verify-outputs`; GATE-01's typecheck is already transitively covered there via `test`, so only the dep-check needs adding). In ci.yml, add a step `name: Zero-runtime-dependency assertion (GATE-02)` running `make verify-no-runtime-deps`, placed after the GATE-01 typecheck step and before `make test`. Then append a `describe('GATE-02 wiring')` block to tests/check-no-runtime-deps.test.ts asserting: the Makefile defines `verify-no-runtime-deps`, its target body does NOT reference `PLUGIN_BUILD_ORDER` (region-scoped to the target body only — the token appears legitimately in other targets), the target is in `.PHONY`, and ci.yml contains the named GATE-02 step running `make verify-no-runtime-deps`.
  </action>
  <verify>
    <automated>make verify-no-runtime-deps && npx vitest run tests/check-no-runtime-deps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-no-runtime-deps` exits 0 on the current repo.
    - `grep -n "GATE-02" .github/workflows/ci.yml` returns the named step.
    - The `verify-no-runtime-deps` Makefile target body contains `package.json` but NOT `PLUGIN_BUILD_ORDER`.
    - `release`/`publish` prereq lines include `verify-no-runtime-deps`.
    - The `GATE-02 wiring` guard test passes.
  </acceptance_criteria>
  <done>The core-only dep-check runs as a named CI step and a release/publish prerequisite, locked by a wiring guard.</done>
</task>

<task type="auto">
  <name>Task 3: Correct GATE-02 wording to core-only (REQUIREMENTS.md + ROADMAP.md)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (line 40 — GATE-02 requirement text)
    - .planning/ROADMAP.md (line 159 — Phase 9 success criterion #2)
    - .planning/phases/09-continuous-debt-guardrails-registry-robustness/09-CONTEXT.md (revised D-08 rationale)
  </read_first>
  <action>
    Edit the GATE-02 requirement line in REQUIREMENTS.md so it asserts the zero-runtime-dependency posture of the **core `@dnzn/dxkit` package** (not "any package"), keeping the "automated bump that pulls in a runtime dep is caught" intent. Edit ROADMAP Phase 9 success criterion #2 so it likewise scopes the assertion to the **core package** and drops the "into any package" phrasing (and the stale `pnpm why`-style example may be replaced with the package.json field-check per D-07). Both edits must make the core-only scope explicit and mention the core package by name.
  </action>
  <verify>
    <automated>grep -q "core" .planning/REQUIREMENTS.md && grep -n "core" .planning/ROADMAP.md | grep -qi "runtime"</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md GATE-02 line references the core `@dnzn/dxkit` package and no longer implies "any package".
    - ROADMAP success criterion #2 references the core package scope and no longer says "into any package".
    - `grep -c "into any package" .planning/ROADMAP.md` is 0 for the Phase 9 criterion.
  </acceptance_criteria>
  <done>Requirement and roadmap text match the implemented core-only GATE-02 scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated dep bump / contributor → published core package | an added external runtime dep would expand DxKit's supply-chain surface for downstream consumers |
| npm registry → repo (installs) | this phase installs zero packages — no external install surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02 | Tampering | Core package runtime deps (V10 Supply Chain) | medium | mitigate | `make verify-no-runtime-deps` fails the build if the root `@dnzn/dxkit` package.json declares any external runtime-visible dependency; wired as a named CI step and a release/publish prerequisite. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install tasks in this phase (RESEARCH Package Legitimacy Gate = N/A; P4 forbids new deps). No install surface to gate. |
</threat_model>

<artifacts>
## Artifacts this plan produces
- Script: `scripts/check-no-runtime-deps.cjs` (exports `checkNoRuntimeDeps`, CLI entry)
- Makefile target: `verify-no-runtime-deps` (root package.json only) + `.PHONY` entry + release/publish prereq
- CI step name: `Zero-runtime-dependency assertion (GATE-02)`
- Tests: `tests/check-no-runtime-deps.test.ts` (fixture unit test + `GATE-02 wiring` guard)
- Doc edits: REQUIREMENTS.md GATE-02 wording, ROADMAP Phase 9 success criterion #2 (core-only)
</artifacts>

<verification>
- `make verify-no-runtime-deps` green on current repo (root zero deps).
- `npx vitest run tests/check-no-runtime-deps.test.ts` green.
- Manual: GATE-02 step present in ci.yml after the GATE-01 step; release/publish include the prereq.
</verification>

<success_criteria>
CI asserts the core package's zero-runtime-dependency posture; an automated bump introducing an external runtime dep into `@dnzn/dxkit` fails the build (GATE-02, ROADMAP success criterion #2, core-only per revised D-08).
</success_criteria>

<output>
Create `.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-02-SUMMARY.md` when done.
</output>
