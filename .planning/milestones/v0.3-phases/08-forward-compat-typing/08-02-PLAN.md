---
phase: 08-forward-compat-typing
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - vitest.smoke.config.ts
  - smoke/dist-exports.smoke.test.ts
  - smoke/fixtures/expected-exports.ts
  - smoke/node-builtins.d.ts
  - Makefile
  - biome.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [FCT-04]
must_haves:
  truths:
    - "make smoke builds the packages first (build prerequisite) then runs vitest against the real dist/ artifacts via a separate vitest.smoke.config.ts — it is wired into release/publish/CI after verify-outputs and is NOT folded into make test (D-04, Phase 7 constraint that make test never builds)."
    - "Each IIFE global — DxKit, DxWallet, DxAuth, DxTheme, DxSettings — attaches to a happy-dom Window (loaded via vm.runInContext, never the happy-dom <script>-element path) with its full expected top-level export-key set (D-05), including all five loaded sequentially into one shared window in dependency order (core before plugins) with no collisions."
    - "CJS require() (via createRequire(import.meta.url)) of each package's dist/index.cjs returns the same exhaustive expected export-key set (D-05)."
    - "biome.json files.includes covers smoke/**/*.ts so make lint actually lints the new smoke code (Pitfall 3 — Biome includes is an allowlist, not an ignore-list)."
    - "[edge:FCT-04] The expected-export assertion is exhaustive and order-independent — keys are compared sorted, and a dropped, renamed, or added export key fails the smoke test (D-05's maintained fixture goes red on public-surface drift, e.g. from verbatimModuleSyntax/isolatedDeclarations churn)."
  artifacts:
    - "vitest.smoke.config.ts — separate config, environment happy-dom, include ['smoke/**/*.smoke.test.ts'], no resolve.alias (targets dist/, not src/)"
    - "smoke/dist-exports.smoke.test.ts — the FCT-04 smoke test (CJS require() + IIFE vm.runInContext assertions)"
    - "smoke/fixtures/expected-exports.ts — the maintained EXPECTED_EXPORTS fixture (5 packages, exhaustive key sets)"
    - "smoke/node-builtins.d.ts — ambient decls for node:vm / node:module (zero-@types/node posture)"
    - "Makefile — new `smoke` target (build prerequisite), .PHONY update, release/publish chains gain smoke after verify-outputs"
    - "biome.json — files.includes gains smoke/**/*.ts"
    - ".github/workflows/ci.yml — `- run: make smoke` step after `make verify-outputs`"
  key_links:
    - "make smoke: build → npx vitest run --config vitest.smoke.config.ts — the FCT-04 required gate that proves the dist/ IIFE/CJS artifact boundary intact (the one output neither tsc nor the default vitest suite exercises)"
    - "smoke test loads IIFE via vm.runInContext(code, happyDomWindow) — NOT document.createElement('script') (verified silently broken for this bundle shape)"
    - "smoke/ is its own top-level directory outside vitest.config.ts's include globs so it never joins make test (Pitfall 2)"
  prohibitions:
    - "The smoke test must never pass silently against a stale, missing, or undefined dist/ artifact — it must assert the positive export-key shape (exact sorted key set), never merely that nothing threw."
    - "A public export key must never be dropped or renamed without a BREAKING CHANGE footer — the exhaustive expected-export fixture must go red on drift (D-05)."
    - "The smoke test must never load the IIFE bundle through happy-dom's classic <script>-element evaluation path — that path silently drops top-level var globals for this bundle shape (research Pitfall 1)."
---

<objective>
Build the FCT-04 build-artifact smoke test: a dedicated `make smoke` target that builds first, then runs a separate vitest config against the real built dist/ artifacts — asserting each IIFE global attaches to a happy-dom Window with its full expected top-level export-key set, and CJS require() interop returns that same set. Wire it into release/publish/CI after verify-outputs (never into make test), and add the smoke/ directory to biome's lint allowlist.

Purpose: Close the one gap neither tsc --noEmit nor the default vitest suite structurally reaches — the IIFE/CJS dist/ boundary that is the milestone's flagged risk surface (STATE.md Blockers/Concerns). FCT-04 is a required gate, not optional: a failing smoke test is a hard stop. The verified mechanism (vm.runInContext + createRequire) attaches all five globals with zero new dependencies.
Output: smoke/ test infrastructure + fixture + ambient decls; a wired `make smoke` target; biome + CI coverage.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-forward-compat-typing/08-CONTEXT.md
@.planning/phases/08-forward-compat-typing/08-RESEARCH.md
@.planning/phases/08-forward-compat-typing/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the smoke/ test infrastructure — vitest config, fixture, ambient decls, and the smoke test spec (FCT-04)</name>
  <files>vitest.smoke.config.ts, smoke/fixtures/expected-exports.ts, smoke/node-builtins.d.ts, smoke/dist-exports.smoke.test.ts</files>
  <read_first>
    - .planning/phases/08-forward-compat-typing/08-RESEARCH.md — Pattern 1 (correct IIFE global-attach via vm.runInContext), Pattern 2 (CJS require via createRequire), Code Examples (full EXPECTED_EXPORTS fixture + ambient decls, verified this session), Common Pitfalls #1 (the <script>-element path silently fails), Multi-global shared-window pattern
    - .planning/phases/08-forward-compat-typing/08-PATTERNS.md — vitest.smoke.config.ts / smoke test / fixture / ambient-decl sections (exact analog shapes + the "no resolve.alias" deviation)
    - vitest.config.ts — the analog config to mirror (defineConfig, environment happy-dom) minus the resolve.alias block
    - tests/typecheck-config.test.ts — analog for the config-reading test structure (describe/it nesting, readFileSync + resolve, per-item iteration, the zero-@types/node import comment)
    - tests/node-builtins.d.ts — analog for smoke/node-builtins.d.ts (minimal declare module, header comment convention)
    - .planning/phases/08-forward-compat-typing/08-CONTEXT.md — D-04 (placement/runner), D-05 (exhaustive expected-key assertions; deliberate maintained fixture)
  </read_first>
  <action>Create four files. (1) vitest.smoke.config.ts at repo root: defineConfig with test.environment set to happy-dom and test.include set to the single glob smoke/**/*.smoke.test.ts; do NOT copy vitest.config.ts's resolve.alias block (smoke tests import built dist/ files directly, not src/ via aliases). (2) smoke/fixtures/expected-exports.ts: a named `EXPECTED_EXPORTS` const (as const, no default export) with the five verified key sets from RESEARCH.md Code Examples — core (createEventBus, createEventRegistry, createLifecycleManager, createPluginRegistry, createRouter, createShell), wallet (createEIP1193Provider, createEthereumWallet, createLocalWalletProvider, createWallet), auth (createPassthroughAuth), theme (createCSSTheme), settings (createSettings); include the @deprecated createEthereumWallet intentionally — dropping it silently is exactly the drift D-05 catches. (3) smoke/node-builtins.d.ts: minimal ambient declarations for node:vm (runInContext, isContext, default export) and node:module (createRequire), mirroring tests/node-builtins.d.ts's header-comment convention and zero-@types/node posture. (4) smoke/dist-exports.smoke.test.ts: for each package assert the CJS surface via createRequire(import.meta.url) against dist/index.cjs (and each plugins/*/dist/index.cjs) with Object.keys(mod).sort() deep-equal to the sorted fixture set; and assert the IIFE surface by reading each dist/index.global.js with readFileSync and executing it via vm.runInContext(code, window) against a happy-dom Window, then asserting Object.keys(window.<Global>).sort() equals the sorted fixture set — load all five sequentially into one shared Window in dependency order (core, then settings/wallet/auth/theme) to also prove no-collision coexistence. Resolve all dist/ paths deterministically relative to the repo root / the test file (never from an env var or CLI arg — Tampering mitigation). Load the IIFE ONLY via vm.runInContext against the happy-dom Window; do not use happy-dom's classic script-element evaluation path (it silently drops top-level var globals for this bundle — research Pitfall 1). Always assert the positive exact key set, never merely that nothing threw. Add a one-line comment noting vm.runInContext must only ever execute this repo's own freshly-built dist/ output, never external/untrusted content.</action>
  <verify>
    <automated>make build && npx vitest run --config vitest.smoke.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest.smoke.config.ts exists, uses environment happy-dom, includes only smoke/**/*.smoke.test.ts, and has NO resolve.alias block (source assertion)
    - smoke/fixtures/expected-exports.ts exports a named EXPECTED_EXPORTS const covering all 5 packages with the exact verified key sets; createEthereumWallet is present in the wallet set (source assertion)
    - smoke/node-builtins.d.ts declares node:vm and node:module with no @types/node dependency (source assertion)
    - smoke/dist-exports.smoke.test.ts asserts, per package, that both CJS require() keys and IIFE window.<Global> keys deep-equal the sorted fixture set (behavior assertion)
    - The IIFE is executed via vm.runInContext against a happy-dom Window; the happy-dom script-element path is not used (source assertion)
    - `make build && npx vitest run --config vitest.smoke.config.ts` exits 0 with all 5 globals + 5 CJS surfaces asserted
    - The smoke test asserts positive key sets (deep-equal on sorted keys), not a bare "did not throw" (behavior assertion)
  </acceptance_criteria>
  <done>The smoke/ directory holds a self-contained FCT-04 smoke test that loads the real dist/ IIFE and CJS artifacts and asserts each of the 5 packages' exhaustive export-key sets; it passes against a fresh build.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the `make smoke` target, .PHONY, release/publish chains, biome includes, and CI step (FCT-04)</name>
  <files>Makefile, biome.json, .github/workflows/ci.yml</files>
  <read_first>
    - Makefile — the verify-outputs target (structural analog: build-then-check loop), the .PHONY line (line 7), the release/publish prerequisite chains (build verify-outputs test), PLUGIN_BUILD_ORDER (line 5)
    - .planning/phases/08-forward-compat-typing/08-PATTERNS.md — Makefile smoke-target section (exact target shape, .PHONY registration, release/publish insertion point), ci.yml section, biome.json section
    - biome.json — files.includes array (line 40; allowlist, not ignore-list — Pitfall 3)
    - .github/workflows/ci.yml — current step order (make build → make verify-outputs → make test); insert make smoke after verify-outputs
    - .planning/phases/08-forward-compat-typing/08-RESEARCH.md — Open Question 2 (insert smoke immediately after verify-outputs in both Makefile chains and CI), Pitfall 2 (never fold smoke into make test), Pitfall 3 (biome allowlist)
    - .planning/phases/08-forward-compat-typing/08-CONTEXT.md — D-04 (wire into build/release/CI, not make test)
  </read_first>
  <action>Four edits. (1) Makefile: add a `smoke` target with a `build` prerequisite whose recipe runs `npx vitest run --config vitest.smoke.config.ts` (self-builds first per D-04; mirror verify-outputs' echo style). (2) Makefile .PHONY line: append `smoke` to the existing list. (3) Makefile release and publish targets: insert `smoke` immediately after `verify-outputs` in both prerequisite chains (build verify-outputs smoke test) per Open Question 2. (4) biome.json files.includes: append the glob string smoke/**/*.ts so the new smoke code is actually linted (Pitfall 3 — includes is an allowlist). (5) .github/workflows/ci.yml: add a `- run: make smoke` step immediately after the `- run: make verify-outputs` step. Do NOT add smoke anywhere in the make test target or in vitest.config.ts's include — make test must keep its "never builds" guarantee (Phase 7 constraint, Pitfall 2).</action>
  <verify>
    <automated>make smoke && make lint && make test</automated>
  </verify>
  <acceptance_criteria>
    - `make smoke` exists, has a `build` prerequisite, runs the smoke vitest config, and exits 0 (behavior assertion)
    - `smoke` appears in the Makefile .PHONY line (CLI: `grep -E '^\.PHONY:.*\bsmoke\b' Makefile`)
    - Both `release` and `publish` prerequisite chains contain `smoke` positioned after `verify-outputs` (source assertion)
    - biome.json files.includes contains "smoke/**/*.ts" (source assertion) and `make lint` exits 0 while now scanning smoke/
    - .github/workflows/ci.yml has `make smoke` as a step after `make verify-outputs` (source assertion)
    - The `test` target and vitest.config.ts include are unchanged — `make test` has no build prerequisite and does not reference smoke (source assertion)
  </acceptance_criteria>
  <done>make smoke is a wired, self-building gate present in .PHONY, in the release/publish chains after verify-outputs, and in CI after verify-outputs; smoke/ is in biome's lint allowlist; make test remains build-free.</done>
</task>

</tasks>

<edge_coverage>
## Edge Coverage — spec-less probe fallback (no silent drops)

FCT-04 emitted one probe row (category `unclassified`, "review manually"). It is COVERED as a must_haves.truth: the exhaustive, order-independent (sorted-key) expected-export assertion — a dropped/renamed/added key fails the smoke test (D-05). The generic adjacency/empty/ordering interpretations all map meaningfully here and are folded into that single truth: adjacency → all 5 IIFE globals coexist in one shared window without collision; empty → an undefined/missing global (e.g. a package that failed to load) fails the positive-shape assertion; ordering → keys are compared sorted, so export order is explicitly irrelevant.

No-silent-drop equality (this plan): 1 probe row (FCT-04) = 1 covered truth + 0 flagged assumptions. Combined with Plan 01's 9 rows, all 10 $COVERAGE rows are accounted for (4 covered truths + 6 flagged assumptions across the two plans).

§B prohibition recall survivors authored into must_haves.prohibitions: smoke-never-silent, export-key-never-dropped-without-BREAKING, never-the-script-element-path (all values/safety survivors rooted in the project's "failures visible, never silent" Core Value and D-05). Routine-engineering candidates (CI step ordering, Makefile wiring) dropped as non-survivors.
</edge_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built dist/ artifact → vm.runInContext | The smoke test executes this repo's own freshly-built dist/ JavaScript in a shared V8 context. Trusted content only (never third-party/user input), same trust level as running the project's own compiled code. |
| repo filesystem → smoke test path resolution | dist/ paths are resolved deterministically from the repo root / test file, never from an env var or CLI argument. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Tampering | smoke test dist/ path resolution | medium | mitigate | Resolve all dist/ paths relative to the repo root / test file (never an env var or CLI arg), and always run `make smoke`'s `build` prerequisite immediately before the vitest pass so the artifact under test is always freshly built, never stale. |
| T-08-03 | Information Disclosure / Tampering | vm.runInContext executing bundle source | low | mitigate | Only ever execute this repo's own freshly-built dist/ output; never point vm.runInContext at external/untrusted script content. Enforced by a one-line comment in the smoke test and by resolving only repo-local dist/ paths. |
| T-08-SC | Tampering | npm/pnpm installs (supply chain) | low | accept | Zero new packages introduced (happy-dom, vitest, typescript are existing devDependencies; node:vm/node:module/node:fs/node:path are Node builtins). Research Package Legitimacy Audit = n/a; no [ASSUMED]/[SUS] packages, so no legitimacy checkpoint required. |
</threat_model>

<verification>
- `make smoke` builds first, then the smoke vitest config passes: all 5 IIFE globals attach with exact expected key sets (including shared-window coexistence), all 5 CJS require() surfaces return the same sets.
- `make lint` scans smoke/ (biome includes updated) and is green.
- `make test` is unchanged — no build prerequisite, no smoke reference (Phase 7 "test never builds" guarantee intact).
- CI runs `make smoke` after `make verify-outputs`; release/publish chains include smoke after verify-outputs.
</verification>

<success_criteria>
- FCT-04: IIFE global-attach and CJS require() interop verified intact on the built dist/ artifacts for all 5 packages via `make smoke`.
- The smoke test is a required gate wired into build/release/CI, self-builds, and is never folded into make test.
- A dropped/renamed/added export key fails the smoke test (D-05 exhaustive fixture).
</success_criteria>

<output>
Create `.planning/phases/08-forward-compat-typing/08-02-SUMMARY.md` when done
</output>
