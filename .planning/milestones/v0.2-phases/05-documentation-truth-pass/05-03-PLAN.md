---
phase: 05-documentation-truth-pass
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/configuration.md
  - docs/getting-started.md
  - .planning/phases/05-documentation-truth-pass/drift/03-config-getting-started.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "Every config option and default in configuration.md and getting-started.md matches the values resolved at construction (timeout ?? 30000, cacheTemplates ?? true, sanitizeTemplate undefined, storageKey ?? 'dxkit:wallet', mode 'history', basePath '/')."
    - "getting-started.md contains a concise 0.1.5 → 0.2.0 migration section covering the three breaking changes with before/after config snippets and the timeout: 0/Infinity escape hatch."
    - "All TypeScript config snippets use the nested ShellConfig.lifecycle shape (no flat loaders) and would compile against 0.2.0 types."
    - "Both docs read in the README voice; no filler/hedging."
  artifacts:
    - "docs/configuration.md — corrected defaults + nested lifecycle shape"
    - "docs/getting-started.md — corrected body + migration section (D-05)"
    - "drift/03-config-getting-started.md — before/after record"
  key_links:
    - "config defaults ↔ RESEARCH §Code Truth: Config Defaults Resolved at Construction"
    - "migration section ↔ the three Phase 2/3 breaking changes (timeout default, nested lifecycle + flat-loader throw, route normalize/reject)"
---

<objective>
Verify configuration.md and getting-started.md against the code-truth config-defaults table, and add
the human-readable 0.1.5 → 0.2.0 migration section (D-05) as a section INSIDE getting-started.md (a new
migration page is out of scope — domain boundary allows only one new file, docs/security.md).

Purpose: DOC-01 drift + the migration path; DOC-02 slop bar.
Output: corrected configuration.md, getting-started.md (incl. migration section), drift fragment 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-documentation-truth-pass/05-RESEARCH.md
@.planning/phases/05-documentation-truth-pass/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify docs/configuration.md against code-truth defaults</name>
  <files>docs/configuration.md, .planning/phases/05-documentation-truth-pass/drift/03-config-getting-started.md</files>
  <read_first>
    - docs/configuration.md — full file (existing DOMPurify snippet ~:68-75 is already correct — keep consistent).
    - 05-RESEARCH.md §"Code Truth: Config Defaults Resolved at Construction" (every default), §"Common Pitfalls → Pitfall 2" (hasPlugin Omit), §"Code Truth: Behavior → Template cache".
    - src/shell.ts (config destructure ~:35-49), src/lifecycle.ts (LifecycleManagerOptions defaults), plugins/*/src/index.ts (plugin storageKey/option defaults).
  </read_first>
  <action>
    Verify every option, default, and shape. Correct to: nested lifecycle group (timeout 30000, cacheTemplates
    true, sanitizeTemplate undefined/pass-through, scriptLoader/styleLoader/templateLoader as custom-loader
    overrides with the documented non-true-abort caveat for custom loaders); flat top-level loaders removed
    and now throw at runtime; hasPlugin not consumer-settable via createShell; template cache defaults on with
    clearTemplateCache/invalidateTemplate; wallet/theme/settings storageKey defaults. Ensure every TS snippet
    is the nested shape. Apply D-13 slop bar (remove the booster/hedge words in the verify grep + prose that
    restates the adjacent snippet) and README voice; timeless present (D-07). Log to
    drift/03-config-getting-started.md. Commit `docs(configuration):` citing DOC-01/DOC-02.
  </action>
  <verify>
    <automated>bash -c "grep -q 'lifecycle' docs/configuration.md && grep -q '30000\|30_000\|30 000\|30s\|30 seconds' docs/configuration.md && ! grep -Eiqw 'simply|just|powerful|seamless|robust|leverage' docs/configuration.md"</automated>
  </verify>
  <acceptance_criteria>
    - All config snippets use nested ShellConfig.lifecycle; no flat loader keys shown as current API.
    - Defaults stated: timeout 30000, cacheTemplates true, sanitizeTemplate undefined, storageKey values.
    - Custom-loader non-true-abort limitation noted (Promise.race hang guard).
    - No D-13 booster/hedge words remain.
    - drift/03-config-getting-started.md records configuration.md changes.
  </acceptance_criteria>
  <done>configuration.md matches construction-time defaults; slop-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify docs/getting-started.md and add the 0.1.5 → 0.2.0 migration section</name>
  <files>docs/getting-started.md, .planning/phases/05-documentation-truth-pass/drift/03-config-getting-started.md</files>
  <read_first>
    - docs/getting-started.md — full file (the nested-lifecycle example ~:157-161 is already correct per RESEARCH).
    - 05-RESEARCH.md §"State of the Art" (the 0.1.5→0.2.0 delta table — the migration source), §"Code Truth: Config Defaults" (BREAKING CHANGE confirmed note on flat-loader removal + runtime throw), §"Code Truth: Behavior → Route normalization and rejection".
    - src/shell.ts (flat-loader throw guard ~:20-29; normalizeRoute), CONTEXT.md D-05/D-07.
  </read_first>
  <action>
    Verify every claim, config shape, and lifecycle description against source; correct drift. Add a concise
    "Migrating 0.1.5 → 0.2.0" section covering exactly the three breaking changes: (1) 30000ms default load
    timeout with the timeout: 0 / Infinity escape hatch; (2) nested ShellConfig.lifecycle replacing flat
    scriptLoader/styleLoader/templateLoader, which now throw a runtime Error if present; (3) manifest route
    normalization + rejection of empty/whitespace routes (shell:route dx:error). Include before/after config
    snippets for the loader-nesting change. The generated changelog remains the release record — this is the
    human-readable path, not a changelog dump. Apply D-13 slop bar + README voice; keep timeless present in
    the doc body (the migration section is the ONLY place "0.1.5 → 0.2.0" framing lives — D-07). Log to
    drift/03-config-getting-started.md (append). Commit `docs(getting-started):` citing DOC-01/DOC-02.
  </action>
  <verify>
    <automated>bash -c "grep -qi 'migrat' docs/getting-started.md && grep -q 'lifecycle' docs/getting-started.md && ! grep -Eiqw 'simply|just|powerful|seamless|robust|leverage' docs/getting-started.md"</automated>
  </verify>
  <acceptance_criteria>
    - Migration section present, covering all three breaking changes with before/after loader-nesting snippets and the timeout opt-out.
    - Doc body outside the migration section uses timeless present (no "previously"/"as of" annotations).
    - All TS snippets use the nested lifecycle shape.
    - No D-13 booster/hedge words remain.
    - drift/03-config-getting-started.md records getting-started.md changes incl. the added migration section.
  </acceptance_criteria>
  <done>getting-started.md is source-accurate and carries the migration path; slop-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → consumer config | Consumers copy config snippets verbatim; a wrong default or shape ships a broken/insecure config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03a | Tampering | config snippets (loader shape) | medium | mitigate | A flat-loader snippet would throw at runtime for the consumer; all snippets corrected to nested shape and mechanically compile-checked in Plan 08. |
| T-05-03b | Spoofing (false assurance) | timeout default claim | low | mitigate | Misstating the 30000ms default (or omitting the 0/Infinity opt-out) would hide the un-hangable-by-default behavior; default traced to src/lifecycle.ts. |

No package installs — no package-legitimacy checkpoint.
</threat_model>

<verification>
- Verify greps pass; drift/03-config-getting-started.md exists with both docs' entries.
- Migration snippets are compile-checked authoritatively in Plan 08 (D-04).
</verification>

<success_criteria>
Config defaults are accurate everywhere, the migration path is documented, and both docs are slop-clean.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-truth-pass/05-03-SUMMARY.md` when done.
</output>
