---
phase: 09-continuous-debt-guardrails-registry-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - tests/typecheck-config.test.ts
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "A CI step named for the deprecation gate runs `make typecheck` as its own red check, distinct from the `make test` step (D-05)."
    - "A `tsc` deprecation/type error in project-owned code (`src/`, `tests/`, `plugins/*/src/`) turns the named GATE-01 CI step red."
  artifacts:
    - ".github/workflows/ci.yml — new named `make typecheck` step between `make smoke` and `make test`"
    - "tests/typecheck-config.test.ts — guard assertions that the named GATE-01 CI step exists and invokes `make typecheck`"
  key_links:
    - "CI step → `make typecheck` → `tsc --noEmit -p tsconfig.typecheck.json` (the Phase 7 mechanism GATE-01 makes visible)"
  prohibitions:
    - "MUST NOT widen `tsc` scope to the repo root or drop `-p tsconfig.typecheck.json` (Pitfall 6 — unfixable-red from node_modules noise)."
    - "MUST NOT touch `tsconfig.typecheck.json` include/exclude (D-06 — scope already correct)."
  backstops:
    - statement: "A diagnostic authored under node_modules/ never turns the GATE-01 build red."
      verification: backstop
---

<objective>
GATE-01: make the existing per-package `tsc --noEmit` typecheck a distinct, named, blocking CI
check so a TS6 deprecation/type regression reads as its own red X in GitHub Checks — not buried
inside the generic `make test` step.

Purpose: Continuous visibility for type/deprecation debt, scoped to project-owned paths only.
Output: A named CI step invoking `make typecheck`, plus a guard test locking the wiring in.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-CONTEXT.md
@.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard test for the named GATE-01 CI step (RED)</name>
  <files>tests/typecheck-config.test.ts</files>
  <read_first>
    - tests/typecheck-config.test.ts (existing Makefile/config guard style, esp. lines ~156–225 — readFileSync + regex-match assertions)
    - .github/workflows/ci.yml (current steps: build → verify-outputs → smoke → test)
  </read_first>
  <behavior>
    - Test A: ci.yml contains a step whose `name:` references the deprecation/typecheck gate (GATE-01) and whose `run:` is `make typecheck`.
    - Test B: that named step is distinct from the `- run: make test` step (D-05 — two separate invocations), i.e. `make typecheck` appears as its own `run:` line, not only transitively via `make test`.
  </behavior>
  <action>
    Add a `describe('GATE-01 CI deprecation gate wiring')` block to tests/typecheck-config.test.ts that reads `.github/workflows/ci.yml` via readFileSync and asserts (A) the file contains a named step running `make typecheck` (match a `run:` line invoking `make typecheck` accompanied by a `name:` that references the gate, e.g. contains "GATE-01" or "deprecation"), and (B) the file still contains a separate `run: make test` line — proving the two are distinct steps. Written before the ci.yml edit, so it fails RED until Task 2 lands.
  </action>
  <verify>
    <automated>npx vitest run tests/typecheck-config.test.ts -t "GATE-01"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/typecheck-config.test.ts -t "GATE-01"` FAILS before Task 2 (no named typecheck step yet) and PASSES after Task 2.
    - The new describe block references `make typecheck` and a distinct `make test` line.
  </acceptance_criteria>
  <done>The guard test exists and is RED against the current ci.yml.</done>
</task>

<task type="auto">
  <name>Task 2: Add named typecheck / deprecation gate step to CI (GREEN)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (insert point: after `- run: make smoke`, before `- run: make test`)
    - Makefile (confirm `typecheck` target invokes `tsc --noEmit -p tsconfig.typecheck.json` per package — no change needed here)
  </read_first>
  <action>
    Insert a new step into the `test` job of ci.yml between `- run: make smoke` and `- run: make test`, with `name: Typecheck / deprecation gate (GATE-01)` and `run: make typecheck`. Do NOT change the Node matrix, other steps, or the trailing `make test` step (the existing `make test` → `typecheck` local-dev prerequisite stays as-is per D-05). Do NOT alter `tsconfig.typecheck.json` (D-06 — scope already excludes node_modules by construction).
  </action>
  <verify>
    <automated>npx vitest run tests/typecheck-config.test.ts -t "GATE-01"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GATE-01" .github/workflows/ci.yml` returns the named step.
    - `grep -c "make typecheck" .github/workflows/ci.yml` is at least 1; `grep -c "make test" .github/workflows/ci.yml` is at least 1 (distinct steps).
    - The named step sits after `make smoke` and before `make test`.
    - `make typecheck` still exits 0 on the current clean tree.
  </acceptance_criteria>
  <done>CI has a named, standalone GATE-01 typecheck step; the Task 1 guard test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor commit → CI | untrusted source changes cross into the build; a type/deprecation regression must fail loudly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | CI type/deprecation gate (V14 Configuration) | low | mitigate | Named, blocking CI step invoking `make typecheck`; a project-owned `tsc` error fails the build visibly. Scope stays `src`/`tests` via the untouched `tsconfig.typecheck.json`, so node_modules noise never causes unfixable-red. |
</threat_model>

<artifacts>
## Artifacts this plan produces
- CI step name: `Typecheck / deprecation gate (GATE-01)` (in `.github/workflows/ci.yml`)
- Test: `describe('GATE-01 CI deprecation gate wiring')` in `tests/typecheck-config.test.ts`
</artifacts>

<verification>
- `npx vitest run tests/typecheck-config.test.ts -t "GATE-01"` green.
- `make typecheck` exits 0 on the current tree.
- Manual: the GATE-01 step appears between `make smoke` and `make test` in ci.yml.
</verification>

<success_criteria>
CI fails the build on a `tsc` typecheck/deprecation error scoped to project-owned paths only, surfaced as its own named check (GATE-01, ROADMAP success criterion #1).
</success_criteria>

<output>
Create `.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-01-SUMMARY.md` when done.
</output>
