---
phase: 09-continuous-debt-guardrails-registry-robustness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shell.ts
  - tests/shell.test.ts
autonomous: true
requirements: [ROB-05]
must_haves:
  truths:
    - "A wrong-shape 200 registry body with an explicit `registryUrl` emits `dx:error` (source `shell:manifest`) and does not throw (D-09)."
    - "The same wrong-shape 200 on the DEFAULT silent `/registry.json` probe (no explicit `registryUrl`) STILL emits `dx:error` — ungated (D-10)."
    - "`shell.init()` resolves and `window.__DXKIT__` is exposed after a wrong-shape 200 (fail-closed to `[]`)."
    - "A well-formed array 200 still flows unchanged into `normalizeAndValidateManifests()`."
  artifacts:
    - "src/shell.ts — `Array.isArray()` guard in `loadManifests()` after `await res.json()`"
    - "tests/shell.test.ts — ROB-05 regression tests (explicit + default-probe + init-exposure + happy-path)"
  key_links:
    - "loadManifests() array guard → prevents the non-array value from reaching normalizeAndValidateManifests()'s `for...of` (the current uncaught-TypeError site) before window.__DXKIT__ is set"
  prohibitions:
    - "The array guard MUST NOT be gated by `registryUrlExplicit` (D-10 / P2 — the one deliberate exception to the silent-absence convention)."
    - "MUST NOT move element-level manifest validation out of `normalizeAndValidateManifests()`; ROB-05 only guards the top-level array shape."
  backstops:
    - statement: "A well-formed array 200 still normalizes/validates exactly as before (no regression to the happy path)."
      verification: backstop
---

<objective>
ROB-05 (WR-01): validate that a `200` `registry.json` body is a JSON array before it reaches
`normalizeAndValidateManifests()`. A wrong-shape body emits `dx:error` (source `shell:manifest`)
and fail-closes to `[]` so `init()` completes and `window.__DXKIT__` is exposed — instead of an
uncaught `TypeError` crashing init before the API surface is ever published.

Purpose: Close the last known registry crash path; failures stay visible, never silent, never fatal.
Output: A localized `Array.isArray()` guard + regression tests covering all four D-09/D-10 criteria.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-CONTEXT.md
@.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-RESEARCH.md
@src/shell.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ROB-05 regression tests (RED)</name>
  <files>tests/shell.test.ts</files>
  <read_first>
    - tests/shell.test.ts (lines 1–41 harness/onDxError helper; lines 387–479 existing D-15 registry tests — mirror this exact style, esp. the fetch mock returning `{ ok: true, json: async () => ... }` and the default-probe test at line 460)
    - src/shell.ts (loadManifests() lines 259–290; normalizeAndValidateManifests() line 314 — the current TypeError site)
  </read_first>
  <behavior>
    - Test A (explicit registryUrl): fetch mock returns `ok:true, json:async()=>({not:'an array'})` with `registryUrl: '/custom-registry.json'` → after init: `getManifests()` empty, a `dx:error` with source `shell:manifest` referencing the registry, and `shell.init()` did not throw.
    - Test B (default probe, D-10 ungated): same wrong-shape 200 with `registryUrl` OMITTED (default `/registry.json` probe) → `dx:error` (source `shell:manifest`) STILL fires — distinct from the existing silent-404 default-probe test.
    - Test C (init exposure): after Test A/B scenario, `window.__DXKIT__` is defined post-init.
    - Test D (happy path): fetch mock returns `ok:true, json:async()=>[validManifest]` → the manifest flows through normally, no `dx:error` from the array guard, `getManifests()` contains it.
  </behavior>
  <action>
    Add ROB-05 regression tests to tests/shell.test.ts mirroring the existing D-15 registry tests (fetch mock + `onDxError` capture + `window.fetch` restore). Cover the four behavior cases above. For Test B, omit `registryUrl` entirely and assert the array-guard `dx:error` still fires — explicitly contrasting the silent-404-on-default-probe test at line 460. Assert `shell.init()` resolves (await without throw) and `window.__DXKIT__` is defined afterward. Written before the src fix, so they fail RED.
  </action>
  <verify>
    <automated>npx vitest run tests/shell.test.ts -t "ROB-05"</automated>
  </verify>
  <acceptance_criteria>
    - New tests are titled/tagged so `-t "ROB-05"` selects them.
    - They FAIL before Task 2 (today the wrong-shape body throws / no guard emit) and PASS after.
    - Test B omits `registryUrl` and still expects a `shell:manifest` `dx:error`.
  </acceptance_criteria>
  <done>Four ROB-05 regression tests exist and are RED against current src/shell.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Array.isArray guard in loadManifests() (GREEN)</name>
  <files>src/shell.ts</files>
  <read_first>
    - src/shell.ts (loadManifests() lines 259–290 — the `return await res.json();` at line 274 is the fix site; existing `dx:error`/`source: 'shell:manifest'` emit convention at lines 267–270 and loadDappManifest message shape ~lines 213–217)
  </read_first>
  <action>
    In `loadManifests()`, replace `return await res.json();` with: assign the parsed value to a const, then `if (!Array.isArray(parsed))` emit `dx:error` with `source: 'shell:manifest'` and an `Error` whose message follows the existing "from `${registryUrl}` — <reason>" shape (state the registry body is not a JSON array of manifests and include the received `typeof parsed`), then `return []`. This emit MUST be UNGATED — do NOT wrap it in `if (registryUrlExplicit)` (D-10 / P2). On the array path, `return parsed`. Leave the `!res.ok` and `catch` branches (D-15 gated emits) unchanged, and do NOT alter `normalizeAndValidateManifests()`.
  </action>
  <verify>
    <automated>npx vitest run tests/shell.test.ts -t "ROB-05" && make typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/shell.test.ts -t "ROB-05"` passes all four cases.
    - `npx vitest run tests/shell.test.ts` (full file) stays green — no regression to existing D-15 tests.
    - The new guard is not wrapped in `registryUrlExplicit`.
    - `make typecheck` exits 0.
  </acceptance_criteria>
  <done>loadManifests() array-guards the 200 body, fail-closes to `[]`, and always emits on a wrong shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote registry.json → shell.init() | untrusted network JSON crosses into the shell's init sequence before window.__DXKIT__ is exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Denial of Service | loadManifests() registry parse (V5 Input Validation) | medium | mitigate | `Array.isArray()` guard on the parsed 200 body; fail closed to `[]` + a visible ungated `dx:error` (source `shell:manifest`), so a malformed/attacker-influenced registry can never throw an uncaught `TypeError` that prevents `window.__DXKIT__` from being exposed. Element-level validation stays in `normalizeAndValidateManifests()`. |
</threat_model>

<artifacts>
## Artifacts this plan produces
- Source guard: `Array.isArray()` check in `loadManifests()` (src/shell.ts) + ungated `dx:error` (source `shell:manifest`)
- Tests: four ROB-05 regression cases in `tests/shell.test.ts` (explicit / default-probe / init-exposure / happy-path)
</artifacts>

<verification>
- `npx vitest run tests/shell.test.ts` fully green (ROB-05 cases + existing D-15 cases).
- `make typecheck` exits 0.
</verification>

<success_criteria>
`loadManifests()` validates registry.json is an array; a wrong-shape 200 emits `dx:error` (source `shell:manifest`) instead of throwing an uncaught `TypeError` in `init()` before `window.__DXKIT__` is exposed — even on the default silent probe (ROB-05 / D-09, D-10, ROADMAP success criterion #4).
</success_criteria>

<output>
Create `.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-04-SUMMARY.md` when done.
</output>
