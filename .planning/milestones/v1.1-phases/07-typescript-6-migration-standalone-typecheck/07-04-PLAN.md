---
phase: 07-typescript-6-migration-standalone-typecheck
plan: 04
type: execute
wave: 3
depends_on: [07-03]
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [TS6-01, TS6-02]
must_haves:
  truths:
    - "The root `typescript` devDependency range is `^6.0.0` and the resolved lockfile version is a 6.0.x (latest stable, 6.0.3 at research time) — TS6-01's '6.0.x' satisfied by the resolved pin (D-08)"
    - "`make typecheck` exits 0 under TypeScript 6.0.x across the root package + all 4 plugins (core + all plugins compile clean, TS6-01)"
    - "`grep -rn ignoreDeprecations` across all tsconfig*.json returns nothing — every deprecation TS6 surfaces is resolved at source, not suppressed (TS6-02)"
    - "The full vitest suite (321 tests) stays green after the bump (Success Criterion 4)"
    - "`make build && make verify-outputs` succeed under TS6 — all 3 output formats x 5 packages still emit"
    - "The bump is its own commit (may go red), and any at-source deprecation fixes are separate follow-up commits — bisectable against the green baseline (D-07 steps 2-3)"
  artifacts:
    - "package.json — `typescript` devDep bumped `^5.8.3` -> `^6.0.0`"
    - "pnpm-lock.yaml — regenerated, pinning the resolved 6.0.x"
  key_links:
    - "the bump lands only AFTER the green baseline (07-03) is committed, so the TS6 diff is measurable and bisectable (Success Criterion 1 / D-07)"
    - "CI runs `pnpm install --frozen-lockfile` then `make build` + `make verify-outputs` + `make test` — the lockfile update MUST land in the same commit as the package.json bump"
  prohibitions:
    - "an ignoreDeprecations shim is added to any tsconfig (build or typecheck) to make a TS6 deprecation disappear instead of fixing at source"
    - "a manufactured change is invented to satisfy TS6-02 when zero deprecations actually surface (the degenerate empty-fixes case is valid)"
    - "the typescript devDep is pinned to an exact patch instead of the caret `^6.0.0` (breaks the D-08 caret convention; the lockfile pins the exact version)"
---

<objective>
Bump the root `typescript` devDependency from `^5.8.3` to `^6.0.0` (resolving to the latest stable
6.0.x), regenerate the lockfile, then run the full baseline under TS6 and resolve at source any
deprecation or error TS6 surfaces — with NO `ignoreDeprecations` shim. Research empirically
verified `src/` across all 5 packages is already TS6-clean and the pre-existing test errors are
fixed in the baseline, so the fixes step is very likely empty (a valid degenerate outcome — do not
manufacture changes).

Purpose: Completes TS6-01 (compile clean on 6.0.x) and TS6-02 (no shim), landing strictly after
the green pre-bump baseline so the change is measurable and bisectable (Success Criterion 1 / D-07).
Output: an updated `package.json` + `pnpm-lock.yaml` and a green `make typecheck`/`make test`/
`make build`/`make verify-outputs` under TypeScript 6.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-CONTEXT.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-PATTERNS.md
</context>

## Artifacts this phase produces

This plan (07-04) produces:
- **`package.json`** — root `typescript` devDependency range `^5.8.3` -> `^6.0.0` (D-08 caret convention).
- **`pnpm-lock.yaml`** — regenerated to pin the resolved 6.0.x.
- **At-source deprecation fixes** (if any surface) in `src/` / `plugins/*/src/` / tests — expected empty per RESEARCH (degenerate valid case).

Depends on the committed green baseline from 07-03 (`make typecheck` + `make test` green on TS 5.9.3). This is the final plan of phase 07.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump typescript to ^6.0.0 and regenerate the lockfile (its own commit)</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (line 51 `"typescript": "^5.8.3"` — the exact line to change)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md (Standard Stack: `pnpm add -D typescript@^6.0.0 -w`; version verification — 6.0.3 is latest 6.0.x; D-08)
    - .planning/phases/06-toolchain-audit-modernization/06-CONTEXT.md (D-03 caret convention; the pnpm-add reformat gotcha noted in STATE 06-03)
  </read_first>
  <action>
    Run `pnpm add -D typescript@^6.0.0 -w` from the repo root to bump the root `typescript` devDep
    range to `^6.0.0` and update `pnpm-lock.yaml` to the resolved latest stable 6.0.x (6.0.3 at
    research time; pick whatever `^6.0.0` resolves at execution time — Claude's discretion, D-08).
    Keep the caret range (do NOT pin an exact patch — D-08; the lockfile pins the exact resolved
    version). Per Phase 6 D-03/STATE learning, `pnpm add` may reformat untouched `package.json`
    keys — revert any incidental formatting so the diff is scoped to the single `typescript` line +
    the lockfile. Commit this as its own bisectable commit (D-07 step 2); it is allowed to go red
    on typecheck/build until the fixes step. Do NOT add `@types/node` or any new devDependency here.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.devDependencies.typescript!=='^6.0.0') throw new Error('range not ^6.0.0: '+p.devDependencies.typescript); console.log('range ok')" && npx tsc --version | grep -E "Version 6\.0\."</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies `typescript` is exactly `"^6.0.0"`
    - `npx tsc --version` reports a `6.0.x` version
    - `pnpm-lock.yaml` is updated and resolves typescript to that 6.0.x
    - `git diff package.json` touches only the `typescript` line (no incidental reformatting)
  </acceptance_criteria>
  <done>TypeScript is bumped to `^6.0.0` (resolved 6.0.x) as an isolated, bisectable commit with the lockfile updated in lockstep.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify full baseline under TS6 and resolve any deprecations at source</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md (headline: src already TS6-clean, zero net-new errors; Pitfall 3 baseUrl/TS5101; degenerate empty-fixes case; Assumptions A1/A2 on plugin builds)
    - tsconfig.json + tsconfig.typecheck.json + plugins/*/tsconfig*.json (confirm no ignoreDeprecations anywhere)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-CONTEXT.md (D-07 fixes step; degenerate case guidance — do not manufacture changes)
  </read_first>
  <action>
    Under TypeScript 6, run `make typecheck`, `make build`, `make verify-outputs`, and `make test`.
    For any error or deprecation TS6 surfaces, resolve it AT SOURCE in `src/`/`plugins/*/src/` (or
    the relevant test), grouped into logical follow-up commits per package or per deprecation class
    (D-07 step 3). Never add `ignoreDeprecations` to any tsconfig and never suppress with
    `@ts-expect-error`/`any` (TS6-02). Per RESEARCH the expected outcome is ZERO net-new errors
    (src is already TS6-clean; test errors were fixed in the baseline) — if nothing surfaces, this
    task is verification-only and the fixes step is legitimately empty (do NOT invent changes to
    satisfy TS6-02; it is trivially satisfied by the absence of any shim). If a plugin's `tsup`
    build breaks under TS6 (RESEARCH Assumption A1 flags this as untested for the 4 plugins/IIFE),
    fix it at source in the fixes commit. Confirm `grep -rn ignoreDeprecations` across all
    tsconfig*.json returns nothing.
  </action>
  <verify>
    <automated>make typecheck && make build && make verify-outputs && make test && test -z "$(grep -rn ignoreDeprecations tsconfig.json tsconfig.typecheck.json plugins/*/tsconfig*.json)"</automated>
  </verify>
  <acceptance_criteria>
    - `make typecheck` exits 0 under TS6 (root + 4 plugins compile clean — TS6-01)
    - `make build && make verify-outputs` succeed (3 formats x 5 packages emit under TS6)
    - `make test` exits 0 (full vitest suite green — Success Criterion 4)
    - `grep -rn "ignoreDeprecations" tsconfig.json tsconfig.typecheck.json plugins/*/tsconfig*.json` returns nothing (TS6-02)
    - No `@ts-expect-error`/`as any` was added purely to silence a TS6 deprecation
  </acceptance_criteria>
  <done>Core + all 4 plugins compile clean and build under TypeScript 6 with the vitest suite green and zero `ignoreDeprecations` shims anywhere.</done>
</task>

</tasks>

<flagged_assumptions>
Spec-less probe edges relevant to this plan (surfaced, not silently dropped):
- **TS6-02 / empty:** zero deprecations surface under TS6 (RESEARCH degenerate case) — TS6-02 is trivially satisfied by the absence of any `ignoreDeprecations` shim, and the fixes step is legitimately empty. No manufactured changes.
- **TS6-02 / adjacency:** resolving one deprecation (if any) does not reintroduce another elsewhere — verified by re-running `make typecheck` to a clean exit after each fix commit.
- **TS6-02 / ordering:** the order deprecation fixes are applied is irrelevant to the final green state (none expected). Confirmed by final `make typecheck` exit 0.
- **RESEARCH Assumption A1 (flagged):** only the root package's ESM/CJS/dts build was smoke-tested under TS6.0.3; the 4 plugins and the IIFE target were not. If a plugin build breaks at the bump commit, it surfaces at `make build`/`make verify-outputs` in this plan and is fixed in the fixes step (D-07 step 3).
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supply chain (devDep bump) | `typescript` devDependency is bumped via pnpm, updating `pnpm-lock.yaml`'s hash-pinned resolution. No new package/registry is introduced; TypeScript remains a devDependency (zero runtime deps preserved). |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | typescript devDep / pnpm-lock.yaml | low | mitigate | pnpm lockfile hash-pinning is the control (unchanged process from every Phase 6 devDep bump); the bump is an official Microsoft package version change, not a new dependency. |
| T-07-SC | Tampering | pnpm install (typescript@^6.0.0) | low | accept | Single existing devDep version bump, no new/`[ASSUMED]`/`[SUS]` package (RESEARCH Package Legitimacy Audit: `typescript` OK/Approved) — no blocking human checkpoint required. No new runtime attack surface. |

No new runtime attack surface is introduced by this plan (build-time compiler bump only).
</threat_model>

<verification>
- `make typecheck` exits 0 under TS6 (root + 4 plugins)
- `make build && make verify-outputs` succeed under TS6 (3 formats x 5 packages)
- `make test` exits 0 (full vitest suite green)
- `grep -rn "ignoreDeprecations" tsconfig.json tsconfig.typecheck.json plugins/*/tsconfig*.json` returns nothing
- `npx tsc --version` reports 6.0.x; `package.json` typescript range is `^6.0.0`
</verification>

<success_criteria>
Core + all four plugins compile clean and build under TypeScript 6.0.x, the standalone typecheck
and full vitest suite are green, and no `ignoreDeprecations` shim exists in any tsconfig — landed
strictly after the committed pre-bump baseline so the migration is measurable and bisectable.
</success_criteria>

<output>
Create `.planning/phases/07-typescript-6-migration-standalone-typecheck/07-04-SUMMARY.md` when done.
</output>
