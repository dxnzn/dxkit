---
phase: 06-toolchain-audit-modernization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [TOOL-02]
must_haves:
  truths:
    - "CI runs the full suite on Node 22 and Node 24, and no longer includes EOL Node 18/20 (TOOL-02, D-07)"
  artifacts:
    - ".github/workflows/ci.yml with matrix node-version: [22, 24]"
  key_links:
    - "pnpm/action-setup@v4 must stay before actions/setup-node@v4 — cache: pnpm depends on pnpm already being installed (RESEARCH Pitfall 6)"
---

<objective>
Move the CI Node matrix off EOL Node 20 to the supported LTS lines: `node-version: [20]`
→ `node-version: [22, 24]` (D-07). Node 22 is the new floor, Node 24 is current stable —
testing on 24 catches forward-compat breakage early.

Purpose: TOOL-02 — CI runs on Node 22 and no longer tests EOL Node 18/20.
Output: One edited `.github/workflows/ci.yml` (a single matrix-values change).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CI Node matrix to [22, 24]</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md (ci.yml full current content + the single-line matrix diff; do-not-reorder note)
    - .planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md (Pitfall 6: setup-node cache: pnpm ordering; Code Examples matrix diff)
  </read_first>
  <action>
    In `.github/workflows/ci.yml`, change the `strategy.matrix.node-version` value from
    `[20]` to `[22, 24]` (D-07). Change ONLY the matrix values — do not add, remove, or
    reorder any step. Specifically, keep `pnpm/action-setup@v4` BEFORE `actions/setup-node@v4`;
    the `cache: pnpm` option on setup-node depends on pnpm already being installed by the
    prior step (RESEARCH Pitfall 6), and this ordering is already correct. Keep the existing
    `make build` and `make test` steps as-is — no new CI steps this phase (typecheck/lint
    gates arrive in Phases 7/9). Commit as `ci: run CI on Node 22 and 24, drop EOL Node 20`
    with the required `Co-Authored-By:` line.
  </action>
  <verify>
    <automated>grep -Eq 'node-version:\s*\[22,\s*24\]' .github/workflows/ci.yml &amp;&amp; ! grep -Eq 'node-version:.*\b(18|20)\b' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` matrix is `node-version: [22, 24]`.
    - No `18` or `20` remains in the node-version matrix.
    - `pnpm/action-setup@v4` still precedes `actions/setup-node@v4`; `make build` + `make test` steps unchanged.
  </acceptance_criteria>
  <done>CI matrix is node-version: [22, 24] with no 18/20; step ordering and build/test steps unchanged.</done>
</task>

</tasks>

<artifacts_produced>
- CHANGED `.github/workflows/ci.yml` — `node-version` matrix `[20]` → `[22, 24]`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI config → GitHub Actions runner | Workflow YAML controls which runtimes execute the build/test suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Tampering | ci.yml step ordering | low | mitigate | Only matrix values change; pinned action refs (@v4) and pnpm-before-setup-node ordering preserved (RESEARCH Pitfall 6) |
| T-06-02-02 | Denial of Service | CI green on an unsupported Node | low | accept | Matrix (22, 24) matches the enforced engines floor; frozen-lockfile install unchanged |
</threat_model>

<verification>
- CI matrix reads `[22, 24]`; no 18/20 present.
- Step ordering and existing build/test steps unchanged.
</verification>

<success_criteria>
TOOL-02 satisfied: CI runs on Node 22 (and 24) and no longer tests EOL Node 18/20.
</success_criteria>

<output>
Create `.planning/phases/06-toolchain-audit-modernization/06-02-SUMMARY.md` when done.
</output>
