---
phase: 06-toolchain-audit-modernization
plan: 05
type: execute
wave: 4
depends_on: [06-03, 06-04]
files_modified:
  - Makefile
autonomous: true
requirements: [TOOL-05]
must_haves:
  truths:
    - "After the toolchain bumps, all three build outputs (ESM dist/index.js, CJS dist/index.cjs, IIFE dist/index.global.js) are present for the root package and each of the four plugins (TOOL-05, D-09)"
    - "A repeatable existence check exists (a make target / shell loop), not the deeper FCT-04 artifact smoke test (that is Phase 8)"
  artifacts:
    - "A `verify-outputs` (or equivalently named) Makefile target that test -f's all three outputs per package"
  key_links:
    - "make build must run first (root tsup + PLUGIN_BUILD_ORDER); the check reads dist/ for root and all four plugin dirs"
---

<objective>
Add and run the build-output presence check for TOOL-05: after the toolchain bumps, confirm
all three output formats (ESM `dist/index.js`, CJS `dist/index.cjs`, IIFE
`dist/index.global.js`) still emit for the root package and each of the four plugins (D-09).
This is file-existence confirmation ONLY — the deeper artifact smoke test (IIFE global attach
+ CJS require() interop) is FCT-04 in Phase 8; do not build it here.

Purpose: TOOL-05 — the Wave 0 validation gap (no existing check covers this).
Output: A small `verify-outputs` Makefile target plus a green run against a fresh `make build`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md
@.planning/phases/06-toolchain-audit-modernization/06-VALIDATION.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a verify-outputs Makefile target for all three formats per package</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (current build/test targets, PLUGIN_BUILD_ORDER, PLUGIN_DIRS variables — reuse them for the package directory list)
    - .planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md (Don't Hand-Roll: use `test -f` chains, not a smoke-test harness; Validation Architecture TOOL-05 command shape)
    - .planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md (Shared Patterns: build-output existence loop; PLUGIN_BUILD_ORDER as the directory list)
  </read_first>
  <action>
    Add a new `.PHONY` Makefile target named `verify-outputs` that, for the root package and
    for each plugin directory (reuse the existing `PLUGIN_BUILD_ORDER` / `PLUGIN_DIRS`
    Makefile variables — do not hardcode a separate list), asserts that all three outputs
    exist: `dist/index.js`, `dist/index.cjs`, and `dist/index.global.js`. Use a `test -f`
    chain per package inside a shell `for` loop, failing (`exit 1`) with a clear message
    naming the missing file/package if any output is absent (D-09 scope is existence only —
    do NOT load or execute the artifacts; that is FCT-04 / Phase 8). Add the target to the
    existing `.PHONY` line. Do not alter the `build`, `test`, or `lint` targets. Commit as
    `chore(build): add verify-outputs target asserting all three formats per package` with
    the `Co-Authored-By:` line.
  </action>
  <verify>
    <automated>make clean &amp;&amp; make build &amp;&amp; make verify-outputs</automated>
  </verify>
  <acceptance_criteria>
    - `make verify-outputs` exists as a `.PHONY` target and exits 0 after a fresh `make build`.
    - It checks `dist/index.js`, `dist/index.cjs`, `dist/index.global.js` for root + all four plugins, deriving the plugin list from the existing Makefile variables.
    - It fails with a clear message if any output is missing; it does not load/execute artifacts.
    - `build`/`test`/`lint` targets unchanged.
  </acceptance_criteria>
  <done>make verify-outputs target added; passes after a fresh build; checks all three formats for root + 4 plugins.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full phase gate and confirm all outputs present post-bump</name>
  <files>Makefile</files>
  <read_first>
    - .planning/phases/06-toolchain-audit-modernization/06-VALIDATION.md (Sampling Rate — phase gate is full make build && make test green plus the TOOL-05 existence check)
  </read_first>
  <action>
    Run the full phase gate against the completed toolchain state: `make clean &amp;&amp; make build
    &amp;&amp; make test &amp;&amp; make verify-outputs`. Confirm all three outputs are present for the root
    package and every plugin, and that the full suite is green on the bumped toolchain. Record
    the resulting file listing (the 15 output files: 3 formats × 5 packages) as TOOL-05
    evidence in the phase SUMMARY. No code changes in this task — it is the phase-level
    confirmation that the bumps (Plans 03–04) did not break emit. If any output is missing,
    surface it as a gap for the relevant bump plan rather than patching around it here.
  </action>
  <verify>
    <automated>make build &amp;&amp; make test &amp;&amp; make verify-outputs</automated>
  </verify>
  <acceptance_criteria>
    - `make build && make test && make verify-outputs` all green on the final toolchain.
    - The 15 expected output files (3 formats × 5 packages) are confirmed present and listed in the SUMMARY.
  </acceptance_criteria>
  <done>Full phase gate green (build + test + verify-outputs); 15 outputs confirmed present and recorded.</done>
</task>

</tasks>

<artifacts_produced>
- CHANGED Makefile — new `verify-outputs` target asserting all three output formats exist for root + 4 plugins.
- TOOL-05 evidence in the phase SUMMARY — confirmed presence of the 15 build outputs on the bumped toolchain.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling → dist artifacts | tsup emit is the source of the files the check verifies; the check only reads existence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05-01 | Spoofing | stale dist/ masking a broken emit | low | mitigate | Phase gate runs `make clean` before `make build` so the check verifies freshly-emitted artifacts, not stale ones |
| T-06-05-02 | Tampering | verify-outputs false-green (weak check) | low | mitigate | Explicit `test -f` per format per package with fail-on-missing; scope deliberately limited to existence (deeper interop is FCT-04/Phase 8) |
</threat_model>

<verification>
- `make verify-outputs` passes after a fresh `make clean && make build`.
- Full `make build && make test && make verify-outputs` green on the bumped toolchain.
- 15 output files (3 × 5) confirmed present and recorded.
</verification>

<success_criteria>
TOOL-05 satisfied: all three build outputs (ESM/CJS/IIFE) are produced per package and
verified present after the toolchain bumps, via a repeatable existence check.
</success_criteria>

<output>
Create `.planning/phases/06-toolchain-audit-modernization/06-05-SUMMARY.md` when done.
</output>
