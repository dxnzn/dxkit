---
phase: 04-testing-stress-edge-case-regression-coverage
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/shell.ts
  - tests/shell.test.ts
  - tests/router.test.ts
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "A manifest route 'blog' (no leading slash) resolves at '/blog' after normalization (D-06)"
    - "An empty/whitespace-only route is rejected with a dx:error and is not routed (D-06 reject-unfixable)"
    - "Invalid manifests are discarded with a shell:manifest dx:error regardless of which of the three tiers (dapp entries, inline, registry.json) produced them (D-07)"
    - "Two manifests declaring the same exact route keep first-registered-wins resolution AND the shell emits a dx:error naming both conflicting ids (D-08)"
    - "loadDappManifest fetch failure, non-2xx response, and JSON-parse failure each emit a dx:error instead of silently returning null (WR-01)"
  artifacts:
    - "src/shell.ts — single-choke-point normalize+validate+dedupe step run once in init() after loadManifests(); WR-01 dx:error emits in loadDappManifest"
    - "tests/shell.test.ts — normalization, reject-unfixable, tier-parity, duplicate-route, and WR-01 cases"
    - "tests/router.test.ts — multi-match longest-prefix (assert as-is) + exact-duplicate first-wins resolution cases"
  key_links:
    - "init(): manifests = await loadManifests() → normalizeAndValidateManifests(manifests) → initEnabledState()/createRouter()"
    - "loadDappManifest catch / !res.ok early-return → events.emit('dx:error', { source: 'shell:manifest', ... })"
---

<objective>
Close the manifest/route validation gaps and the WR-01 silent-swallow, then land the edge-case tests (TEST-02): invalid route formats, deep-merge override behavior (covered in plan 03), and multi-match/duplicate routes each asserting the correct accept/reject/merge/emit outcome.

Purpose: Today `isValidManifest` guards only the dapp-entries tier (`src/shell.ts:177`) — inline manifests (`:202-204`) and `registry.json` results (`:206-213`) flow through unvalidated; routes are matched verbatim with no normalization so `"blog"` silently dead-routes; duplicate routes silently shadow; and `loadDappManifest`'s `catch { return null; }` (`:190-192`) swallows 404s and corrupt JSON while its sibling validation failure emits `dx:error`. Every gap resolves toward visibility per the milestone's no-silent-failures charter (D-05/D-06/D-07/D-08/WR-01).

Output: A shell-owned normalize+validate+dedupe choke point plus WR-01 emits in `src/shell.ts`, with regression coverage in `tests/shell.test.ts` and `tests/router.test.ts`. Suite ships green.
</objective>

<artifacts_produced>
Symbols this plan creates (for source-grounding / drift exclusion):

- `src/shell.ts`:
  - `normalizeAndValidateManifests(manifests): DappManifest[]` — new shell-owned helper (name discretionary) invoked once from `init()` after `loadManifests()`; performs route normalization (D-06), reject-unfixable, tier-uniform `isValidManifest` filtering (D-07), and duplicate-route detection (D-08)
  - a route-normalization helper (leading-slash/trailing-slash subset of `router.ts` `normalizePath`, basePath-stripping omitted) — inline or a small named function
  - new `dx:error` source string `shell:route` for reject-unfixable (per RESEARCH Open Question 1 recommendation)
  - WR-01 `dx:error` emits (source `shell:manifest`) in `loadDappManifest` on `!res.ok` and in the `catch` block, distinguishing fetch-failure vs invalid-JSON via `cause`
- `tests/shell.test.ts` — new cases (normalization, reject, tier parity, duplicate-route, WR-01)
- `tests/router.test.ts` — new cases (multi-match assert-as-is, exact-duplicate first-wins)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-testing-stress-edge-case-regression-coverage/04-CONTEXT.md
@.planning/phases/04-testing-stress-edge-case-regression-coverage/04-RESEARCH.md
@.planning/phases/04-testing-stress-edge-case-regression-coverage/04-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shell-owned manifest normalize + validate + dedupe + WR-01 emits (src/shell.ts)</name>
  <files>src/shell.ts</files>
  <read_first>
    - src/shell.ts (the file being modified — `isValidManifest()` at 160-170, `loadDappManifest()` at 172-193, `loadManifests()` three-tier fallback at 195-216, `init()` at 218-269 especially `manifests = await loadManifests()` at 228 and `createRouter` at 250, the existing manifest-validation dx:error emit template at 177-185)
    - src/router.ts (`normalizePath()` at 27-39 — the exact leading-slash/trailing-slash algorithm to reuse; note `resolve()` at 41-51 and the construction-time stable sort at 24 which already guarantees first-registered-wins for equal-length routes)
    - src/utils.ts (deepMerge — used by loadDappManifest overrides at :187; do not change it here)
    - plugins/settings/src/index.ts (the `{ cause: err }` error-wrapping convention referenced at :60-64 to mirror for WR-01)
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-RESEARCH.md ("Manifest & Route Validation Design", "Assumptions Log" A1-A3, "Open Questions" 1 and 3)
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-PATTERNS.md (src/shell.ts section — exact fix sites and emit templates)
  </read_first>
  <action>
    Add a single shell-owned normalize+validate+dedupe choke point (RESEARCH shape 2 — keeps `router.ts` free of an EventBus dependency it does not have today) and close WR-01. Concretely:

    1. Add a helper (e.g. `normalizeAndValidateManifests(list: DappManifest[]): DappManifest[]`) and call it once in `init()` on the result of `loadManifests()` — i.e. `manifests = normalizeAndValidateManifests(await loadManifests());` at line 228 — BEFORE `initEnabledState()` and `createRouter()`. Running it once per shell lifetime (not per `rebuildRouter()`) is correct: enable/disable does not change the manifest list.

    2. Route normalization (D-06): normalize each `manifest.route` using the leading-slash/trailing-slash subset of `router.ts` `normalizePath` (ensure leading slash; strip trailing slash except root) — OMIT the basePath-stripping branch (manifest routes are declared without a basePath prefix). So `"blog"` → `"/blog"`, `"/blog/"` → `"/blog"`. Assign the normalized value back onto the manifest route.

    3. Reject-unfixable (D-06): treat `route.trim() === ''` (empty or whitespace-only) as unfixable — discard that manifest from the returned list and emit `events.emit('dx:error', { source: 'shell:route', error: new Error(...) })` naming the offending manifest id (per RESEARCH Assumption A1 boundary + Open Question 1 recommendation for the `shell:route` source). `isValidManifest` already guarantees `route` is a string before this runs.

    4. Tier-uniform validation (D-07): run `isValidManifest` over EVERY manifest in the list regardless of tier; discard invalid ones and emit the existing `shell:manifest` dx:error (mirror the template at 177-185, naming the source manifest). This makes the inline-manifests tier (:202-204) and registry.json tier (:206-213) validate identically to the dapp-entries tier.

    5. Duplicate-route detection (D-08): after normalization/validation, walk the list with a `Map<string, string>` (route → first-seen id) first-seen-wins tracker. On a second manifest with an already-seen exact route, KEEP it in the list (first-registered-wins is already guaranteed at resolution time by the stable construction-time sort in router.ts:24 — do NOT change router.ts) but emit `events.emit('dx:error', { source: 'shell:manifest', error: new Error(...) })` naming BOTH conflicting ids so the collision is visible. Overlapping-prefix multi-match (`/tools` vs `/tools/sender`) is NOT a duplicate — leave it untouched (longest-route-wins, asserted as-is in Task 2).

    6. WR-01 (fold): in `loadDappManifest` (172-193), replace the silent `catch { return null; }` (190-192) with an `events.emit('dx:error', { source: 'shell:manifest', error: ... })` before `return null`, wrapping the caught error with `{ cause: err }` and a message distinguishing the failure mode (the catch covers both network throw and `res.json()` parse failure — reflect that in the message). ALSO add a `dx:error` emit on the `!res.ok` early-return (line 175) covering the non-2xx/HTTP-failure mode (RESEARCH Open Question 3 recommendation: cover all three failure modes — fetch throw, non-2xx, JSON parse). Use `source: 'shell:manifest'` per the CONTEXT discretion "mirroring" instruction.

    BREAKING CHANGE judgment (per D discretion): route normalization, tier validation, and duplicate-route emit are behavior changes. Judge each against the milestone rule (justified + migration notes). Route normalization makes previously-dead routes work (additive-feeling, low risk); tier validation may now discard manifests that previously loaded unvalidated (behavior change — warrants a `BREAKING CHANGE:` footer + migration note if any previously-accepted manifest would now be rejected); duplicate-route and WR-01 are additive dx:error emits (no footer). Add `BREAKING CHANGE:` footers only where a consumer's previously-working input now behaves differently, with migration notes. Follow project comment style (one-line "why").

    Do NOT modify router.ts. Do NOT modify deepMerge.
  </action>
  <verify>
    <automated>pnpm vitest run tests/shell.test.ts</automated>
  </verify>
  <done>
    src/shell.ts validates all three manifest tiers, normalizes routes with reject-unfixable, emits dx:error on duplicate routes and on WR-01 fetch/HTTP/parse failures; existing shell.test.ts tests still pass except the WR-01 anchor test whose assertion is updated in Task 3.
  </done>
  <acceptance_criteria>
    - `src/shell.ts` `init()` calls the normalize/validate helper on the `loadManifests()` result before `createRouter()`.
    - `loadDappManifest` emits `dx:error` (source `shell:manifest`) in both the `catch` block and the `!res.ok` branch; the catch-block emit wraps the error with `cause`.
    - A `shell:route` dx:error source string exists for the reject-unfixable path.
    - `git grep -n "source: 'shell:route'" src/shell.ts` returns at least one hit.
    - No change to `src/router.ts` or `src/utils.ts` (`git diff --name-only` for this task lists only `src/shell.ts`).
    - Any `BREAKING CHANGE:` footer added carries a migration note.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Router-level multi-match + exact-duplicate resolution tests (tests/router.test.ts)</name>
  <files>tests/router.test.ts</files>
  <read_first>
    - tests/router.test.ts (the file being extended — read `uses longest prefix match` at 54-65, the `construction-time sort caching` describe at 151+, and the manifest factory conventions)
    - src/router.ts (`resolve()` at 41-51, construction-time sort at 24 — stable sort guarantees equal-length routes preserve insertion order)
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-RESEARCH.md ("D-08 duplicate-route detection" — the stable-sort first-wins guarantee)
  </read_first>
  <action>
    Extend tests/router.test.ts with the pure router-resolution edge cases D-08 requires (no dx:error at this layer — that is shell-owned, tested in Task 3):

    1. Multi-match / overlapping-prefix (assert as-is per D-08): assert longest-route-wins for `/tools` vs `/tools/sender` — `resolve('/tools/sender')` returns the `/tools/sender` manifest and `resolve('/tools/other')` returns the `/tools` manifest. The existing `uses longest prefix match` test (54-65) likely already covers this; add an explicit case only if a gap exists, do not duplicate.
    2. Exact-duplicate first-registered-wins resolution: construct a router with two manifests declaring the identical exact route (e.g. both `/dup`) in a known input order; assert `resolve('/dup')` returns the FIRST-registered manifest (stable-sort guarantee). This locks the resolution half of D-08 (the dx:error emit half is Task 3).

    Use the existing inline manifest factory and `router.destroy()` cleanup conventions already in the file.
  </action>
  <verify>
    <automated>pnpm vitest run tests/router.test.ts</automated>
  </verify>
  <done>
    tests/router.test.ts asserts longest-prefix multi-match and exact-duplicate first-registered-wins resolution; all router tests pass green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/router.test.ts` exits 0.
    - A test asserts `resolve()` returns the first-registered manifest when two share an identical exact route.
    - Longest-prefix precedence (`/tools/sender` over `/tools`) is asserted (existing or added).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Shell-level normalization / reject / tier-parity / duplicate / WR-01 tests (tests/shell.test.ts)</name>
  <files>tests/shell.test.ts</files>
  <read_first>
    - tests/shell.test.ts (the file being extended — the manifest-loading tests at 262-321 including `skips dapps with failed manifest fetch` at 310-321, the `rejects manifests missing required fields and emits dx:error` at 352-367, the `emits dx:error (source shell:mount) when #dx-mount is absent` at 323 for the dx:error listen/assert pattern, `accepts inline manifests` at 135, and the fetch-mock pattern at 282-288)
    - src/shell.ts (as modified by Task 1 — the new sources `shell:route` / `shell:manifest` and the tier/normalization/duplicate/WR-01 behavior being asserted)
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-RESEARCH.md ("Phase Requirements → Test Map", "Wave 0 Gaps" — the WR-01 anchor-test assertion change)
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-PATTERNS.md (tests/shell.test.ts extended section)
  </read_first>
  <action>
    Extend tests/shell.test.ts (same describe area as existing manifest-loading tests) with the integration-level TEST-02 cases (these need the shell's dx:error, so they live here, not in router.test.ts). Reuse the existing dx:error listen/assert pattern (window `dx:error` listener capturing `{ source, error }`) and the fetch-mock pattern.

    1. Route normalization (D-06): an inline manifest with `route: 'blog'` (no leading slash) mounts/resolves at `/blog` — construct the shell, init, `navigate('/blog')`, assert it mounts the dapp (the previously-dead route now works).
    2. Reject-unfixable (D-06): an inline manifest with `route: ''` (or whitespace-only) is discarded and a `dx:error` with `source === 'shell:route'` is emitted naming the manifest id; assert `shell.getManifests()` does not include it.
    3. Tier parity (D-07): an INLINE-tier invalid manifest (missing a required field) is discarded and emits `shell:manifest` dx:error; and a registry.json-tier invalid manifest (via mocked `fetch` returning an invalid manifest array) is likewise discarded and emits dx:error. This proves both previously-unvalidated tiers now validate.
    4. Duplicate-route (D-08): two inline manifests with the same exact route → a `dx:error` (`source shell:manifest`) is emitted whose message names BOTH conflicting ids; navigating the route mounts the first-registered manifest (assert winner via a `dx:dapp:mounted` listener registered before navigate).
    5. WR-01: UPDATE the existing `skips dapps with failed manifest fetch` test (310-321) — it currently asserts only the silent skip (`getManifests()` length 0). Change it to ALSO assert a `dx:error` was emitted (the behavior changes from silent to silent-list-but-loud-dx:error). Add sibling cases for the JSON-parse-failure mode (mock `fetch` to resolve `{ ok: true, json: async () => { throw ... } }`) and the network-throw mode (mock `fetch` to reject), each asserting a `shell:manifest` dx:error.

    Restore any mocked `window.fetch` to the original in each test (mirror the existing `originalFetch` save/restore pattern).
  </action>
  <verify>
    <automated>pnpm vitest run tests/shell.test.ts</automated>
  </verify>
  <done>
    tests/shell.test.ts covers normalization, reject-unfixable, both previously-unvalidated tiers, duplicate-route dx:error, and all three WR-01 failure modes; the pre-existing WR-01 anchor test's assertion is updated (not merely supplemented); full file green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/shell.test.ts` exits 0.
    - A test asserts a `shell:route` dx:error for an empty/whitespace route.
    - A test asserts an inline-tier invalid manifest AND a registry-tier invalid manifest are each discarded with a dx:error.
    - A test asserts a duplicate-route dx:error whose message contains both conflicting ids, and that the first-registered dapp is the one mounted.
    - The `skips dapps with failed manifest fetch` test now also asserts a `dx:error` was emitted; parse-failure and network-throw WR-01 cases exist.
    - Full suite green: `make test` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer/remote manifest host → shell | Structured manifest data (inline, dapp-entry remote fetch, or registry.json) crosses into the shell and drives routing + script-injection decisions (input validation boundary) |
| manifest route table → router resolution | Route strings become the routing table; collisions/malformed routes silently change which dapp serves a path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | loadManifests three tiers / isValidManifest | high | mitigate | `isValidManifest` applied uniformly to all three tiers (D-07) + reject-unfixable routes (D-06); Task 3 asserts inline + registry tiers now discard invalid manifests. Existing deepMerge prototype-pollution guard remains in force for overrides |
| T-04-05 | Spoofing | duplicate-route resolution | medium | mitigate | D-08 dx:error naming both conflicting ids turns a silent route-shadow into an observable event; Task 3 asserts the emit + the deterministic first-wins winner |
| T-04-06 | Repudiation | loadDappManifest silent catch (WR-01) | medium | mitigate | dx:error on fetch throw, non-2xx, and JSON-parse failure; Task 3 asserts all three modes emit instead of silently returning null |

No package-manager installs in this plan — package legitimacy gate N/A; no `T-04-SC` row.
</threat_model>

<verification>
- `pnpm vitest run tests/router.test.ts tests/shell.test.ts` green.
- `make test` green (lint + full suite) — no `test.fails`, no `.skip`.
- `git grep -n "shell:route" src/shell.ts` confirms the reject-unfixable source exists.
</verification>

<success_criteria>
ROADMAP success criterion 2 met: manifest-validation tests cover invalid route formats, deep-merge override behavior (deepMerge assertions land in plan 03), and multi-match routes, each asserting the correct accept/reject/merge/emit outcome. TEST-02 satisfied: normalization, tier parity, duplicate-route visibility, and WR-01 fetch/parse visibility are all implemented in `src/shell.ts` and locked by tests. Suite ships green.
</success_criteria>

<output>
Create `.planning/phases/04-testing-stress-edge-case-regression-coverage/04-02-SUMMARY.md` when done.
</output>
