---
phase: 05-documentation-truth-pass
plan: 08
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04", "05-05", "05-06", "05-07"]
files_modified:
  - README.md
  - examples/getting-started/main.js
  - examples/getting-started/index.html
  - docs/events-reference.md
  - docs/api-reference.md
  - docs/configuration.md
  - docs/getting-started.md
  - docs/dapp-development.md
  - docs/system-internals.md
  - docs/plugin-development.md
  - docs/cookbook.md
  - docs/security.md
  - .planning/phases/05-documentation-truth-pass/05-DRIFT-LOG.md
  - .planning/phases/05-documentation-truth-pass/drift/08-closeout.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "Every TypeScript snippet across all docs type-checks against the real 0.2.0 types via a throwaway tsc harness (D-04) — stale config shapes (flat loaders, hasPlugin override) are caught mechanically, not by eye."
    - "The README doc table indexes all docs including the previously-unindexed configuration.md, development.md, testing.md and the new docs/security.md; the audit link and install/build claims are verified; the version cell is left unchanged and status stays alpha (D-06)."
    - "No contradictions remain across independently-edited docs (same event/config/behavior described identically everywhere) after a cross-doc consistency sweep."
    - "examples/getting-started constructs a valid 0.2.0 shell (nested lifecycle, no flat loaders)."
    - "05-DRIFT-LOG.md exists, assembling all per-doc fragments — the phase's auditable proof that all 14 docs + README + example were checked."
  artifacts:
    - "README.md — reconciled doc table (4 added rows), verified links/claims, version cell untouched"
    - "05-DRIFT-LOG.md — assembled canonical drift log (D-01)"
    - "any docs/*.md — snippet/cross-doc fixes surfaced by the harness and consistency sweep"
    - "drift/08-closeout.md — records compile-check + cross-doc + README/example findings"
  key_links:
    - "tsconfig.scratch.json paths ↔ vitest.config.ts alias map (@dnzn/dxkit* → src/ and plugins/*/src/)"
    - "README doc table ↔ docs/ directory (index reconciliation, D-12) + docs/security.md row (D-08)"
    - "05-DRIFT-LOG.md ↔ drift/*.md fragments from Plans 02–07 + this plan"
---

<objective>
Close out the truth pass: mechanically compile-check every doc TS snippet against real 0.2.0 types
(D-04), reconcile the README doc index (D-12) + verify links/claims (D-06), spot-check the example,
run the cross-doc consistency sweep (catch contradictions between independently-edited docs), and
assemble the canonical drift log (D-01) — the phase's proof.

Purpose: DOC-01 mechanical snippet verification + DOC-02 README index reconciliation + the drift-log proof.
Output: harness-verified docs, reconciled README, cross-doc-consistent docs, 05-DRIFT-LOG.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-documentation-truth-pass/05-RESEARCH.md
@.planning/phases/05-documentation-truth-pass/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-04 compile-check harness across all doc TypeScript snippets</name>
  <files>docs/*.md (snippet fixes as surfaced), tsconfig.scratch.json (throwaway, not committed), .planning/phases/05-documentation-truth-pass/drift/08-closeout.md</files>
  <read_first>
    - vitest.config.ts — the alias map (@dnzn/dxkit → src/index.ts; each plugin → plugins/*/src/index.ts) to mirror into the scratch tsconfig paths.
    - tsconfig.json — compilerOptions to base the throwaway config on (ES2022, strict, moduleResolution bundler, DOM lib).
    - 05-RESEARCH.md §"Open Questions #3" (hand-mirrored scratch file recommendation), §"Common Pitfalls → Pitfall 2" (hasPlugin Omit — a snippet passing hasPlugin through createShell must compile-FAIL), §"Validation Architecture → Wave 0 Gaps".
    - All docs with ts/js code fences: events-reference.md, api-reference.md, configuration.md, getting-started.md, dapp-development.md, system-internals.md, plugin-development.md, cookbook.md, security.md.
  </read_first>
  <action>
    Stand up a throwaway compile-check harness (NOT committed as CI — D-04). Create tsconfig.scratch.json
    (gitignored, or in a scratch/tmp location) extending tsconfig.json's compilerOptions with noEmit true and
    a paths map mirroring vitest.config.ts's alias map so imports of @dnzn/dxkit* resolve to the real source.
    Hand-mirror each doc's TypeScript snippet into one scratch .ts file per doc (or a per-doc scratch dir),
    then run tsc --noEmit --strict against tsconfig.scratch.json. Any type error reveals stale doc content
    (e.g. flat loaders, hasPlugin passed through createShell, removed fields, wrong option names) — fix the
    SOURCE DOC (not the snippet-in-isolation), re-run until clean. HTML/IIFE snippets get eyeball verification
    against the example. Record every snippet drift found + fixed in drift/08-closeout.md. Remove the scratch
    harness artifacts after the pass (or leave gitignored). Commit doc fixes with a docs: commit noting the
    compile-check pass; do NOT commit tsconfig.scratch.json or scratch mirrors.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - Every doc TS snippet type-checks against real 0.2.0 types (harness runs clean, or documented intentional-error snippets are clearly marked as such).
    - Any snippet passing hasPlugin through createShell is corrected (it must not appear as valid current API).
    - No flat scriptLoader/styleLoader/templateLoader snippet remains in any doc.
    - Scratch harness (tsconfig.scratch.json, mirror files) is not committed.
    - drift/08-closeout.md records each snippet drift found and fixed.
  </acceptance_criteria>
  <done>All doc TS snippets are mechanically verified against 0.2.0 types; harness discarded.</done>
</task>

<task type="auto">
  <name>Task 2: README doc-index reconciliation + example spot-check + cross-doc consistency sweep</name>
  <files>README.md, examples/getting-started/main.js, examples/getting-started/index.html, docs/*.md (cross-doc fixes as surfaced), .planning/phases/05-documentation-truth-pass/drift/08-closeout.md</files>
  <read_first>
    - README.md — full file (status table, Documentation doc tables, installation/build claims, audit link).
    - 05-RESEARCH.md §"Recommended Sweep Order" (README fixed after indexed docs settle), §"State of the Art" (deltas README must not contradict), §"Common Pitfalls → Pitfall 3" (single disable rule stated identically everywhere).
    - CONTEXT.md D-06 (verify table, DO NOT bump version cell, status stays alpha), D-08 (security.md gets a README row), D-12 (index configuration/development/testing).
    - examples/getting-started/main.js + index.html — confirm nested lifecycle / no flat loaders (RESEARCH confirms already correct — spot-check against final ShellConfig shape).
    - All corrected docs from Plans 02–07 (for the cross-doc contradiction check).
  </read_first>
  <action>
    Reconcile the README doc index (D-12): add rows for docs/configuration.md, docs/development.md,
    docs/testing.md, and docs/security.md (D-08) in the same terse register as existing rows (no
    throat-clearing descriptions). Verify the audit link, installation commands, and build-system claims
    against the repo; fix drift. Leave the version cell at its current value and keep status alpha (D-06 —
    release tooling owns the bump). Spot-check examples/getting-started (main.js/index.html) against the final
    0.2.0 ShellConfig/manifest shape — it should already use nested lifecycle + IIFE correctly (RESEARCH);
    fix only if drift is found. Run the cross-doc consistency sweep: confirm the same event names/sources,
    config defaults, and the single disable-while-active rule are described identically across all docs (no
    contradictions introduced by independent edits) — fix any divergence at its source doc. Apply the D-13
    slop bar + README voice to README edits. Record findings in drift/08-closeout.md. Commit docs(readme):
    for the README/index work; per-doc docs: commits for any cross-doc fixes.
  </action>
  <verify>
    <automated>bash -c "grep -q 'security.md' README.md && grep -q 'configuration.md' README.md && grep -q 'development.md' README.md && grep -q 'testing.md' README.md"</automated>
  </verify>
  <acceptance_criteria>
    - README doc table indexes configuration.md, development.md, testing.md, and security.md.
    - Audit link + install/build claims verified; version cell unchanged; status still alpha.
    - examples/getting-started constructs a 0.2.0 shell (nested lifecycle, no flat loaders).
    - No cross-doc contradictions remain (event names/sources, config defaults, disable rule consistent everywhere).
    - drift/08-closeout.md records README + example + cross-doc findings.
  </acceptance_criteria>
  <done>README index reconciled, example verified, docs mutually consistent.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble the canonical drift log (D-01)</name>
  <files>.planning/phases/05-documentation-truth-pass/05-DRIFT-LOG.md</files>
  <read_first>
    - .planning/phases/05-documentation-truth-pass/drift/*.md — all per-plan fragments (02-reference, 03-config-getting-started, 04-behavior-docs, 05-plugins, 06-low-fanout, 07-security, 08-closeout).
    - CONTEXT.md D-01 (drift log is the phase's proof; format is Claude's discretion), D-02 (completion = all 14 docs + README + example checked off).
  </read_first>
  <action>
    Assemble .planning/phases/05-documentation-truth-pass/05-DRIFT-LOG.md from the drift/*.md fragments into
    one auditable record: a per-doc entry (what was wrong, what changed) covering all 14 docs + README +
    example + the net-new security.md, plus a completion checklist confirming every unit was checked. This
    is DOC-01's verifiable proof. Keep the terse register. Commit docs: with the assembled drift log.
  </action>
  <verify>
    <automated>bash -c "test -f .planning/phases/05-documentation-truth-pass/05-DRIFT-LOG.md && for d in events-reference api-reference configuration getting-started dapp-development system-internals plugin-development cookbook development testing security; do grep -q \"$d\" .planning/phases/05-documentation-truth-pass/05-DRIFT-LOG.md || { echo \"MISSING $d\"; exit 1; }; done && grep -q 'README' .planning/phases/05-documentation-truth-pass/05-DRIFT-LOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - 05-DRIFT-LOG.md exists with a per-doc entry for all 10 framework docs, all 4 plugin docs, README, and the example.
    - A completion checklist confirms every doc unit was checked (D-02).
    - The log is traceable to the drift/*.md fragments.
  </acceptance_criteria>
  <done>The canonical drift log is assembled — the phase's proof that the truth pass is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → consumer | Final index + snippets are what consumers land on; a broken snippet or missing index row misleads at the entry point |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08a | Tampering | doc TS snippets | medium | mitigate | Stale snippets (flat loaders, hasPlugin override) would ship broken/insecure config; the D-04 tsc harness catches type-shape drift mechanically before sign-off. |
| T-05-08b | Repudiation | cross-doc contradictions | low | mitigate | Divergent descriptions of the same security-relevant behavior (e.g. disable rule, dx:error sources) undermine trust; consistency sweep reconciles them and the drift log records the reconciliation. |

No package installs — tsc is an existing devDependency; no `T-05-SC` checkpoint required.
</threat_model>

<verification>
- `make test` green (harness fixes must not break the suite).
- Verify greps pass; 05-DRIFT-LOG.md assembled with all doc units represented.
</verification>

<success_criteria>
All doc snippets compile against 0.2.0 types, README indexes every doc, docs are mutually consistent, and the drift log proves the pass.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-truth-pass/05-08-SUMMARY.md` when done.
</output>
