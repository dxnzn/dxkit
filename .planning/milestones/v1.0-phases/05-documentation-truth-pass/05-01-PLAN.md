---
phase: 05-documentation-truth-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shell.ts
  - src/lifecycle.ts
  - tests/shell.test.ts
  - tests/lifecycle.test.ts
  - tests/router.test.ts
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "An explicitly-configured registryUrl whose fetch/parse fails emits a dx:error (source shell:manifest); the default /registry.json probe stays silent."
    - "Disabling a dapp whose route is currently active while its mount is still in flight navigates the browser to / — same final state as disabling a committed mount."
    - "inFlightMountId is cleared only by the mount() call that set it; a superseded (stale) call returning through a bare isStale() gate cannot null out a newer call's marker."
    - "make test is green: full vitest suite plus the three new regression tests pass, biome clean."
  artifacts:
    - "src/shell.ts — registry-failure dx:error emit gated on Object.hasOwn(config,'registryUrl'); disableDapp() navigate-to-/ for in-flight disable"
    - "src/lifecycle.ts — inFlightMountId generation-ownership guard"
    - "tests/shell.test.ts — D-15 registry cases (explicit→emit ×3, default→silent), D-16 disable-mid-flight case, dx:error listener cleanup"
    - "tests/lifecycle.test.ts — D-17 inFlightMountId ownership regression"
    - "tests/router.test.ts — duplicate-route test id rename (order-neutral)"
  key_links:
    - "loadManifests() registry catch ↔ Object.hasOwn(config,'registryUrl') presence check (must be captured before the destructure default erases it)"
    - "disableDapp() ↔ router.resolve(router.getCurrentPath()) route-ownership check ↔ router.navigate('/')"
    - "mount() bare isStale() gates ↔ generation stored alongside inFlightMountId"
---

<objective>
Land the three folded code fixes (D-15, D-16, D-17) with their regression tests BEFORE the docs
that describe these paths are verified. This is the fixes-first wave — per CONTEXT.md's explicit
sequencing note (D-16 changes user-visible routing behavior that dapp-development.md /
system-internals.md describe), the code must reach its final 0.2.0 shape here so the doc sweep
describes post-fix behavior.

Purpose: docs describe actual 0.2.0 behavior (D-03 "code is truth"); these three fixes close the
last behavior gaps so no doc has to describe a divergence or a swallowed failure.
Output: corrected src/shell.ts + src/lifecycle.ts, three regression tests, two test-nit cleanups.
</objective>

## Artifacts this phase produces

Full phase-wide list (this plan produces the code artifacts; later plans produce the docs):
- `dx:error` emit at the registry-fetch site in `loadManifests()` (source `shell:manifest`), gated on explicit `registryUrl` (D-15).
- `disableDapp()` navigate-to-`/` behavior for an in-flight (uncommitted) mount whose route is active (D-16).
- `inFlightMountId` generation-tracking ownership guard in `src/lifecycle.ts` (D-17).
- `docs/security.md` (new file — Plan 07).
- 0.1.5 → 0.2.0 migration section inside `docs/getting-started.md` (Plan 03).
- New tests: D-15 registry cases + D-16 disable-mid-flight case in `tests/shell.test.ts`; D-17 `inFlightMountId` ownership regression in `tests/lifecycle.test.ts`.
- Drift-log fragments per doc-sweep plan under `.planning/phases/05-documentation-truth-pass/drift/`, assembled into `05-DRIFT-LOG.md` (Plan 08).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-truth-pass/05-RESEARCH.md
@.planning/phases/05-documentation-truth-pass/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-15 — surface registry.json fetch/parse failures when registryUrl is explicit</name>
  <files>src/shell.ts, tests/shell.test.ts</files>
  <read_first>
    - src/shell.ts — loadManifests() registry catch (the `// No registry.json — that's fine` block, currently ~:237-244); loadDappManifest() emit sites (~:184-224) as the mirror template; the D-05 flat-loader Object.hasOwn presence guard (~:20-29) as the presence-check idiom; the registryUrl destructure default (~:35).
    - 05-RESEARCH.md §"Folded-Fix Landing Sites → D-15" and §"Code Truth: Behavior → Three-tier manifest loading".
    - 05-PATTERNS.md §"src/shell.ts — D-15 registry.json failure emit" (non-OK and throw/parse mirror excerpts).
    - tests/shell.test.ts — existing WR-01 dapps-tier tests (~:310-372) for the fetch-mock idiom.
  </read_first>
  <behavior>
    - Explicit `registryUrl` + non-OK HTTP response → one dx:error, source `shell:manifest`, message names the registry URL and status.
    - Explicit `registryUrl` + fetch throw → one dx:error, source `shell:manifest`, message includes original error, `cause` preserved.
    - Explicit `registryUrl` + res.json() parse failure → one dx:error, source `shell:manifest`.
    - Default `registryUrl` (omitted from config) + any of the three failure modes → NO dx:error; getManifests() returns [].
  </behavior>
  <action>
    In loadManifests(), replace the silent registry catch so all three failure modes (non-OK response,
    fetch throw, res.json() parse failure) emit dx:error with source 'shell:manifest', but ONLY when the
    caller explicitly passed registryUrl. Capture presence via Object.hasOwn(config, 'registryUrl') BEFORE
    the `registryUrl = '/registry.json'` destructure default erases it (mirror the existing Object.hasOwn
    flat-loader guard) — or destructure without a default and apply `?? '/registry.json'` separately while
    keeping the raw presence flag. Mirror loadDappManifest()'s two-message split (status-info message for
    non-OK; unified "request failed or response was not valid JSON" message with `{ cause: err }` for the
    throw/parse catch) per RESEARCH Open Question 1 (Claude's Discretion). The default probe path stays a
    silent return [] — absence of /registry.json is an expected state for dapps/manifests-only consumers.
    Emit-every-time, no dedupe state. Add the four regression tests to tests/shell.test.ts extending the
    WR-01 fetch-mock idiom; each new test adds and then removes its own dx:error window listener. Commit
    as a single `fix(shell):` commit citing D-15.
  </action>
  <verify>
    <automated>npx vitest run tests/shell.test.ts -t "registry"</automated>
  </verify>
  <acceptance_criteria>
    - Explicit-registryUrl non-OK / throw / parse tests each assert `errors.some(e => e.source === 'shell:manifest')` is true.
    - Default-registryUrl (omitted) failure test asserts NO dx:error fired AND getManifests() returns [].
    - Emitted error for the throw/parse case has a defined `.cause`.
    - `make test` exits 0.
  </acceptance_criteria>
  <done>Registry failures are visible when registryUrl is explicit and silent on the default probe; four tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-16 — disable-mid-flight navigates to /</name>
  <files>src/shell.ts, tests/shell.test.ts</files>
  <read_first>
    - src/shell.ts — disableDapp() (~:129-146), rebuildRouter() navigate-to-/ branch (~:97-117, the mirror), releasePendingMount()/invalidatePendingMount wiring, and the init() route-ownership check via router.resolve(router.getCurrentPath()) (~:359).
    - 05-RESEARCH.md §"disableDapp() — current divergence that D-16 closes" and §"Folded-Fix Landing Sites → D-16"; §"Common Pitfalls → Pitfall 3".
    - 05-PATTERNS.md §"src/shell.ts — D-16 disable-mid-flight navigates to /".
    - tests/stress.test.ts — slow-loader / in-flight-mount pattern; tests/shell.test.ts — committed-mount disable test for the assertion shape.
  </read_first>
  <behavior>
    - Disable a dapp whose manifest matches the current route while its mount is still in flight (uncommitted) → browser ends navigated to `/`.
    - This matches the already-correct committed-mount disable path (both end at `/`).
    - Disabling a dapp whose route is NOT the current route does not force a navigate.
  </behavior>
  <action>
    In disableDapp(), after the existing invalidatePendingMount(id)/releasePendingMount() calls and
    rebuildRouter(), add the missing in-flight case: when lifecycle.getCurrentDapp() is null (nothing
    committed) but router.resolve(router.getCurrentPath()) resolves to the just-disabled dapp id, call
    router.navigate('/') — mirroring rebuildRouter()'s existing committed-mount branch. Use the existing
    router.resolve(getCurrentPath()) ownership check (the same pattern init() uses); do NOT add new router
    API. Converge the OUTCOME (both disable paths end at /) without collapsing the two code paths — per
    Pitfall 3, they remain distinct branches. Add one integration test to tests/shell.test.ts using the
    stress-suite slow-loader technique to hold a mount in flight, call disableDapp() mid-flight, and assert
    the router's current path is '/'. Commit as `fix(shell):` citing D-16, with a BREAKING CHANGE footer
    only if the observable navigation contract is judged breaking (it is a behavior fix converging to the
    documented committed-mount outcome — treat as additive/fix unless the executor determines otherwise).
  </action>
  <verify>
    <automated>npx vitest run tests/shell.test.ts -t "disable"</automated>
  </verify>
  <acceptance_criteria>
    - New test drives an in-flight mount for a route-matching dapp, calls disableDapp(), asserts current path === '/'.
    - Existing committed-mount disable test still passes (no regression).
    - `make test` exits 0.
  </acceptance_criteria>
  <done>Both disable-while-active paths end at `/`; docs can then state one rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-17 — inFlightMountId ownership guard + test nits</name>
  <files>src/lifecycle.ts, tests/lifecycle.test.ts, tests/shell.test.ts, tests/router.test.ts</files>
  <read_first>
    - src/lifecycle.ts — mount()'s four bare `if (isStale()) return false;` gates (~:352, :372, :396, :415) and the catch blocks that DO clear inFlightMountId inside `if (!isStale())` (~:341-349, :362-371, :383-395, :404-414); invalidateAnyPendingMount()/invalidatePendingMount() (~:454-469); mountGeneration/inFlightMountId decls (~:273-274, :296-298).
    - src/shell.ts — pendingMountId/pendingMountToken call-scoped ownership pattern (~:58-61, :391-394, :443-450) as the idiom to mirror.
    - 05-RESEARCH.md §"Folded-Fix Landing Sites → D-17" (all three sites); 05-PATTERNS.md §"src/lifecycle.ts — D-17 inFlightMountId hygiene".
    - tests/shell.test.ts — cleaned-up listener pattern (~:41-69) vs uncleaned dx:error sites; tests/router.test.ts duplicate-route tests (~:148-172).
  </read_first>
  <behavior>
    - After two overlapping mount() calls where the first is superseded and returns via a bare isStale() gate, inFlightMountId reflects only the second (current) call's state — never a stale leftover id.
    - invalidateAnyPendingMount() bumps mountGeneration only when a mount is genuinely in flight (no spurious bump from a stale leftover).
  </behavior>
  <action>
    Track the generation that set inFlightMountId (store it alongside, mirroring shell's
    pendingMountId/pendingMountToken pairing). At each bare isStale() gate return, null inFlightMountId only
    if the returning call still owns it (its captured generation matches the stored owner generation) — a
    superseded call must not clear a newer call's marker, and must not leave its own stale id behind either.
    Keep the catch-block clearing behavior consistent with the new ownership rule. Add a unit test to
    tests/lifecycle.test.ts locking the invariant (two overlapping mounts, first invalidated via a bare-gate
    path, assert inFlightMountId reflects only the current call). Then the two test nits: (a) in
    tests/shell.test.ts add matching window.removeEventListener('dx:error', ...) to every uncleaned site
    (grep addEventListener('dx:error' for the list) — or factor a shared afterEach helper; (b) in
    tests/router.test.ts rename the reversed-input duplicate-route test's manifest ids to order-neutral
    names (alpha/beta) or add a one-line clarifying comment, WITHOUT changing the test's logic or assertion.
    Commit the hygiene fix + lifecycle test as one commit citing D-17; the two test-file nits may share a
    `test:` commit.
  </action>
  <verify>
    <automated>npx vitest run tests/lifecycle.test.ts tests/router.test.ts tests/shell.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New lifecycle test asserts inFlightMountId ownership invariant after overlapping bare-gate supersession.
    - No `addEventListener('dx:error'` in tests/shell.test.ts lacks a matching removeEventListener (or an afterEach cleanup covers them).
    - Router duplicate-route reversed-input test passes with order-neutral ids or a clarifying comment; assertion unchanged.
    - `make test` exits 0.
  </acceptance_criteria>
  <done>inFlightMountId is owner-cleared only; test nits resolved; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer config → shell | `registryUrl` and dapp/manifest sources are consumer-supplied; shell fetches them |
| browser navigation → shell | route changes and disable calls drive mount/unmount control flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | D-15 registry dx:error message | low | accept | Message includes only the consumer-supplied `registryUrl` and the underlying fetch/parse error; no secrets are introduced by DxKit. `cause` is passed for consumer inspection, not logged by the framework. |
| T-05-02 | Denial of Service | D-16 disableDapp navigate('/') | low | accept | Single bounded navigate mirroring the existing rebuildRouter branch; no loop, no re-entrancy introduced. |
| T-05-03 | Tampering | D-17 inFlightMountId ownership | medium | mitigate | Generation-ownership prevents a superseded/stale mount call from clearing a newer call's in-flight marker, preserving mount-dedupe state integrity; regression test locks the invariant. |
| T-05-04 | Elevation of Privilege | mount/route control flow | low | accept | No trust boundary changes; fixes are event-emission + navigation-outcome corrections, not new injection or storage paths (per RESEARCH Security Domain). |

No package installs in this phase — no `T-05-SC` package-legitimacy checkpoint required.
</threat_model>

<verification>
- `make test` green (lint + full vitest suite including the three new regression tests).
- Manual: confirm the default-registryUrl silence contract is intact (no dx:error on the probe path).
</verification>

<success_criteria>
D-15, D-16, D-17 land with passing regression tests; no other shipped code is reopened; suite green.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-truth-pass/05-01-SUMMARY.md` when done.
</output>
