---
phase: 04-testing-stress-edge-case-regression-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/settings/tests/integration.test.ts
  - tests/utils.test.ts
  - src/utils.ts
autonomous: true
requirements: [TEST-02, TEST-03]
must_haves:
  truths:
    - "A settings handler registered by a dapp does not fire after that dapp is disabled via the real shell.disableDapp() → dx:dapp:disabled wiring (D-10, TEST-03)"
    - "The disabled dapp's settings handler is pruned (no leak): a subsequent api.set() for its key does not invoke the handler"
    - "deepMerge's documented semantics are locked by tests: recursive nested merge, wholesale array replace, undefined-skip, and __proto__/constructor/prototype pollution guard (D-09)"
    - "The deepMerge JSDoc null-handling claim matches actual code behavior (reconciled — see task 2)"
  artifacts:
    - "plugins/settings/tests/integration.test.ts — full-shell settings-cleanup regression (real createShell + real createSettings + real disableDapp)"
    - "tests/utils.test.ts — deepMerge documented-semantics assertions (gap-fill only)"
    - "src/utils.ts — JSDoc null-handling reconcile (comment-only)"
  key_links:
    - "createShell({ plugins: { settings } }) → shell.disableDapp(id) → dx:dapp:disabled → settings cleanup(dappId)"
---

<objective>
Prove settings-handler cleanup end-to-end through the real shell wiring (TEST-03) and lock deepMerge's documented override semantics (D-09).

Purpose: Phase 2 (ROB-04) shipped settings cleanup with plugin-level tests using a mocked context. TEST-03/D-10 requires the integration layer: drive the REAL `createShell` → mount dapp → register settings handlers via the actual plugin → `disableDapp()` → assert handlers neither fire nor leak, through the real `dx:dapp:disabled` emit (`plugins/settings/src/index.ts:242` subscribes cleanup to it). Separately, D-09 locks the deepMerge contract used by manifest overrides.

Output: A new `plugins/settings/tests/integration.test.ts` regression, deepMerge assertions in `tests/utils.test.ts`, and a one-line JSDoc reconcile in `src/utils.ts`. No production runtime behavior changes. This plan touches no source module that plans 01/02 modify, so it runs in parallel (wave 1).
</objective>

<artifacts_produced>
Symbols this plan creates (for source-grounding / drift exclusion):

- `plugins/settings/tests/integration.test.ts` — new file; full-shell settings-cleanup regression
- `tests/utils.test.ts` — new deepMerge assertions (gap-fill only; most cases already exist)
- `src/utils.ts` — JSDoc comment reconcile only (no runtime change)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-testing-stress-edge-case-regression-coverage/04-CONTEXT.md
@.planning/phases/04-testing-stress-edge-case-regression-coverage/04-RESEARCH.md
@.planning/phases/04-testing-stress-edge-case-regression-coverage/04-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-shell settings-cleanup regression (plugins/settings/tests/integration.test.ts)</name>
  <files>plugins/settings/tests/integration.test.ts</files>
  <read_first>
    - plugins/settings/src/index.ts (the cleanup wiring being driven end-to-end — `getSettingsAPI()` at 256, `onChange(dappId, key, handler)` at 212, `cleanup(dappId)` at 142, and the `dx:dapp:disabled` subscription at 242 `disabledListener = context.events.on('dx:dapp:disabled', ({ id }) => cleanup(id))`)
    - plugins/settings/tests/settings.test.ts (existing mocked-context tests — read the `createSettings` import, `uniqueStorageKey()` fixture, and the `onChange()` usage at 135-165 to mirror API calls; this new file is the real-shell contrast to those mocked tests)
    - src/shell.ts (`createShell`, `disableDapp()` at 126-134, the nested `lifecycle` config group at 38/44, and `initEnabledState()` at 68-91 which reads persisted _shell enable state — the dapp under test must be `optional: true` to be disableable)
    - tests/shell.test.ts (real-shell DOM setup — beforeEach `#dx-mount` container + `window.history.replaceState`, afterEach `shell.destroy()` + `delete window.__DXKIT__`, and `testLoaders` for the nested lifecycle loaders)
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-RESEARCH.md ("TEST-03 Integration Design" + the integration skeleton in Code Examples)
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-PATTERNS.md (TEST-03 integration regression section)
  </read_first>
  <action>
    Create plugins/settings/tests/integration.test.ts driving the REAL wiring (no mocked context — that is what distinguishes this from the existing plugins/settings/tests/settings.test.ts). Mirror the tests/shell.test.ts DOM setup (create `#dx-mount`, `window.history.replaceState(null,'','/')` in beforeEach; `shell.destroy()`, remove container, `delete window.__DXKIT__` in afterEach). Use a fresh `createSettings({ storageKey: uniqueStorageKey() })`-style unique key so persisted state does not leak between tests (mirror the settings.test.ts convention).

    Test the D-10 contract:
    1. Construct the shell with the nested `lifecycle` test loaders (`lifecycle: { scriptLoader: async () => {}, styleLoader: async () => {} }`), `plugins: { settings: settingsPlugin }`, and an `optional: true` manifest (e.g. id `hello`, route `/hello`, entry a `data:text/javascript,` URL). `await shell.init()`.
    2. Get `const api = settingsPlugin.getSettingsAPI();`, register `const handler = vi.fn(); api.onChange('hello', 'someKey', handler);`.
    3. Call the REAL `shell.disableDapp('hello')` — this drives the actual `dx:dapp:disabled` emit through the shell's real path into the settings plugin's `cleanup('hello')` subscription.
    4. Call `api.set('hello', 'someKey', 'newValue')` (the internal store write still succeeds — cleanup prunes only handlers) and assert `expect(handler).not.toHaveBeenCalled()`.
    5. Add a no-leak assertion: confirm a `_shell`-scoped handler or a different dapp's handler is unaffected by the disable (cleanup prunes by exact dappId per plugins/settings/src/index.ts:142) — e.g. register a handler for a second still-enabled dapp key and assert it STILL fires after the first dapp is disabled.

    Use the path-alias imports (`@dnzn/dxkit` for `createShell`, `@dnzn/dxkit-settings` for `createSettings`) already configured in vitest.config.ts.
  </action>
  <verify>
    <automated>pnpm vitest run plugins/settings/tests/integration.test.ts</automated>
  </verify>
  <done>
    plugins/settings/tests/integration.test.ts drives createShell + real createSettings + real disableDapp and asserts the disabled dapp's handler does not fire post-disable while an unrelated handler still fires; green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run plugins/settings/tests/integration.test.ts` exits 0.
    - The test calls `shell.disableDapp(...)` (the real shell method), NOT a mocked-context `disableDapp` (grep confirms no `mockContext` usage in this file).
    - It asserts `handler` is not called after disable following an `api.set(...)` for the disabled dapp's key.
    - It asserts an unrelated/still-enabled handler is NOT pruned (no over-cleanup).
    - Full suite green: `make test` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: deepMerge documented-semantics assertions + JSDoc reconcile (tests/utils.test.ts, src/utils.ts)</name>
  <files>tests/utils.test.ts, src/utils.ts</files>
  <read_first>
    - src/utils.ts (the file being reconciled — the whole 22-line `deepMerge`: JSDoc at line 1 claims "Null/undefined values are skipped", but line 17 `else if (val !== undefined)` only skips `undefined` — `null` REPLACES; the pollution guard is line 5)
    - tests/utils.test.ts (the file being extended — existing coverage: flat/nested/deeply-nested merge, array-replace at 20, undefined-skip at 30, `replaces with null` at 35-37, object↔primitive, new-keys, __proto__/constructor/prototype guards at 55-72 including nested at 75, no-mutation at 82)
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-RESEARCH.md ("Don't Hand-Roll" deepMerge row — D-09 is largely already satisfied; verify gaps, do not duplicate)
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-CONTEXT.md (D-09 wording + D-05 fix-correctness-in-phase + the "docs match code" charter)
  </read_first>
  <action>
    Lock the D-09 deepMerge contract and reconcile the one doc/code discrepancy the review surfaced.

    1. Verify gaps, do not duplicate: tests/utils.test.ts ALREADY covers recursive nested merge, wholesale array-replace, undefined-skip, null-replace, and all three pollution-guard keys (including nested). Add ONLY genuinely missing D-09 assertions if any (e.g. an array in an override replacing a nested array wholesale, or a `constructor`/`prototype` key nested one level deeper than the existing `__proto__` nested case if not covered). Do not re-add cases that already exist.

    2. Reconcile the null-handling discrepancy toward CODE TRUTH (per the "code is truth" / "docs match code" charter). The JSDoc at src/utils.ts:1 claims "Null/undefined values are skipped," but the implementation (line 17) only skips `undefined`; `null` REPLACES, and the existing deliberate test `replaces with null` (tests/utils.test.ts:35-37) locks null-replaces. Do NOT change the runtime merge behavior — manifest overrides (`loadDappManifest` deepMerge at shell.ts:187) depend on it and an existing test deliberately pins it. Instead correct the JSDoc comment at src/utils.ts:1 to accurately state that only `undefined` values are skipped and `null` replaces (arrays still replace, no concatenation). Keep the D-09 tests asserting the ACTUAL behavior (null replaces, undefined skips).

    3. Surface the CONTEXT/JSDoc-vs-code divergence for the Phase 5 docs pass: leave a brief note in the plan summary that CONTEXT D-09's "null/undefined values are skipped" wording diverges from actual code (null replaces), reconciled here toward code-truth by correcting the JSDoc — so the developer can decide in Phase 5 whether runtime null-skip is ever desired. Do NOT file a runtime change.

    This task changes only a comment in src/utils.ts (no runtime/behavior change) plus test assertions. Commit type `test(utils):` with the JSDoc fix noted as a `docs:`-flavored comment correction in the body.
  </action>
  <verify>
    <automated>pnpm vitest run tests/utils.test.ts</automated>
  </verify>
  <done>
    tests/utils.test.ts locks all four D-09 semantics (recursive merge, array-replace, undefined-skip, pollution guard) with no duplicated cases; src/utils.ts JSDoc accurately describes null-replaces/undefined-skip; green.
  </done>
  <acceptance_criteria>
    - `pnpm vitest run tests/utils.test.ts` exits 0.
    - The src/utils.ts line-1 JSDoc no longer claims null is skipped (it states null replaces, undefined is skipped).
    - `git diff src/utils.ts` shows a comment-only change (no change to the function body / control flow).
    - No new `deepMerge` test duplicates a pre-existing case.
    - Full suite green: `make test` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → shell.disableDapp | Disabling a dapp is the access-control decision that its registered settings handlers must stop firing (V4) |
| manifest override input → deepMerge | Override objects (potentially from a remote manifest host) are merged into base manifests; malicious keys must not pollute prototypes (V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Elevation of Privilege | settings cleanup on disableDapp | medium | mitigate | The ROB-04 cleanup shipped in Phase 2; this plan adds the full-shell integration regression (Task 1) proving a disabled dapp's handlers do not fire, closing the coverage gap that would let the leak regress silently |
| T-04-08 | Tampering | deepMerge prototype-pollution guard | high | mitigate | D-09 tests (Task 2) lock the existing `__proto__`/`constructor`/`prototype` rejection (src/utils.ts:5) including nested, so an override cannot pollute Object.prototype via manifest merges |

No package-manager installs in this plan — package legitimacy gate N/A; no `T-04-SC` row.
</threat_model>

<verification>
- `pnpm vitest run plugins/settings/tests/integration.test.ts tests/utils.test.ts` green.
- `make test` green (lint + full suite) — no `test.fails`, no `.skip`.
</verification>

<success_criteria>
ROADMAP success criterion 3 met: a regression test verifies settings handlers registered by a dapp do not fire after that dapp is disabled via `disableDapp()`, through real shell wiring (TEST-03/D-10). The deep-merge override behavior portion of criterion 2 (D-09) is locked with the deepMerge assertions, and the JSDoc is reconciled to code truth. Suite ships green.
</success_criteria>

<output>
Create `.planning/phases/04-testing-stress-edge-case-regression-coverage/04-03-SUMMARY.md` when done.
</output>
