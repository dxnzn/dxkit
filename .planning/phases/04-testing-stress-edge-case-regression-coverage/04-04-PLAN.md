---
phase: 04-testing-stress-edge-case-regression-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shell.ts
  - tests/stress.test.ts
autonomous: true
gap_closure: true
requirements: [TEST-01]
must_haves:
  truths:
    - "Navigating to an unmatched (null-manifest) route while a dapp mount is in flight abandons that mount: no dx:mount, no dx:dapp:mounted, no container write, no dx:route:subpath — satisfying the D-01 last-navigation-wins invariant for the dapp->unmatched-route transition, not only dapp->dapp"
  artifacts:
    - "src/shell.ts handleRouteChange null-manifest branch invalidates the in-flight pending mount"
    - "tests/stress.test.ts regression scenario for dapp->unmatched-route in-flight supersession"
  key_links:
    - "handleRouteChange(null) -> lifecycle.invalidatePendingMount(pendingMountId)"
---

<objective>
Close verification gap CR-01 (04-VERIFICATION.md Truth #4): the D-01 "last-navigation-wins" invariant has a hole for the dapp->unmatched-route transition. `handleRouteChange`'s null-manifest branch (src/shell.ts:358-368) only calls `lifecycle.unmount()` and never bumps the mount-generation guard via `lifecycle.invalidatePendingMount(pendingMountId)`. An in-flight mount for the dapp the user navigated away FROM still commits: `dx:mount`/`dx:dapp:mounted` fire, the container is populated with the stale dapp's template, and a misattributed `dx:route:subpath` names the stale dapp under the new (unmatched) path.

Purpose: Restore the D-01 invariant Plan 01's mount-generation guard was built to enforce, across ALL navigation transitions — closing the one-line gap Plan 01 left in the null-manifest branch and locking it with a regression test.
Output: A one-line-shaped fix in src/shell.ts plus a new stress scenario in tests/stress.test.ts that fails before the fix and passes after.

Scope discipline: Close CR-01 ONLY. WR-01..WR-07 and IN-01..IN-04 (04-VERIFICATION.md Anti-Patterns table) were explicitly judged out-of-scope for this phase — do NOT touch them.
</objective>

<artifacts_produced>
Symbols this plan creates or modifies (source-grounding allowlist for review convergence):
- Modified function: `handleRouteChange` (src/shell.ts) — null-manifest branch now invalidates the in-flight pending mount
- New test scenario: "navigate to an unmatched route while a dapp mount is in flight abandons it" in tests/stress.test.ts (inside the existing `describe('stress: concurrency & mount races (TEST-01, D-01/D-02/D-03)')` block)
- Reused (not new): `lifecycle.invalidatePendingMount` (src/lifecycle.ts:440-447, already exported), shell-level `pendingMountId`, test helpers `keyedGate`, `countMounts`, `dappA`
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-testing-stress-edge-case-regression-coverage/04-VERIFICATION.md
@.planning/phases/04-testing-stress-edge-case-regression-coverage/04-CONTEXT.md
@src/shell.ts
@src/lifecycle.ts
@tests/stress.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invalidate the in-flight pending mount in handleRouteChange's null-manifest branch</name>
  <files>src/shell.ts</files>
  <read_first>
    - src/shell.ts — read `handleRouteChange` (lines 358-368) and `mountDapp` (lines 370-412) to see how the shell-level `pendingMountId` closure variable is set (line 397) and cleared in the `finally` (line 410)
    - src/lifecycle.ts — read `invalidatePendingMount` (lines 440-447): it bumps `mountGeneration` only when `inFlightMountId === id`, which makes the in-flight `mount()` call's `isStale()` gate fail at its next check; it is a safe no-op when `id` is not the currently in-flight mount
    - .planning/phases/04-testing-stress-edge-case-regression-coverage/04-CONTEXT.md — D-01 (line 11): "Under concurrent mounts, the final DOM and lifecycle.getCurrentDapp() MUST match the most recent navigation. Stale in-flight mounts must not complete"
  </read_first>
  <behavior>
    - When `handleRouteChange` receives a null manifest AND a mount is in flight (shell-level `pendingMountId` is a non-null string), the in-flight mount is superseded: it must not inject DOM, must not set currentDappId, and must not emit dx:mount / dx:dapp:mounted / dx:route:subpath.
    - When `handleRouteChange` receives a null manifest AND no mount is in flight (`pendingMountId` is null), behavior is unchanged: unmount() runs, dx:route:changed fires. No error, no crash.
    - The existing dapp->dapp supersession path (mountDapp) is unaffected — its own generation bump inside lifecycle.mount() still occurs.
  </behavior>
  <action>
    In the `else` (null-manifest) branch of `handleRouteChange` (currently only `lifecycle.unmount();` at line 362), invalidate any in-flight mount BEFORE calling `lifecycle.unmount()`: when the shell-level `pendingMountId` is non-null, call `lifecycle.invalidatePendingMount(pendingMountId)`. Guard the call on `pendingMountId` being truthy so it is a no-op when nothing is mounting. This is the same wiring already used by `disableDapp()` at src/shell.ts:136, applied to the unmatched-route branch.

    Add a single one-line "why" comment (per CLAUDE.md comment style: explain the why, not the what) stating that navigating to an unmatched route must supersede an in-flight mount so a stale dapp cannot commit under the new URL (D-01). Do not add JSDoc, section headers, or multi-line commentary.

    Do NOT change the dx:route:changed emit, the `if (manifest)` branch, `mountDapp`, or any other function. Do NOT alter WR-01..WR-07 / IN-01..IN-04 code paths.
  </action>
  <verify>
    <automated>grep -nE 'invalidatePendingMount\(pendingMountId\)' src/shell.ts | grep -c . && pnpm vitest run tests/stress.test.ts tests/shell.test.ts tests/lifecycle.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'invalidatePendingMount\(pendingMountId\)' src/shell.ts` returns at least 2 matches (the existing disableDapp call at line ~136 plus the new null-branch call).
    - The new call sits inside `handleRouteChange`'s `else` branch and is guarded by a truthiness check on `pendingMountId`.
    - `pnpm vitest run tests/stress.test.ts tests/shell.test.ts tests/lifecycle.test.ts` passes with zero regressions (all previously-green scenarios still pass).
    - `make lint` (biome) reports clean on src/shell.ts.
  </acceptance_criteria>
  <done>Navigating to a null-manifest route while a dapp is mid-mount bumps the mount generation so the in-flight mount is superseded before it can inject DOM or emit dx:mount; no existing test regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Add dapp->unmatched-route in-flight supersession regression scenario to the stress suite</name>
  <files>tests/stress.test.ts</files>
  <read_first>
    - tests/stress.test.ts — read the whole file: reuse the `keyedGate` helper (lines 25-42), `countMounts` (lines 44-51), the `dappA` manifest (lines 89-96), and the existing beforeEach/afterEach + `tick` pattern; add the new scenario inside the existing `describe('stress: concurrency & mount races (TEST-01, D-01/D-02/D-03)')` block, alongside the "rapid A -> B -> A" and "disableDapp() while that dapp is mid-mount" scenarios
    - src/shell.ts — confirm `handleRouteChange(null)` is reached when navigating to a route no manifest matches, and that `getCurrentRoute()` reports the unmatched path
  </read_first>
  <action>
    Add one `it(...)` scenario inside the existing top-level describe block (history mode, driven through `createShell()` + `shell.navigate()`, matching the file's established pattern). Name it to describe navigating to an unmatched route while a dapp mount is in flight and asserting the mount is abandoned.

    Scenario shape:
    - Build a dapp-A manifest variant WITH a template (spread `dappA` and add `template: '/dapps/a/tpl.html'`) so a buggy commit would write the template into the container — making the "container stays empty" assertion load-bearing.
    - Create a `keyedGate<string>()` for the template loader and pass it as `templateLoader`; use a resolving `scriptLoader` stub (`async () => {}`) and `styleLoader: async () => {}`. Register only the dapp-A-with-template manifest so any other path is unmatched.
    - Attach `countMounts('a')` and a `dx:route:subpath` listener that pushes into an array (mirror the existing subpath test at lines 262-264).
    - `shell.navigate('/a')` then `await tick()` — A's mount suspends at the held template gate, container still empty.
    - `shell.navigate('/nowhere')` (a route no manifest matches) then `await tick()` — this drives `handleRouteChange(null)`, which must invalidate the in-flight A mount.
    - `templateGate.release('/dapps/a/tpl.html', '<div data-dapp="a">A content</div>')` then `await tick()` — the released, now-stale mount must hit its `isStale()` gate and return before the DOM write.
    - Assert: `mounts.count()` is `0`; `container.innerHTML` is `''` (never populated with A's content); the captured `dx:route:subpath` array has length `0`; `shell.getCurrentRoute()` is `'/nowhere'`.
    - Clean up listeners (`mounts.cleanup()`, `removeEventListener` for the subpath handler) at the end, matching the file's existing cleanup discipline.

    Do NOT modify existing scenarios or helpers. Do NOT add hash-mode cases (the suite is deliberately history-mode-only per the file header). Do NOT introduce `test.fails` — the scenario must pass green against the Task 1 fix (D-04).
  </action>
  <verify>
    <automated>pnpm vitest run tests/stress.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/stress.test.ts` passes with one more scenario than before (6 scenarios in the file's describe blocks) and zero failures.
    - The new scenario asserts all four: zero dx:mount for `a`, `container.innerHTML === ''`, zero dx:route:subpath events, and `shell.getCurrentRoute() === '/nowhere'`.
    - Temporarily reverting the Task 1 change causes THIS scenario to fail (fix is load-bearing) — the executor may confirm mentally from the code path; do not commit a reverted state.
    - `make lint` reports clean on tests/stress.test.ts.
  </acceptance_criteria>
  <done>A committed regression test drives navigate('/a', held template) -> navigate('/nowhere') -> release template and proves the in-flight A mount is abandoned (no dx:mount, empty container, no dx:route:subpath), locking the D-01 invariant for the dapp->unmatched-route transition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dapp script -> shell container (#dx-mount) | A dapp's template/entry is injected into the shared mount container; only the dapp matching the current route may own it |
| browser navigation -> shell route resolution | User-driven URL changes (including to unmatched routes) drive mount/unmount decisions |

## STRIDE Threat Register
(ASVS level 1; blocking threshold: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | handleRouteChange null branch (src/shell.ts) | medium | mitigate | This plan's fix: a stale dapp's DOM/template can currently commit under an unmatched URL, misrepresenting which dapp owns the container. Invalidating the pending mount prevents the stale commit — closing the misattribution. |
| T-04-02 | Tampering | #dx-mount container | low | mitigate | Superseded mount injecting stale content into a container the shell has navigated away from — same fix (generation bump before DOM write) prevents the stale write. |
| T-04-03 | Information Disclosure | dx:route:subpath event payload | low | accept | Misattributed subpath event names the stale dapp with the new path; eliminated as a side effect of the fix. No dapp-secret exposure — event names route/id only. Accepted residual: no new mitigation beyond the fix. |

No package-manager installs in this plan (zero-dep posture preserved) — no T-04-SC supply-chain checkpoint required.
</threat_model>

<verification>
- `pnpm vitest run tests/stress.test.ts tests/shell.test.ts tests/lifecycle.test.ts` — all pass, new scenario included.
- `make test` — full suite green (309/309 expected: prior 308 + 1 new scenario), lint clean.
- `grep -nE 'invalidatePendingMount\(pendingMountId\)' src/shell.ts` — matches in both `disableDapp` and `handleRouteChange`.
- No debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) introduced in src/shell.ts or tests/stress.test.ts.
</verification>

<success_criteria>
- D-01 last-navigation-wins invariant holds for the dapp->unmatched-route transition: navigating to a null-manifest route while a dapp mount is in flight abandons that mount (no dx:mount, no dx:dapp:mounted, empty container, no dx:route:subpath), while getCurrentRoute() reports the unmatched route.
- The regression is locked by a committed, green (not test.fails) stress scenario.
- No existing test regresses; full suite and lint pass.
- No out-of-scope findings (WR-01..WR-07, IN-01..IN-04) touched.
</success_criteria>

<output>
Create `.planning/phases/04-testing-stress-edge-case-regression-coverage/04-04-SUMMARY.md` when done.
</output>
