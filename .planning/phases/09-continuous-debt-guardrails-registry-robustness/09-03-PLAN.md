---
phase: 09-continuous-debt-guardrails-registry-robustness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - renovate.json
  - tests/renovate-config.test.ts
autonomous: true
requirements: [GATE-03]
user_setup:
  - service: mend-renovate
    why: "Renovate config is inert until the hosted Mend Renovate GitHub App is installed on the repo/org (D-01). Committing renovate.json does not make automation live."
    dashboard_config:
      - task: "Install the Mend Renovate GitHub App and grant it access to the dxkit repository"
        location: "https://github.com/apps/renovate → Configure → select the dxkit repo/org"
must_haves:
  truths:
    - "`renovate.json` extends `config:recommended`, sets `minimumReleaseAge: \"3 days\"` (D-03), and encodes a toolchain group (tsup, vite, vitest, happy-dom, @biomejs/biome, typescript) that never automerges — majors always blocked (D-02, D-04)."
    - "Non-major (patch/minor) devDependencies outside the toolchain group automerge after gating; all major bumps open a review PR (D-02)."
    - "A weekly `lockFileMaintenance` PR refreshes pnpm-lock.yaml (D-04), expressed as an object (not a boolean)."
  artifacts:
    - "renovate.json — repo-root Renovate config encoding D-01..D-04"
    - "tests/renovate-config.test.ts — schema/invariant guard test"
  key_links:
    - "renovate.json → Mend Renovate GitHub App (operator-installed) → dependency-freshness PRs for the pnpm workspace"
  prohibitions:
    - "MUST NOT use deprecated keys: `config:base`, `matchPackagePatterns`, or a boolean `lockFileMaintenance` (P5)."
    - "MUST NOT add any npm dependency to make this work — renovate.json is consumed by an external hosted app (P4)."
  backstops:
    - statement: "Renovate auto-detects the pnpm workspace and excludes internal workspace:* links — no special config needed."
      verification: backstop
---

<objective>
GATE-03: configure Renovate for the pnpm workspace via a committed `renovate.json` — grouped PRs,
3-day release-age gating, weekly lockfile maintenance, and an automerge policy that always blocks
unreviewed major toolchain bumps.

Purpose: Continuous dependency-freshness with a supply-chain-aware automerge policy.
Output: `renovate.json` (root) + a guard test locking its load-bearing invariants; the Mend App
install is a recorded operator next-step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-CONTEXT.md
@.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author renovate.json (D-01..D-04)</name>
  <files>renovate.json</files>
  <read_first>
    - package.json (devDependencies list — the six toolchain packages: tsup, vite, vitest, happy-dom, @biomejs/biome, typescript)
    - .planning/phases/09-continuous-debt-guardrails-registry-robustness/09-RESEARCH.md (Pattern 3 skeleton + Pitfall 3 schema-drift note)
    - .planning/phases/09-continuous-debt-guardrails-registry-robustness/09-CONTEXT.md (D-01..D-04)
  </read_first>
  <action>
    Create `renovate.json` at the repo root with: `$schema` set to `https://docs.renovatebot.com/renovate-schema.json`; `extends: ["config:recommended"]`; `minimumReleaseAge: "3 days"`; and a `packageRules` array containing (rule A) a toolchain group — `groupName: "toolchain"`, `matchPackageNames` listing exactly tsup, vite, vitest, happy-dom, @biomejs/biome, typescript, `automerge: false`; (rule B) the same six `matchPackageNames` with `matchUpdateTypes: ["major"]` and `automerge: false` (reinforces always-blocked majors, D-02); (rule C) `matchDepTypes: ["devDependencies"]`, `matchUpdateTypes: ["patch","minor"]` excluding the six toolchain names, `automerge: true`, `automergeType: "pr"`; (rule D) `matchDepTypes: ["devDependencies"]`, `matchUpdateTypes: ["major"]`, `automerge: false`. Add a `lockFileMaintenance` OBJECT: `{ "enabled": true, "schedule": ["before 4am on monday"], "automerge": false }`. Before finalizing, re-check the live schema per Pitfall 3. Do NOT use `config:base`, `matchPackagePatterns`, or a boolean `lockFileMaintenance` (P5). No pnpm-specific top-level key is needed (Renovate auto-detects the workspace).
  </action>
  <verify>
    <automated>node -e "const c=require('./renovate.json'); if(!c.extends.includes('config:recommended')) process.exit(1); if(c.minimumReleaseAge!=='3 days') process.exit(1); if(typeof c.lockFileMaintenance!=='object') process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `renovate.json` parses as valid JSON.
    - `extends` includes `config:recommended`; `minimumReleaseAge` is `"3 days"`; `lockFileMaintenance` is an object with `enabled: true`.
    - A toolchain `packageRules` entry lists all six toolchain packages with `automerge: false`.
    - `grep -E "config:base|matchPackagePatterns" renovate.json` returns nothing.
    - `grep -E "\"lockFileMaintenance\"[[:space:]]*:[[:space:]]*(true|false)" renovate.json` returns nothing (object, not boolean).
  </acceptance_criteria>
  <done>renovate.json encodes D-01..D-04 with no deprecated keys.</done>
</task>

<task type="auto">
  <name>Task 2: Guard test for renovate.json invariants</name>
  <files>tests/renovate-config.test.ts</files>
  <read_first>
    - renovate.json (from Task 1)
    - tests/typecheck-config.test.ts (guard-test style)
  </read_first>
  <action>
    Create `tests/renovate-config.test.ts` that reads and JSON-parses `renovate.json` and asserts: `extends` contains `config:recommended`; `minimumReleaseAge === "3 days"`; a `packageRules` entry names all six toolchain packages and sets `automerge: false`; the raw file text contains none of the deprecated tokens `config:base` or `matchPackagePatterns`; and `lockFileMaintenance` is an object (typeof object, not boolean) with `enabled: true`. Optionally, if `npx --yes renovate-config-validator` is available offline, note it in a skipped/conditional assertion — but do not add it as a dependency (P4).
  </action>
  <verify>
    <automated>npx vitest run tests/renovate-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/renovate-config.test.ts` passes.
    - The test fails if a deprecated key (`config:base`, `matchPackagePatterns`, boolean `lockFileMaintenance`) is reintroduced.
    - The test fails if any toolchain package is dropped from the always-blocked group.
  </acceptance_criteria>
  <done>renovate.json's load-bearing invariants are locked by an automated guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → toolchain deps → build | a freshly-published or compromised version could be pulled before the community flags it |
| Mend Renovate App → repo (external) | hosted app opens PRs; automerge policy governs which land unattended |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Tampering | Toolchain dependency updates (V10 Supply Chain) | medium | mitigate | `minimumReleaseAge: "3 days"` filters yanked/compromised fresh releases; the toolchain group's always-blocked-major-automerge (D-02) forces human review for tsup/vite/vitest/happy-dom/@biomejs/biome/typescript majors; CI (typecheck + smoke + tests) gates the automerged non-major tier. |
| T-09-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs zero packages; renovate.json is consumed by an external hosted app. No repo-side install surface to gate (P4). |
</threat_model>

<artifacts>
## Artifacts this plan produces
- Config: `renovate.json` (repo root) — GATE-03
- Test: `tests/renovate-config.test.ts` — schema/invariant guard
- Operator next-step (user_setup): install the Mend Renovate GitHub App
</artifacts>

<verification>
- `npx vitest run tests/renovate-config.test.ts` green.
- Manual: read renovate.json against the live Renovate schema (Pitfall 3).
- Record the Mend App install as a phase operator next-step in STATE.md.
</verification>

<success_criteria>
Renovate is configured for the pnpm workspace with grouped PRs, 3-day release-age gating, and an automerge policy that blocks unreviewed major toolchain bumps (GATE-03, ROADMAP success criterion #3). Automation goes live once the operator installs the Mend App.
</success_criteria>

<output>
Create `.planning/phases/09-continuous-debt-guardrails-registry-robustness/09-03-SUMMARY.md` when done.
</output>
