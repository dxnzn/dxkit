---
phase: 08-forward-compat-typing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - tests/typecheck-config.test.ts
autonomous: true
requirements: [FCT-01, FCT-02, FCT-03]
must_haves:
  truths:
    - "Root tsconfig.json compilerOptions contains verbatimModuleSyntax:true, isolatedDeclarations:true, and erasableSyntaxOnly:true (D-01) — all three flags live in the single base config inherited by plugins via extends and read by the tsc --emitDeclarationOnly onSuccess pass."
    - "make typecheck passes for core + all 4 plugins under TypeScript 6.0.3 with all three flags on."
    - "make build emits dist/index.d.ts for all 5 packages — isolatedDeclarations .d.ts emission succeeds via each package's tsc --emitDeclarationOnly onSuccess pass (FCT-02)."
    - "make test stays green after the flags land (FCT-01 build-and-tests-green criterion)."
    - "[edge:FCT-01/empty] Root tsconfig.json compilerOptions.verbatimModuleSyntax is exactly true (not missing, not false) — asserted by the flag-presence guard test, so a silent flag removal fails the suite."
    - "[edge:FCT-02/empty] Root tsconfig.json compilerOptions.isolatedDeclarations is exactly true (not missing, not false) — asserted by the flag-presence guard test."
    - "[edge:FCT-03/empty] Root tsconfig.json compilerOptions.erasableSyntaxOnly is exactly true (not missing, not false) — asserted by the flag-presence guard test."
  artifacts:
    - "tsconfig.json — three new compilerOptions flags"
    - "tests/typecheck-config.test.ts — new describe block asserting the three flags are present and true (Wave 0 durable regression guard for FCT-01/FCT-03)"
  key_links:
    - "root tsconfig.json → inherited by plugins/*/tsconfig.json via extends '../../tsconfig.json' and read by the tsc --emitDeclarationOnly -p tsconfig.json onSuccess hook in all 5 tsup configs — one edit turns the flags on everywhere and guards the real .d.ts emit in lockstep"
  prohibitions:
    - "isolatedDeclarations annotations must never be manufactured where the compiler does not demand them — if the flag surfaces zero required annotations (the research-verified outcome), land the flag-on commit only and add no invented type annotations (D-02)."
---

<objective>
Enable the three strict forward-compat compiler flags — verbatimModuleSyntax (FCT-01), erasableSyntaxOnly (FCT-03), and isolatedDeclarations (FCT-02) — in the root base tsconfig.json (D-01), fixing whatever they surface at the source (no shims, no manufactured churn), and add a durable flag-presence regression guard so a silent flag removal fails the test suite.

Purpose: Make the eventual TS7 jump a config swap, not a rewrite. All three flags were empirically verified against the real codebase this research session to compile with zero errors, including real .d.ts emission for all 5 packages — so this is a near-no-op flag-flip with a durable guard, not a refactor.
Output: tsconfig.json with the three flags on; a flag-presence guard block in tests/typecheck-config.test.ts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-forward-compat-typing/08-CONTEXT.md
@.planning/phases/08-forward-compat-typing/08-RESEARCH.md
@.planning/phases/08-forward-compat-typing/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable verbatimModuleSyntax + erasableSyntaxOnly in root tsconfig.json (FCT-01, FCT-03)</name>
  <files>tsconfig.json</files>
  <read_first>
    - tsconfig.json — the file being edited (current 15-key compilerOptions block; add flags near isolatedModules/declaration to match the existing strictness/emit grouping)
    - .planning/phases/08-forward-compat-typing/08-PATTERNS.md — tsconfig.json section (exact placement guidance; no JSON-comment/ordering convention beyond alphabetical-ish grouping)
    - .planning/phases/08-forward-compat-typing/08-RESEARCH.md — Summary + FCT-01/FCT-03 rows (both flags verified zero-error against the real codebase; erasableSyntaxOnly is a true no-op — zero classes/enums/namespaces/parameter-properties exist)
    - plugins/wallet/tsconfig.json — confirms plugins inherit via extends '../../tsconfig.json' (no per-plugin edit needed for the flags)
  </read_first>
  <action>Add compilerOptions keys verbatimModuleSyntax set to true and erasableSyntaxOnly set to true in the root tsconfig.json (per D-01, base config only — plugins inherit via extends, do NOT edit any plugin tsconfig). Place them adjacent to isolatedModules/declaration to match the existing grouping. Per D-06 these two near/actual-no-op flags land together as one bisectable commit. Do NOT manufacture any source change to satisfy erasableSyntaxOnly — research grep-confirmed zero non-erasable syntax exists; if make typecheck is green with the flag on, stop. If verbatimModuleSyntax surfaces any import/export elision error at source (research expects none), fix it at the source with import type / export type — never a shim.</action>
  <verify>
    <automated>make typecheck && make build && make test</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json compilerOptions.verbatimModuleSyntax === true and compilerOptions.erasableSyntaxOnly === true (source assertion)
    - No plugin tsconfig.json was modified (the flags are inherited via extends)
    - `make typecheck` exits 0 for root + all 4 plugins
    - `make build` exits 0 (all 5 packages, all 3 output formats + .d.ts)
    - `make test` exits 0 (existing vitest suite stays green)
    - No shim key (e.g. ignoreDeprecations) was added; no source file was changed to satisfy erasableSyntaxOnly
  </acceptance_criteria>
  <done>verbatimModuleSyntax and erasableSyntaxOnly are true in root tsconfig.json; typecheck, build, and the existing test suite are all green under TS6 with zero at-source churn.</done>
</task>

<task type="auto">
  <name>Task 2: Enable isolatedDeclarations in root tsconfig.json, at-source annotations only if demanded (FCT-02)</name>
  <files>tsconfig.json</files>
  <read_first>
    - tsconfig.json — the file being edited (now carries the two flags from Task 1)
    - .planning/phases/08-forward-compat-typing/08-RESEARCH.md — FCT-02 row, Architecture Patterns diagram, Common Pitfalls #4 (the tsc --emitDeclarationOnly onSuccess pass fails loudly, not silently; isolatedDeclarations verified to require ZERO annotations anywhere in src/ or plugins/*/src/), Anti-Patterns to Avoid (do not manufacture annotations)
    - .planning/phases/08-forward-compat-typing/08-CONTEXT.md — D-02 (minimal at-source annotations only), D-03 (BREAKING CHANGE note only if a public type shape actually changes), Claude's Discretion (degenerate zero-annotation case is the real outcome)
    - src/index.ts — the core entry barrel (factory returns already annotated with named interfaces; the surface isolatedDeclarations would touch if anything)
  </read_first>
  <action>Add compilerOptions key isolatedDeclarations set to true in root tsconfig.json (D-01, base config only). Per D-06 this is its own bisectable commit, core-before-plugins in effect (base config applies to core's emit first). Run make build and confirm each package's tsc --emitDeclarationOnly onSuccess pass emits dist/index.d.ts with zero errors. Per D-02, add explicit return-type/type annotations at the source ONLY where the compiler emits an isolatedDeclarations error — research verified the count is zero, so the expected outcome is a flag-on-only commit with no source edits; do NOT invent annotations to look thorough. Per D-03, write a BREAKING CHANGE footer + migration note ONLY if a consumer-visible public type shape actually changes (module augmentation on Context / window.__DXKIT__); if nothing public shifts, this is a non-breaking additive config change and no note is manufactured.</action>
  <verify>
    <automated>make build && make typecheck && make test</automated>
  </verify>
  <acceptance_criteria>
    - tsconfig.json compilerOptions.isolatedDeclarations === true (source assertion)
    - `make build` exits 0 and dist/index.d.ts exists for all 5 packages (core + settings + wallet + auth + theme) after the build (behavior assertion: `test -f dist/index.d.ts && for d in plugins/settings plugins/wallet plugins/auth plugins/theme; do test -f $d/dist/index.d.ts; done`)
    - `make typecheck` and `make test` exit 0
    - Any source annotation added is traceable to a specific compiler-emitted isolatedDeclarations error (no manufactured annotations); if zero errors surfaced, zero source files changed
    - A BREAKING CHANGE footer exists in the commit iff a public consumer-visible type shape changed; absent otherwise (D-03)
  </acceptance_criteria>
  <done>isolatedDeclarations is true in root tsconfig.json; .d.ts emit succeeds for every package; only compiler-demanded annotations (expected: none) were added at source.</done>
</task>

<task type="auto">
  <name>Task 3: Add flag-presence regression guard to tests/typecheck-config.test.ts (FCT-01, FCT-03 durable guard)</name>
  <files>tests/typecheck-config.test.ts</files>
  <read_first>
    - tests/typecheck-config.test.ts — the file being extended (has readConfigFile/parseJsonWithComments helpers and the existing "TypeScript 6 Config Invariants" describe; mirror its readConfigFile + expect().toBe(true) style)
    - .planning/phases/08-forward-compat-typing/08-PATTERNS.md — "Config-guard test style" shared pattern + "Config-guard test style (read raw JSON/JSONC, assert on shape)" (this file already contains the reusable helpers; extend it with a new describe block, no new file needed)
    - .planning/phases/08-forward-compat-typing/08-RESEARCH.md — Validation Architecture Wave 0 Gaps (a tsconfig flag-presence guard test is the recommended durable regression check for FCT-01/FCT-03, not just a one-time green build)
  </read_first>
  <action>Add a new describe block to tests/typecheck-config.test.ts (reusing the existing readConfigFile helper) that reads root tsconfig.json and asserts compilerOptions.verbatimModuleSyntax, compilerOptions.isolatedDeclarations, and compilerOptions.erasableSyntaxOnly are each exactly true. This is the durable regression guard so a future silent flag removal fails make test. Assert on the root config only (D-01 single source of truth; plugins inherit). Cite FCT-01/FCT-02/FCT-03 in the test descriptions. Keep the zero-@types/node posture — the file already imports node:fs/node:path typed via tests/node-builtins.d.ts.</action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - tests/typecheck-config.test.ts contains a new describe block asserting all three flags are `true` in root tsconfig.json (source assertion)
    - The three assertions use expect(...).toBe(true) against readConfigFile('tsconfig.json').compilerOptions (behavior assertion)
    - `make test` exits 0 with the new assertions passing
    - Manually setting any one flag to false (or deleting it) makes the new test fail (negative-case reasoning; do not commit the mutation)
    - No @types/node devDependency was added
  </acceptance_criteria>
  <done>A durable guard in tests/typecheck-config.test.ts asserts the three forward-compat flags are present and true; make test is green.</done>
</task>

</tasks>

<edge_coverage>
## Edge Coverage — Flagged Assumptions (spec-less probe fallback, no silent drops)

The deterministic edge probe emitted adjacency/empty/ordering rows for FCT-01/02/03. The `empty` rows are covered as must_haves.truths above (flag must be exactly `true`, guarded by Task 3). The remaining rows are N/A for a boolean compiler flag and are surfaced here rather than dropped:

| Probe row | Disposition | Rationale |
|-----------|-------------|-----------|
| FCT-01 / adjacency | N/A (flagged) | A boolean tsconfig flag has no "two elements merge/collide/separate" input domain. Presence-and-value is the only meaningful invariant — covered by Task 3's guard. |
| FCT-01 / ordering | N/A (flagged) | A boolean flag has no element-ordering / stability domain. |
| FCT-02 / adjacency | N/A (flagged) | A boolean flag has no adjacency domain. (The related "do all 5 IIFE globals coexist without collision" adjacency concern belongs to FCT-04 and is covered in Plan 02.) |
| FCT-02 / ordering | N/A (flagged) | A boolean flag has no ordering domain. |
| FCT-03 / adjacency | N/A (flagged) | A boolean flag has no adjacency domain. |
| FCT-03 / ordering | N/A (flagged) | A boolean flag has no ordering domain. |

No-silent-drop equality (this plan's rows): 6 flagged (above) + 3 covered truths (FCT-01/02/03 empty) = 9 probe rows accounted for. The 10th row (FCT-04 unclassified) is owned by Plan 02.

§B prohibition recall survivor authored into must_haves.prohibitions: "isolatedDeclarations annotations must never be manufactured" (values/discipline survivor from D-02). Routine-engineering candidates (flag placement, config inheritance) dropped as non-survivors.
</edge_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain → tsconfig | Compiler flags only; no runtime input surface, no network, no user data crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | tsconfig.json compiler flags | low | mitigate | Task 3's flag-presence guard test makes a silent flag removal (or a reintroduced suppression shim) fail `make test`, matching the project's "failures are visible, never silent" Core Value. |
| T-08-SC | Tampering | npm/pnpm installs (supply chain) | low | accept | This phase installs zero new packages (research Package Legitimacy Audit = n/a); all tooling is existing devDependencies or Node builtins. No [ASSUMED]/[SUS] packages exist, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `make typecheck` green for root + all 4 plugins under TS6 with all three flags on.
- `make build` green; dist/index.d.ts present for all 5 packages (isolatedDeclarations emit succeeds).
- `make test` green including the new flag-presence guard block.
- No shim keys, no manufactured source annotations (git diff on src/ + plugins/*/src/ is empty unless a real compiler error demanded a fix).
</verification>

<success_criteria>
- FCT-01: verbatimModuleSyntax enabled in root tsconfig.json; build and tests stay green.
- FCT-02: isolatedDeclarations enabled; .d.ts emit succeeds for every package.
- FCT-03: erasableSyntaxOnly enabled; no non-erasable syntax remains (verified: none existed).
- Durable guard asserts the three flags are true so future removal is caught.
</success_criteria>

<output>
Create `.planning/phases/08-forward-compat-typing/08-01-SUMMARY.md` when done
</output>
