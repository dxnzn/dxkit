---
phase: 07-typescript-6-migration-standalone-typecheck
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - Makefile
autonomous: true
requirements: [TS6-03]
must_haves:
  truths:
    - "`make typecheck` runs `tsc --noEmit -p tsconfig.typecheck.json` for the root package + all 4 plugins and exits 0 on the currently-resolved TypeScript"
    - "`make test` runs typecheck as a prerequisite before vitest (lint -> typecheck -> vitest, D-06) and exits 0 — the committed green pre-bump baseline"
    - "`make typecheck` catches an intentionally-introduced type error (fails non-zero), proving the gate actually checks and is not a no-op"
    - "The typecheck loop reuses PLUGIN_BUILD_ORDER so its package list cannot drift from build/verify-outputs (D-05)"
    - "The typecheck step is a distinct standalone target (not folded into another) so Phase 9's deprecation gate can call `make typecheck` directly (D-06)"
  artifacts:
    - "Makefile — new `typecheck` target; `test`/`test-watch` gain `typecheck` as a prerequisite; `.PHONY` lists `typecheck`"
  key_links:
    - "`make test` is the single choke point CI already runs (make build + make test) — wiring typecheck here means CI inherits it with no ci.yml edit (D-06)"
    - "typecheck target loops root first then PLUGIN_BUILD_ORDER, mirroring the verify-outputs target shape (Makefile:77-95)"
  prohibitions:
    - "the typecheck target emits files / duplicates tsup's dts build instead of being noEmit-only"
    - "a package is silently excluded from the typecheck loop so it never gets checked"
    - "the typecheck target is folded into another target instead of being a standalone attach point for the Phase 9 gate"
---

<objective>
Add a standalone `make typecheck` target that runs `tsc --noEmit -p tsconfig.typecheck.json` for
the root package and each plugin, wire it as a prerequisite of `make test`/`make test-watch`
(ordering lint -> typecheck -> vitest), and register it in `.PHONY`. With Plans 07-01/07-02
already green, this makes the whole standalone typecheck green on the currently-resolved
TypeScript — the committed, measurable pre-bump baseline (Success Criterion 1 / D-07 step 1).

Purpose: Completes TS6-03's standalone typecheck step and gives CI the check with no ci.yml edit
(CI already runs `make build` + `make test`). Keeps it a distinct target so Phase 9's deprecation
gate can call it directly.
Output: an edited `Makefile` and a green `make typecheck` + `make test` on today's TypeScript.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-CONTEXT.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-PATTERNS.md
</context>

## Artifacts this phase produces

This plan (07-03) produces:
- **`Makefile` `typecheck` target** (NEW) — root-first + `PLUGIN_BUILD_ORDER` loop running `tsc --noEmit -p tsconfig.typecheck.json` per package.
- **`Makefile` `test` / `test-watch` prerequisite change** — `lint typecheck` (was `lint`).
- **`Makefile` `.PHONY` addition** of `typecheck`.

Depends on the 5 `tsconfig.typecheck.json` files from plans 07-01 (root) and 07-02 (4 plugins). Sibling plan 07-04 produces the `typescript@^6.0.0` bump + `pnpm-lock.yaml` update.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the make typecheck target and wire it into make test</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (line 5 PLUGIN_BUILD_ORDER; line 7 .PHONY; lines 33-37 test/test-watch; lines 77-95 verify-outputs — the structural template)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md (Pattern 2: verified typecheck target + test prereq shape)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-PATTERNS.md (Makefile section — exact target + prereq + .PHONY edits)
    - tsconfig.typecheck.json + plugins/*/tsconfig.typecheck.json (the configs this target invokes — created by 07-01/07-02)
  </read_first>
  <action>
    Add a `typecheck` target that first runs `npx tsc --noEmit -p tsconfig.typecheck.json` for the
    root package, then loops `$(PLUGIN_BUILD_ORDER)` running `(cd $$dir && npx tsc --noEmit -p
    tsconfig.typecheck.json) || exit 1` per plugin — structurally identical to the `verify-outputs`
    target (Makefile:77-95), reusing `PLUGIN_BUILD_ORDER` verbatim (D-05, do not redefine the
    package list). Change the `test` and `test-watch` prerequisites from `lint` to `lint typecheck`
    so the order is lint -> typecheck -> vitest (D-06; lint first is fastest, Claude's discretion
    per CONTEXT.md). Add `typecheck` to the `.PHONY` line (Makefile:7). Do NOT edit `ci.yml` — CI
    already invokes `make test`, so it inherits typecheck automatically (D-06). Keep `typecheck` a
    standalone target (do not fold it into `test`) so Phase 9's scoped deprecation gate (GATE-01)
    can call `make typecheck` directly. Do NOT add any emit/output flag — the configs are
    noEmit-only and this target must never duplicate tsup's dts build.
  </action>
  <verify>
    <automated>make typecheck && make test</automated>
  </verify>
  <acceptance_criteria>
    - `make typecheck` exits 0 and its output shows all 5 packages typechecked (root + settings + wallet + auth + theme)
    - `make test` exits 0 with typecheck running before vitest (321 tests pass)
    - `grep -n "typecheck" Makefile` shows it in `.PHONY`, as a target, and as a prereq of both `test` and `test-watch`
    - The `typecheck` target reuses `$(PLUGIN_BUILD_ORDER)` (no hardcoded second package list)
    - Negative check: temporarily edit any typechecked source/test to introduce a type error, run `make typecheck`, confirm it exits non-zero, then revert — proves the target actually checks
  </acceptance_criteria>
  <done>`make typecheck` is a standalone target covering all 5 packages, wired as a `make test` prereq; the full standalone typecheck is green on today's TypeScript — the committed baseline.</done>
</task>

</tasks>

<flagged_assumptions>
Spec-less probe edges relevant to this plan (surfaced, not silently dropped):
- **TS6-03 / unclassified:** the standalone typecheck is genuinely independent of tsup's `dts:true` emit — it is a separate program reading a separate `tsconfig.typecheck.json` (noEmit) and does not touch the `build` target's `tsup` invocation. Confirmed: `make typecheck` produces no `dist/` output.
- **TS6-01 / empty (target level):** a package with zero source errors still executes in the loop and passes (not skipped). Confirmed by the per-package echo output of `make typecheck`.
- **TS6-01 / ordering (target level):** the root-first-then-PLUGIN_BUILD_ORDER sequence is deterministic and outcome-independent per package. Confirmed by `make typecheck` exit 0.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A Makefile target that invokes `tsc --noEmit` — build-time only, no new runtime inputs, network, auth, or data flow. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | Makefile typecheck target | low | accept | No new runtime attack surface — build-time typecheck wiring only; noEmit configs produce no artifacts; zero runtime deps preserved. |

No new runtime attack surface is introduced by this plan.
</threat_model>

<verification>
- `make typecheck` exits 0 (root + 4 plugins)
- `make test` exits 0 (typecheck prereq runs before vitest; 321 tests pass)
- `make typecheck` produces no files under any `dist/` (noEmit confirmed)
- `.PHONY` includes `typecheck`; `test`/`test-watch` both depend on `typecheck`
</verification>

<success_criteria>
The standalone `make typecheck` target exists, covers all 5 packages via `PLUGIN_BUILD_ORDER`, is
wired as a `make test` prerequisite, and the full typecheck is green on the currently-resolved
TypeScript — a committed, bisectable pre-bump baseline that CI inherits with no workflow edit.
</success_criteria>

<output>
Create `.planning/phases/07-typescript-6-migration-standalone-typecheck/07-03-SUMMARY.md` when done.
</output>
