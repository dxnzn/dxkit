---
phase: 07-typescript-6-migration-standalone-typecheck
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/auth/tsconfig.typecheck.json
  - plugins/wallet/tsconfig.typecheck.json
  - plugins/theme/tsconfig.typecheck.json
  - plugins/settings/tsconfig.typecheck.json
  - plugins/auth/tests/auth.test.ts
  - plugins/wallet/tests/wallet.test.ts
  - plugins/theme/tests/theme.test.ts
  - plugins/settings/tests/settings.test.ts
autonomous: true
requirements: [TS6-01, TS6-03]
must_haves:
  truths:
    - "Each of the 4 plugins' `npx tsc --noEmit -p tsconfig.typecheck.json` exits 0 on the currently-resolved TypeScript"
    - "Each plugin typecheck program includes its own tests/ (D-01) and resolves @dnzn/dxkit + sibling plugins to real src/*.ts via paths (D-03)"
    - "Every plugin package is covered — none is silently excluded from getting its own typecheck config (4/4 plugins)"
    - "The plugin typecheck configs run without a prior build (paths point at src/*.ts, not unbuilt dist/*.d.ts)"
  artifacts:
    - "plugins/{auth,wallet,theme,settings}/tsconfig.typecheck.json — noEmit:true, rootDir:'../..', include:['src','tests'], 5-key paths, NO baseUrl"
  key_links:
    - "each plugin typecheck config extends ./tsconfig.json (which extends ../../tsconfig.json) — never skips straight to root (D-02)"
    - "rootDir widened to '../..' (monorepo root) so cross-package paths resolving to sibling src/*.ts do not trip TS6059 (RESEARCH Pitfalls 1-2)"
  prohibitions:
    - "a plugin package is silently excluded from the typecheck loop so it never gets checked"
    - "a plugin typecheck config sets baseUrl (would trigger TS5101 under TS6, forcing an ignoreDeprecations shim TS6-02 forbids)"
    - "an ignoreDeprecations shim is introduced in any plugin typecheck config to make errors disappear rather than fixing at source"
---

<objective>
Create the four plugin packages' standalone `tsc --noEmit` typecheck configs and fix each
plugin's pre-existing, never-before-checked test-only type errors, so every plugin's typecheck is
green on the currently-resolved TypeScript BEFORE the TS6 bump.

Purpose: The plugin half of the pre-bump baseline (Success Criterion 1 / D-07). Uniform treatment
across all 4 plugins (plugin lockstep) means none is left un-typechecked.
Output: 4 `tsconfig.typecheck.json` files + corrected plugin test fixtures + green plugin
typechecks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-CONTEXT.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-PATTERNS.md
</context>

## Artifacts this phase produces

This plan (07-02) produces:
- **`plugins/auth/tsconfig.typecheck.json`**, **`plugins/wallet/tsconfig.typecheck.json`**, **`plugins/theme/tsconfig.typecheck.json`**, **`plugins/settings/tsconfig.typecheck.json`** (4 NEW configs) — each `noEmit:true`, `rootDir:"../.."`, `include:["src","tests"]`, 5-key `paths`, no `baseUrl`.
- In-place type-error fixes in each plugin's `tests/*.test.ts` (no new symbols).

Sibling plans in phase 07 produce: the root `tsconfig.typecheck.json` + `DeepPartial<T>` + root test fixes (07-01); the `Makefile` `typecheck` target + `make test` wiring (07-03); the `typescript@^6.0.0` bump + lockfile (07-04).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the four plugin tsconfig.typecheck.json files</name>
  <files>plugins/auth/tsconfig.typecheck.json, plugins/wallet/tsconfig.typecheck.json, plugins/theme/tsconfig.typecheck.json, plugins/settings/tsconfig.typecheck.json</files>
  <read_first>
    - plugins/auth/tsconfig.json (the plugin build config each typecheck config extends; same shape for wallet/theme/settings)
    - vitest.config.ts (lines 6-11 — alias map the paths block mirrors, D-03)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md (Pattern 1 plugin example: verified rootDir '../..' + paths shape; Pitfalls 1-3)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-PATTERNS.md (plugins tsconfig.typecheck.json section)
  </read_first>
  <action>
    In each of `plugins/auth`, `plugins/wallet`, `plugins/theme`, `plugins/settings` create a
    `tsconfig.typecheck.json` that extends `./tsconfig.json` (the plugin's own build config, which
    itself extends `../../tsconfig.json` — do not skip straight to root, D-02) and sets, in
    compilerOptions: `noEmit: true`, `rootDir: "../.."`, and a `paths` block with the five keys
    resolved relative to the plugin dir (`@dnzn/dxkit` to `../../src/index.ts`; `@dnzn/dxkit-wallet`
    to `../wallet/src/index.ts`; `@dnzn/dxkit-auth` to `../auth/src/index.ts`; `@dnzn/dxkit-theme`
    to `../theme/src/index.ts`; `@dnzn/dxkit-settings` to `../settings/src/index.ts`). Set
    `include: ["src", "tests"]`. `rootDir` MUST be `"../.."` (monorepo root), not the plugin's own
    dir — cross-package `paths` resolve to real sibling `.ts` source outside the plugin, and a
    narrower rootDir trips TS6059 (RESEARCH Pitfalls 1-2). Do NOT set `baseUrl` (Pitfall 3, TS5101,
    would need the ignoreDeprecations shim TS6-02 forbids). Leave each plugin's build
    `tsconfig.json` untouched (D-02). Reuse the verified shape from RESEARCH Pattern 1 verbatim,
    substituting each plugin's directory.
  </action>
  <verify>
    <automated>for d in auth wallet theme settings; do node -e "const c=require('./plugins/$d/tsconfig.typecheck.json'); if(!c.compilerOptions.noEmit) throw new Error('$d noEmit'); if('baseUrl' in c.compilerOptions) throw new Error('$d baseUrl'); if(c.compilerOptions.rootDir!=='../..') throw new Error('$d rootDir'); if(Object.keys(c.compilerOptions.paths).length!==5) throw new Error('$d paths'); console.log('$d ok')" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist and parse as JSON, each with `"noEmit": true` and `"rootDir": "../.."`
    - None contains `"baseUrl"`; each `paths` has exactly the 5 alias keys; each `include` is `["src","tests"]`
    - Each plugin's build `tsconfig.json` is byte-for-byte unchanged
  </acceptance_criteria>
  <done>All four plugin typecheck configs exist in the verified-working shape; build configs untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix each plugin's pre-existing test-only type errors</name>
  <files>plugins/auth/tests/auth.test.ts, plugins/wallet/tests/wallet.test.ts, plugins/theme/tests/theme.test.ts, plugins/settings/tests/settings.test.ts</files>
  <read_first>
    - plugins/auth/tests/auth.test.ts (line 18 mock Wallet missing getProviders/getActiveProvider; line 53 Record<string,Plugin> rejects undefined)
    - plugins/wallet/tests/wallet.test.ts (line 254 Buffer not found; line 563 stale @ts-expect-error)
    - plugins/theme/tests/theme.test.ts (line 425 stale @ts-expect-error; line 462 narrow callback return)
    - plugins/settings/tests/settings.test.ts (line 511 stale @ts-expect-error)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md (Pitfall 4 catalog + Pitfall 5 Buffer/types default + Open Question 1 recommendation)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-PATTERNS.md (Pre-existing test-only errors table)
  </read_first>
  <action>
    Apply the exact at-source fixes cataloged in RESEARCH Pitfall 4, one per site:
    auth.test.ts:18 — add the two missing members (`getProviders`, `getActiveProvider`) to the mock
    `Wallet` object literal so it satisfies the current `Wallet` interface. auth.test.ts:53 — adjust
    the conditional-spread so it does not produce `{ wallet?: undefined }` (e.g. build the object
    without the undefined branch, or type the intermediate as `Partial<Record<...>>` before the
    final assignment) so it satisfies `Record<string, Plugin>`. wallet.test.ts:254 — prefer
    rewriting to avoid the Node-only `Buffer` global (e.g. `TextEncoder`/manual hex) per RESEARCH
    Open Question 1: keeps zero new devDeps and the browser-first posture, and sidesteps TS6's new
    `types: []` default (Pitfall 5) — do NOT add `@types/node` as a devDependency (zero-runtime-dep
    posture). wallet.test.ts:563, theme.test.ts:425, settings.test.ts:511 — remove each now-stale
    `@ts-expect-error` directive (TS2578). theme.test.ts:462 — widen the callback's declared return
    type to match the generic. These are pre-existing errors identical on 5.9.3 and 6.0.3, so they
    belong in this baseline step (D-07 step 1), NOT the TS6 fixes step. Do not suppress with new
    `@ts-expect-error`/`any`-casts/`ignoreDeprecations` — fix at source (TS6-02).
  </action>
  <verify>
    <automated>for d in auth wallet theme settings; do (cd plugins/$d && npx tsc --noEmit -p tsconfig.typecheck.json) || exit 1; done && npx vitest run plugins/</automated>
  </verify>
  <acceptance_criteria>
    - Each `(cd plugins/<name> && npx tsc --noEmit -p tsconfig.typecheck.json)` exits 0 for all 4 plugins
    - `grep -rn "@types/node" package.json plugins/*/package.json` shows no NEW devDependency added
    - `npx vitest run plugins/` still passes (runtime behavior unchanged)
    - No new `@ts-expect-error` or `as any` was added purely to suppress a type error
  </acceptance_criteria>
  <done>All four plugin typechecks are green on the current TS with tests included; plugin vitest suites still pass.</done>
</task>

</tasks>

<flagged_assumptions>
Spec-less probe edges relevant to this plan (surfaced, not silently dropped):
- **TS6-01 / ordering:** the order in which the 4 plugins are typechecked does not affect the outcome — each plugin's typecheck program is independent and resolves root `src` (already clean) via paths, not another plugin's typecheck config. Confirmed by the per-plugin verify loop.
- **TS6-01 / adjacency:** fixing one plugin's test errors does not regress an adjacent plugin's typecheck (disjoint files; no shared test fixtures). Confirmed at the 07-03 wave merge (`make typecheck`).
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Build-time typecheck configs + test-fixture type fixes only — no new runtime inputs, network, auth, or data flow. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Tampering | plugins/*/tsconfig.typecheck.json + plugin tests | low | accept | No new runtime attack surface — build-time typecheck/config only; the Buffer fix removes a Node-only global rather than adding a dependency; zero runtime deps preserved. |

No new runtime attack surface is introduced by this plan.
</threat_model>

<verification>
- Each `(cd plugins/<name> && npx tsc --noEmit -p tsconfig.typecheck.json)` exits 0 (all 4 plugins)
- `npx vitest run plugins/` passes
- `git diff --stat plugins/*/tsconfig.json` is empty (build configs untouched)
- `grep -rn "baseUrl\|ignoreDeprecations" plugins/*/tsconfig.typecheck.json` returns nothing
</verification>

<success_criteria>
All four plugin typecheck configs exist in the verified shape and every plugin typechecks clean
(src + tests) on today's TypeScript, with plugin vitest suites still green — the plugin half of the
pre-bump baseline.
</success_criteria>

<output>
Create `.planning/phases/07-typescript-6-migration-standalone-typecheck/07-02-SUMMARY.md` when done.
</output>
