---
phase: 07-typescript-6-migration-standalone-typecheck
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.typecheck.json
  - src/utils.ts
  - tests/shell.test.ts
  - tests/utils.test.ts
  - tests/stress.test.ts
autonomous: true
requirements: [TS6-01, TS6-03]
must_haves:
  truths:
    - "npx tsc --noEmit -p tsconfig.typecheck.json (root) exits 0 on the currently-resolved TypeScript (^5.8.3 → 5.9.3)"
    - "The root typecheck program includes tests/ (not just src/) — tests become a first-class typed consumer of the package's public types (D-01)"
    - "A tsconfig with zero errors in src/ still shows zero under the new typecheck target — src/ was already clean; this plan only fixes pre-existing test-only errors and adds the config"
    - "deepMerge's second parameter accepts a partially-nested override object (DeepPartial<T>) that was previously rejected by the shallow Partial<T>"
  artifacts:
    - "tsconfig.typecheck.json (root) — noEmit:true, rootDir:'.', include:['src','tests'], paths mirroring the vitest aliases, NO baseUrl"
    - "src/utils.ts — DeepPartial<T> utility type + deepMerge signature using it"
  key_links:
    - "root tsconfig.typecheck.json extends ./tsconfig.json (build config left untouched, D-02)"
    - "paths block resolves @dnzn/dxkit + 4 plugin aliases to real src/*.ts, mirroring vitest.config.ts:6-11 (D-03)"
  prohibitions:
    - "the root typecheck config silently emits files or duplicates tsup's dts build instead of being noEmit-only"
    - "baseUrl is added to make paths resolve (would trigger TS5101 deprecation, forcing an ignoreDeprecations shim that TS6-02 forbids)"
---

<objective>
Create the root package's standalone `tsc --noEmit` typecheck config and fix the pre-existing,
never-before-checked test-only type errors in the root package's `tests/` tree, so the root
package's typecheck is green on the currently-resolved TypeScript BEFORE the TS6 bump lands.

Purpose: Establishes the root half of the measurable pre-bump baseline (Success Criterion 1 /
D-07). Widening the typecheck to `tests/` (D-01) is the point where latent test-only type errors
surface; fixing them here keeps the later TS6 bump's diff strictly attributable to TS6 itself.
Output: `tsconfig.typecheck.json` (root), a corrected `deepMerge` signature, and green root
typecheck.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-CONTEXT.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md
@.planning/phases/07-typescript-6-migration-standalone-typecheck/07-PATTERNS.md
</context>

## Artifacts this phase produces

This plan (07-01) produces:
- **`tsconfig.typecheck.json`** (root, NEW) — `noEmit:true`, `rootDir:"."`, `include:["src","tests"]`, `paths` mirroring the vitest aliases, no `baseUrl`.
- **`DeepPartial<T>`** type in `src/utils.ts` (NEW type) + `deepMerge` signature change from `Partial<T>` to `DeepPartial<T>`.
- In-place type-error fixes in `tests/shell.test.ts`, `tests/utils.test.ts`, `tests/stress.test.ts` (no new symbols).

Sibling plans in phase 07 produce: the 4 plugin `tsconfig.typecheck.json` files + plugin test fixes (07-02); the `Makefile` `typecheck` target and `make test` wiring (07-03); the `typescript@^6.0.0` devDep bump + `pnpm-lock.yaml` update (07-04).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create root tsconfig.typecheck.json</name>
  <files>tsconfig.typecheck.json</files>
  <read_first>
    - tsconfig.json (root build config — the base this extends; MUST stay untouched per D-02)
    - vitest.config.ts (lines 6-11 — the 5-entry alias map the paths block must mirror, D-03)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md (Architecture Patterns → Pattern 1: verified root config shape; Pitfalls 1-3)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-PATTERNS.md ("tsconfig.typecheck.json (root, NEW)" section)
  </read_first>
  <action>
    Create `tsconfig.typecheck.json` at repo root that `extends: "./tsconfig.json"` and sets, in
    compilerOptions: `noEmit: true`, `rootDir: "."`, and a `paths` block with exactly five keys
    mirroring the vitest aliases (`@dnzn/dxkit` → `./src/index.ts`; `@dnzn/dxkit-wallet` →
    `./plugins/wallet/src/index.ts`; `@dnzn/dxkit-auth` → `./plugins/auth/src/index.ts`;
    `@dnzn/dxkit-theme` → `./plugins/theme/src/index.ts`; `@dnzn/dxkit-settings` →
    `./plugins/settings/src/index.ts`). Set top-level `include: ["src", "tests"]`.
    Per D-01/D-02 the build `tsconfig.json` (declaration/outDir/include:["src"]) is NOT edited —
    typecheck and dts-emit stay separate programs. Do NOT set `baseUrl` (RESEARCH Pitfall 3:
    triggers TS5101 deprecation under TS6, which would force an `ignoreDeprecations` shim that
    TS6-02 forbids — paths resolve relative to the tsconfig file since TS 4.1 without it). Do NOT
    leave `rootDir` at the inherited `"src"` (RESEARCH Pitfalls 1-2: TS6059 because cross-package
    `paths` resolve to real sibling `.ts` source outside `src`). Project references / `composite`
    are out of scope (D-04).
  </action>
  <verify>
    <automated>test -f tsconfig.typecheck.json && node -e "const c=require('./tsconfig.typecheck.json'); if(!c.compilerOptions.noEmit) throw new Error('noEmit missing'); if('baseUrl' in c.compilerOptions) throw new Error('baseUrl present'); if(c.compilerOptions.rootDir!=='.') throw new Error('rootDir not .'); if(Object.keys(c.compilerOptions.paths).length!==5) throw new Error('paths != 5'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tsconfig.typecheck.json` exists at repo root and parses as JSON
    - Contains `"noEmit": true` and `"rootDir": "."`; does NOT contain `"baseUrl"`
    - `paths` has exactly the 5 vitest-mirroring alias keys
    - `include` is `["src", "tests"]`
    - `tsconfig.json` (build config) is byte-for-byte unchanged
  </acceptance_criteria>
  <done>Root typecheck config exists in the verified-working shape; build tsconfig untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix root-package pre-existing test-only type errors</name>
  <files>src/utils.ts, tests/shell.test.ts, tests/utils.test.ts, tests/stress.test.ts</files>
  <read_first>
    - src/utils.ts (deepMerge — current shallow `Partial<T>` signature at line 2)
    - tests/utils.test.ts (lines 11, 16, 26, 102 — the shallow-Partial symptoms)
    - tests/shell.test.ts (line 308 — `overrides.nav` missing `label`, same root cause)
    - tests/stress.test.ts (lines 243, 338, 366, 507 — CustomEvent→EventListener casts)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-RESEARCH.md (Pitfall 4 catalog + Code Examples: DeepPartial<T> and the CustomEvent cast fixes)
    - .planning/phases/07-typescript-6-migration-standalone-typecheck/07-PATTERNS.md ("src/utils.ts" and "tests/stress.test.ts" sections)
  </read_first>
  <action>
    Add a `DeepPartial<T>` mapped type to `src/utils.ts` and change `deepMerge`'s second parameter
    from `Partial<T>` to `DeepPartial<T>` — signature only; the implementation body is already
    correct (the type was the only thing wrong). This resolves `tests/utils.test.ts:11,16,26,102`
    and, if `ShellConfig`'s per-dapp `overrides` field is typed off the same shallow pattern,
    `tests/shell.test.ts:308` — check whether that field can reuse `DeepPartial<T>` at its
    declaration rather than patching the call site; otherwise fix the call site minimally.
    For the four `tests/stress.test.ts` CustomEvent casts, apply one consistent at-source fix
    across all four occurrences: either double-cast through `unknown` (`as unknown as
    EventListener`) or type the handler as `EventListener` and narrow `(e as CustomEvent)` inside.
    These are pre-existing errors that exist identically on 5.9.3 and 6.0.3 (RESEARCH Pitfall 4),
    so they belong in this baseline step (D-07 step 1), NOT the TS6 fixes step. Do not add
    `@ts-expect-error`/`ignoreDeprecations`/`any`-widening to silence — fix at source per TS6-02.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.typecheck.json && npx vitest run tests/</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p tsconfig.typecheck.json` exits 0 (zero errors across root src + tests)
    - `src/utils.ts` defines `DeepPartial<T>` and `deepMerge` uses it as the 2nd param type
    - No `@ts-expect-error` or `as any` was added purely to suppress a type error
    - `npx vitest run tests/` still passes (runtime behavior unchanged)
    - Negative check: temporarily add a bogus `const _x: number = 'str'` to any root test file, confirm `tsc --noEmit -p tsconfig.typecheck.json` now FAILS, then revert — proves the config actually checks tests
  </acceptance_criteria>
  <done>Root package typecheck is green on the current TS with tests included; runtime tests still pass.</done>
</task>

</tasks>

<flagged_assumptions>
Spec-less probe edges relevant to this plan (surfaced, not silently dropped):
- **TS6-01 / empty:** a package whose `src/` has zero type errors still runs the typecheck and passes (it is not skipped as a no-op). Verified by the Task 2 negative check (introduce an error → typecheck fails).
- **TS6-01 / adjacency:** fixing the root package's test errors does not regress any plugin's typecheck (root and plugin fixes touch disjoint files; plugin typechecks resolve root `src` which is already clean). Confirmed at the 07-03 wave merge.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds a build-time typecheck config + type-signature/test fixes only — no new runtime inputs, network, auth, or data flow. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | tsconfig.typecheck.json / src/utils.ts | low | accept | No new runtime attack surface — build-time typecheck/config and a type-only signature change; zero runtime deps preserved. |

No new runtime attack surface is introduced by this plan.
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.typecheck.json` exits 0 on the currently-resolved TypeScript
- `npx vitest run tests/` passes
- `git diff --stat tsconfig.json` is empty (build config untouched)
- `grep -n baseUrl tsconfig.typecheck.json` returns nothing
</verification>

<success_criteria>
Root typecheck config exists in the verified shape and the root package typechecks clean
(src + tests) on today's TypeScript, with the vitest suite still green — the root half of the
pre-bump baseline.
</success_criteria>

<output>
Create `.planning/phases/07-typescript-6-migration-standalone-typecheck/07-01-SUMMARY.md` when done.
</output>
