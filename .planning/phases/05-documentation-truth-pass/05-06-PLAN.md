---
phase: 05-documentation-truth-pass
plan: 06
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/cookbook.md
  - docs/development.md
  - docs/testing.md
  - .planning/phases/05-documentation-truth-pass/drift/06-low-fanout.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "Every recipe in cookbook.md uses the 0.2.0 API (nested lifecycle, correct event names/sources, real config defaults) and each TS snippet would compile against 0.2.0 types."
    - "development.md and testing.md match the actual build/test toolchain and commands (make targets, tsup outputs, vitest + happy-dom, alias map) as they exist on the current branch."
    - "All three docs read in the README voice; no filler/hedging; every claim traces to source or the repo's build/test config."
  artifacts:
    - "docs/cookbook.md + docs/development.md + docs/testing.md — corrected"
    - "drift/06-low-fanout.md — per-doc before/after record"
  key_links:
    - "cookbook recipes ↔ RESEARCH §Code Truth (events/config/behavior)"
    - "development.md/testing.md ↔ Makefile, package.json scripts, tsup.config.ts, vitest.config.ts"
---

<objective>
Verify the three lower-fanout docs. cookbook.md recipes must use current API/events; development.md and
testing.md must match the actual toolchain and commands. These are edited after the high-fanout reference
docs so their event/config references check against corrected truth.

Purpose: DOC-01 drift + DOC-02 slop bar.
Output: three corrected docs + drift fragment 06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-documentation-truth-pass/05-RESEARCH.md
@.planning/phases/05-documentation-truth-pass/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify docs/cookbook.md against source</name>
  <files>docs/cookbook.md, .planning/phases/05-documentation-truth-pass/drift/06-low-fanout.md</files>
  <read_first>
    - docs/cookbook.md — full file (patterns & recipes).
    - 05-RESEARCH.md §"Code Truth: Event Catalog", §"Code Truth: Config Defaults", §"Code Truth: Behavior", §"Code Examples" (verified dx:error handler + DOMPurify ESM snippet shapes).
    - src/*.ts and plugins/*/src/index.ts for any recipe touching a specific API.
  </read_first>
  <action>
    Verify every recipe: event names/sources, config shapes (nested lifecycle), plugin usage, and behavior
    claims against source. Correct stale API (flat loaders, wrong event names, removed fields). Ensure the
    dx:error handling recipe and any sanitizer recipe match the verified shapes. Delete recipes that no
    longer reflect a real capability rather than softening them (D-13). Apply the D-13 slop bar (booster/hedge
    words in verify grep + prose restating the code) + README voice; timeless present. Log to
    drift/06-low-fanout.md. Commit `docs(cookbook):` citing DOC-01/DOC-02.
  </action>
  <verify>
    <automated>bash -c "! grep -Eiqw 'simply|just|powerful|seamless|robust|leverage' docs/cookbook.md"</automated>
  </verify>
  <acceptance_criteria>
    - All cookbook TS snippets use the nested lifecycle shape and current event names/sources.
    - No recipe references a removed API (flat loaders, non-existent fields/events) as current.
    - No D-13 booster/hedge words remain.
    - drift/06-low-fanout.md records cookbook.md changes.
  </acceptance_criteria>
  <done>cookbook.md recipes are all 0.2.0-accurate; slop-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify docs/development.md + docs/testing.md against the toolchain</name>
  <files>docs/development.md, docs/testing.md, .planning/phases/05-documentation-truth-pass/drift/06-low-fanout.md</files>
  <read_first>
    - docs/development.md, docs/testing.md — full files.
    - Makefile (targets), package.json (scripts, versions), tsup.config.ts + plugins/*/tsup.config.ts (three output formats), vitest.config.ts (happy-dom, alias map, include globs), tsconfig.json, biome.json.
    - README.md §Development (make helpers + build system) for consistency of claims.
  </read_first>
  <action>
    Verify every command, path, and toolchain claim against the actual repo config: make targets
    (setup/build/test/lint/audit/clean), tsup ESM/CJS/IIFE outputs + global names, vitest + happy-dom config,
    alias map, biome rules, Node/pnpm versions. Correct any drifted command, version, output path, or config
    claim. Ensure testing.md's described test layout matches tests/ and plugins/*/tests/. Apply D-13 slop bar
    + README voice; timeless present. One commit per doc (`docs(development):`, `docs(testing):`) citing
    DOC-01/DOC-02. Log both to drift/06-low-fanout.md (append).
  </action>
  <verify>
    <automated>bash -c "grep -q 'make ' docs/development.md && grep -qi 'vitest' docs/testing.md && ! grep -Eiqw 'simply|just|powerful|seamless|robust|leverage' docs/development.md docs/testing.md"</automated>
  </verify>
  <acceptance_criteria>
    - development.md make targets / tsup outputs / global names match Makefile + tsup config.
    - testing.md test framework, config file, and test layout match vitest.config.ts + tests/ tree.
    - No D-13 booster/hedge words remain in either doc.
    - drift/06-low-fanout.md records development.md + testing.md changes.
  </acceptance_criteria>
  <done>development.md and testing.md match the real toolchain; slop-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → contributor/consumer | Recipes and build/test instructions are copied and run; a wrong command or stale recipe wastes trust or ships a broken pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06a | Tampering | cookbook recipe API shapes | low | mitigate | Stale recipes (flat loaders, wrong events) fail for consumers; corrected to current API and compile-checked in Plan 08. |
| T-05-06b | Spoofing (false assurance) | build/test command claims | low | accept | Wrong commands are self-evident on run; verified against Makefile/package.json this plan. |

No package installs — no package-legitimacy checkpoint.
</threat_model>

<verification>
- Verify greps pass; drift/06-low-fanout.md exists with all three docs' entries.
- cookbook snippets compile-checked authoritatively in Plan 08 (D-04).
</verification>

<success_criteria>
cookbook/development/testing docs are source-and-toolchain accurate and slop-clean.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-truth-pass/05-06-SUMMARY.md` when done.
</output>
