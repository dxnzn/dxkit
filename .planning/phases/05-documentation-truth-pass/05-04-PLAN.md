---
phase: 05-documentation-truth-pass
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/dapp-development.md
  - docs/system-internals.md
  - .planning/phases/05-documentation-truth-pass/drift/04-behavior-docs.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "dapp-development.md and system-internals.md describe ONE disable-while-active rule: disabling the dapp whose route is currently active — mounted or still loading — returns you to / (the post-D-16 converged outcome)."
    - "Mount/route/lifecycle behavior claims match source: last-navigation-wins generation guard, container-clear on post-injection failure, sub-path no-remount + fresh-path catch-up, two-tier route normalization (router vs shell), longest-prefix match, three-tier manifest loading, hash-mode quirk."
    - "Both docs read in the README voice; no filler/hedging; sequence diagrams (if any) match current control flow."
  artifacts:
    - "docs/dapp-development.md — corrected (single disable rule, lifecycle/context/settings behavior)"
    - "docs/system-internals.md — corrected (router/lifecycle/mount internals, disable path)"
    - "drift/04-behavior-docs.md — before/after record"
  key_links:
    - "disable-while-active prose ↔ post-D-16 src/shell.ts disableDapp() (verify against code, not the pre-fix divergence in RESEARCH)"
    - "mount semantics ↔ RESEARCH §Code Truth: Behavior → Mount semantics"
---

<objective>
Verify the two behavior-describing docs against final post-D-16 source. These docs describe the
disable-while-active routing behavior that Plan 01's D-16 fix converged — verify AGAINST the code
(not RESEARCH's pre-fix divergence description) and state the single converged rule per Pitfall 3.

Purpose: DOC-01 drift (routing/lifecycle behavior) + DOC-02 slop bar.
Output: corrected dapp-development.md, system-internals.md, drift fragment 04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-documentation-truth-pass/05-RESEARCH.md
@.planning/phases/05-documentation-truth-pass/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify docs/dapp-development.md against source</name>
  <files>docs/dapp-development.md, .planning/phases/05-documentation-truth-pass/drift/04-behavior-docs.md</files>
  <read_first>
    - docs/dapp-development.md — full file (manifest, lifecycle events, context, settings, sub-routing, standalone mode).
    - 05-RESEARCH.md §"Code Truth: Behavior" (route normalization/rejection, mount semantics last-navigation-wins, sub-path no-remount + fresh-path catch-up, settings handler cleanup), §"Common Pitfalls → Pitfall 3" (single disable rule), §"Code Truth: Config Defaults" (DappManifest.standalone is a doc convention, NOT shell-enforced — don't overstate).
    - src/shell.ts (disableDapp/rebuildRouter POST-D-16 — read current code), src/lifecycle.ts (mount/unmount), plugins/settings/src/index.ts (cleanup on dx:dapp:disabled only).
  </read_first>
  <action>
    Verify every behavior claim against current source. State the single disable-while-active rule
    ("disabling the dapp whose route is currently active — mounted or still loading — always returns you to
    /") as an OUTCOME rule without asserting a single implementation function (Pitfall 3 — the outcome spans
    two code paths). Correct: settings handler cleanup fires only on disableDapp (survives normal
    navigation-away), sub-path navigation does not remount, dx:mount/dx:unmount contract, standalone mode is
    a dapp-author convention not shell-enforced. Apply D-13 slop bar (remove booster/hedge words in the
    verify grep + restated-obviousness) + README voice; timeless present (D-07). Log to
    drift/04-behavior-docs.md. Commit `docs(dapp-development):` citing DOC-01/DOC-02.
  </action>
  <verify>
    <automated>bash -c "grep -q 'disable' docs/dapp-development.md && ! grep -Eiqw 'simply|just|powerful|seamless|robust|leverage' docs/dapp-development.md"</automated>
  </verify>
  <acceptance_criteria>
    - The disable-while-active behavior is stated as a single outcome rule (return to /), matching post-D-16 code, with no lingering description of two divergent outcomes.
    - Settings-cleanup-on-disable, sub-path no-remount, and standalone-is-convention claims are accurate.
    - No D-13 booster/hedge words remain.
    - drift/04-behavior-docs.md records dapp-development.md changes.
  </acceptance_criteria>
  <done>dapp-development.md matches post-D-16 behavior; slop-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify docs/system-internals.md against source</name>
  <files>docs/system-internals.md, .planning/phases/05-documentation-truth-pass/drift/04-behavior-docs.md</files>
  <read_first>
    - docs/system-internals.md — full file (architecture, sequence diagrams, router/lifecycle/event-bus internals).
    - 05-RESEARCH.md §"Code Truth: Behavior" (all sub-sections: route normalization two-tier, longest-prefix, duplicate-route first-wins, three-tier manifest loading, hash-mode quirk, mount generation guard, container-clear guarantee, template cache), §"Common Pitfalls → Pitfall 3".
    - src/router.ts (normalizePath vs shell normalizeRoute — two distinct functions), src/lifecycle.ts, src/shell.ts, src/events.ts.
  </read_first>
  <action>
    Verify architecture claims, sequence diagrams, and internal-behavior descriptions against source. Correct:
    the two-tier normalization reality (router normalizePath per-navigation vs shell normalizeRoute once at
    manifest load — do NOT describe a single normalization path), longest-prefix match with hoisted sort,
    duplicate-route first-registered-wins + shell:manifest emit, three-tier manifest loading (not merged),
    hash-mode identical-hash notifyListeners quirk, last-navigation-wins generation guard, post-injection
    container clear, template cache wrapping outermost. Update any sequence diagram that no longer matches the
    disable/mount control flow (state the disable outcome per Pitfall 3). Apply D-13 slop bar + README voice;
    timeless present. Log to drift/04-behavior-docs.md (append). Commit `docs(system-internals):` citing
    DOC-01/DOC-02.
  </action>
  <verify>
    <automated>bash -c "grep -qi 'longest' docs/system-internals.md && grep -qi 'normal' docs/system-internals.md && ! grep -Eiqw 'simply|just|powerful|seamless|robust|leverage' docs/system-internals.md"</automated>
  </verify>
  <acceptance_criteria>
    - Two-tier normalization described accurately (router vs shell, not one path).
    - Duplicate-route first-wins, three-tier loading, hash-mode quirk, generation guard, container-clear all match source.
    - Sequence diagrams reflect current control flow including the disable outcome.
    - No D-13 booster/hedge words remain.
    - drift/04-behavior-docs.md records system-internals.md changes.
  </acceptance_criteria>
  <done>system-internals.md matches current internals; slop-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → consumer mental model | Consumers reason about mount/route/disable behavior from these docs; a wrong internals claim leads to unsafe assumptions (e.g. stale DOM after failure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04a | Spoofing (false assurance) | container-clear / stale-DOM claims | medium | mitigate | Overstating or understating the post-injection container-clear guarantee could imply no stale dapp DOM survives when it does (or vice-versa); claims traced to src/lifecycle.ts container.innerHTML='' sites. |
| T-05-04b | Repudiation | disable-while-active rule | low | mitigate | Documenting the pre-D-16 divergence would misrepresent behavior; verified against post-fix code (Pitfall 3). |

No package installs — no package-legitimacy checkpoint.
</threat_model>

<verification>
- Verify greps pass; drift/04-behavior-docs.md exists with both docs' entries.
- Cross-doc consistency (disable rule stated identically across docs) is finalized in Plan 08.
</verification>

<success_criteria>
Both behavior docs match post-D-16 source, describe one disable rule, and are slop-clean.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-truth-pass/05-04-SUMMARY.md` when done.
</output>
