---
phase: 05-documentation-truth-pass
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/events-reference.md
  - docs/api-reference.md
  - .planning/phases/05-documentation-truth-pass/drift/02-reference.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "docs/events-reference.md's dx:error section lists every source string from the RESEARCH Event Catalog (all 22 distinct-trigger rows, including the D-15 registry emit), not the 4 illustrative examples it lists today."
    - "Every event name, payload shape, and emit condition in events-reference.md and api-reference.md traces to src/types/events.ts / src/types/* / src/events.ts on the current branch."
    - "Every type signature, factory return, and option in api-reference.md matches the 0.2.0 public type surface (nested ShellConfig.lifecycle, timeout, cacheTemplates, sanitizeTemplate, storageKey)."
    - "No filler/hedging/booster prose remains; both docs read in the README's terse register."
  artifacts:
    - "docs/events-reference.md — corrected (dx:error catalog rewritten wholesale)"
    - "docs/api-reference.md — corrected against 0.2.0 types"
    - "drift/02-reference.md — per-doc before/after drift record"
  key_links:
    - "events-reference dx:error table ↔ RESEARCH §Code Truth: Event Catalog (22-row source catalog)"
    - "api-reference ShellConfig ↔ src/types/shell.ts (lifecycle is Omit<LifecycleManagerOptions,'hasPlugin'>)"
---

<objective>
Verify and correct the two highest-fanout reference docs. events-reference.md has the highest drift
concentration (its dx:error section lists 4 of ~22 source strings) — rewrite that section wholesale
against the RESEARCH Event Catalog. api-reference.md is the public-type surface that configuration.md
and the plugin docs restate, so it must be correct before those are cross-checked.

Purpose: DOC-01 drift correction + DOC-02 slop bar on the two docs everything else quotes.
Output: corrected events-reference.md, api-reference.md, and drift fragment 02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-documentation-truth-pass/05-RESEARCH.md
@.planning/phases/05-documentation-truth-pass/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/events-reference.md against the code-truth Event Catalog</name>
  <files>docs/events-reference.md, .planning/phases/05-documentation-truth-pass/drift/02-reference.md</files>
  <read_first>
    - docs/events-reference.md — full file (current dx:error section ~:157-169 lists only 4 example sources).
    - 05-RESEARCH.md §"Code Truth: Event Catalog" (shell/lifecycle events table, the complete 22-row dx:error source catalog, plugin custom-events table) and §"Common Pitfalls → Pitfall 1".
    - src/types/events.ts, src/events.ts — the type-level EventMap + registry namespace rules, as the authority behind the RESEARCH tables.
  </read_first>
  <action>
    Verify every event row (name, payload, emit site, notes) against the RESEARCH Event Catalog; correct
    drift in place. Replace the dx:error source list wholesale (do not patch incrementally per Pitfall 1):
    document every distinct source string — shell:manifest, shell:route, shell:mount, plugin:${name},
    lifecycle:${id}, :styles, :template, :sanitize, :dependency, and the six plugin storage/state/reconnect
    sources — including the new D-15 registry shell:manifest emit that Plan 01 landed. State the
    wrapped-Error-with-cause convention accurately (not every site sets cause). Confirm the plugin custom
    events (dx:plugin:<name>:<action>) and payloads match. Apply the D-13 slop bar (remove the booster/hedge
    words listed in the verify grep, throat-clearing intros, and prose that merely restates an adjacent
    sample) and the README voice (D-14). Timeless present only (D-07) — no "as of 0.2.0"/"previously". Log
    the before/after to drift/02-reference.md. Commit `docs(events-reference):` citing DOC-01/DOC-02.
  </action>
  <verify>
    <automated>bash -c "grep -q 'shell:manifest' docs/events-reference.md && grep -q 'shell:mount' docs/events-reference.md && grep -q ':sanitize' docs/events-reference.md && grep -q 'plugin:wallet:reconnect' docs/events-reference.md && grep -q 'plugin:theme:storage:read' docs/events-reference.md"</automated>
  </verify>
  <acceptance_criteria>
    - dx:error section names all shell:* sources (shell:manifest, shell:route, shell:mount), plugin:${name}, every lifecycle:${id}[:stage] variant (incl. :sanitize), and all six plugin storage/state/reconnect sources.
    - Registry-failure emit (D-15) is represented under shell:manifest with its explicit-registryUrl condition.
    - No occurrence of the D-13 booster/hedge words (verify grep in Task-2 verify covers both docs).
    - drift/02-reference.md records what was wrong in events-reference.md and what changed.
  </acceptance_criteria>
  <done>events-reference.md is a complete, source-traced event catalog in the README voice.</done>
</task>

<task type="auto">
  <name>Task 2: Verify docs/api-reference.md against the 0.2.0 public type surface</name>
  <files>docs/api-reference.md, .planning/phases/05-documentation-truth-pass/drift/02-reference.md</files>
  <read_first>
    - docs/api-reference.md — full file (largest doc; all factory signatures + interfaces).
    - 05-RESEARCH.md §"Code Truth: Config Defaults Resolved at Construction", §"State of the Art" (0.1.5→0.2.0 API deltas), §"Common Pitfalls → Pitfall 2" (ShellConfig.lifecycle is Omit<..,'hasPlugin'>).
    - src/types/shell.ts, src/types/manifest.ts, src/types/context.ts, src/types/interfaces.ts, src/types/index.ts, src/lifecycle.ts (LifecycleManagerOptions), plugins/*/src/index.ts (plugin option types).
  </read_first>
  <action>
    Check every type signature, factory return type, interface field, and default value against the current
    type surface. Correct: nested ShellConfig.lifecycle (flat scriptLoader/styleLoader/templateLoader are
    removed and now throw at runtime); LifecycleManagerOptions.timeout (default 30000), cacheTemplates
    (default true), sanitizeTemplate (undefined pass-through, no ?? default), hasPlugin NOT configurable from
    createShell (Omit); template cache API (clearTemplateCache/invalidateTemplate); wallet storageKey
    (default 'dxkit:wallet'). Confirm createEthereumWallet() is still described as @deprecated (not
    first-class). Delete any interface field or option that no longer exists; add any shipped one that's
    missing. Apply the D-13 slop bar + README voice; timeless present. Log before/after to
    drift/02-reference.md (append). Commit `docs(api-reference):` citing DOC-01/DOC-02.
  </action>
  <verify>
    <automated>bash -c "! grep -Eiqw 'simply|just|powerful|seamless|robust|leverage' docs/events-reference.md docs/api-reference.md && grep -q 'lifecycle' docs/api-reference.md && grep -q 'sanitizeTemplate' docs/api-reference.md"</automated>
  </verify>
  <acceptance_criteria>
    - api-reference.md documents ShellConfig.lifecycle nesting and shows no flat top-level scriptLoader/styleLoader/templateLoader as current API.
    - timeout (30000), cacheTemplates (true), sanitizeTemplate (undefined default), storageKey ('dxkit:wallet') defaults stated correctly.
    - hasPlugin is not presented as a createShell-configurable field.
    - createEthereumWallet() described as deprecated.
    - No D-13 booster/hedge words remain in either doc (verify grep passes).
    - drift/02-reference.md records api-reference.md corrections.
  </acceptance_criteria>
  <done>api-reference.md matches the 0.2.0 public types exactly; both reference docs slop-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → consumer | Consumers rely on documented event/type contracts to write dapps and error handlers; inaccurate docs mislead security-relevant handling (e.g. dx:error listeners) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02a | Repudiation | events-reference dx:error catalog | medium | mitigate | Under-documenting dx:error sources leaves failures effectively invisible to consumers who build listeners from the doc; wholesale rewrite against the source-grepped catalog closes the gap (DOC-01/D-03). |
| T-05-02b | Spoofing (false assurance) | api-reference type/default claims | low | mitigate | Stale option shapes (e.g. flat loaders, hasPlugin override) would imply capabilities that don't exist; every claim traced to current types, verified mechanically in Plan 08 compile-check. |

No package installs — no package-legitimacy checkpoint.
</threat_model>

<verification>
- Verify greps pass; drift/02-reference.md exists with entries for both docs.
- Snippet compile-check is authoritative in Plan 08 (D-04) — this plan traces claims by reading source.
</verification>

<success_criteria>
Both reference docs are source-accurate, slop-clean, and recorded in the drift fragment.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-truth-pass/05-02-SUMMARY.md` when done.
</output>
