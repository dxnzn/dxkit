---
phase: 06-toolchain-audit-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - plugins/auth/package.json
  - plugins/wallet/package.json
  - plugins/theme/package.json
  - plugins/settings/package.json
  - .npmrc
autonomous: true
requirements: [TOOL-01]
must_haves:
  truths:
    - "All five package.json (root + 4 plugins) declare engines.node >=22, identical string (D-05)"
    - "engine-strict=true in .npmrc converts the advisory engines field into a hard pnpm install failure on Node <22 (TOOL-01)"
    - "The Node-floor commit carries a BREAKING CHANGE footer + migration note (D-10)"
  artifacts:
    - ".npmrc (new, root) with engine-strict=true"
    - "engines: { node: >=22 } in root + 4 plugin package.json"
  key_links:
    - ".npmrc engine-strict is the load-bearing enforcement — engines alone is advisory-only under pnpm (RESEARCH Pitfall 1)"
---

<objective>
Raise the Node floor to 22 LTS across the whole workspace and make it enforceable.
Add `engines: { "node": ">=22" }` to all five package.json files in lockstep (root +
auth/wallet/theme/settings) and create the root `.npmrc` with `engine-strict=true` —
the mechanism that actually fails a wrong-Node install.

Purpose: Establishes the enforced runtime baseline every later v1.1 phase depends on
(TOOL-01). Raising the floor drops Node 18/20 and is a breaking change (D-10).
Output: Five edited package.json files + one new `.npmrc`, committed with a
`BREAKING CHANGE:` footer and migration note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-toolchain-audit-modernization/06-CONTEXT.md
@.planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md
@.planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add engines floor to all five package.json (lockstep) and create .npmrc</name>
  <files>package.json, plugins/auth/package.json, plugins/wallet/package.json, plugins/theme/package.json, plugins/settings/package.json, .npmrc</files>
  <read_first>
    - .planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md (exact current content + insertion points for root and plugin package.json; .npmrc full content)
    - .planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md (Pattern 2: engine-strict is the load-bearing mechanism; Pitfall 1)
  </read_first>
  <action>
    Add the top-level key `engines` with value `{ "node": ">=22" }` to the root
    `package.json`, placed directly after the `packageManager` key (per PATTERNS insertion
    point). Add the identical `engines` key/value to each of the four plugin package.json
    files (auth, wallet, theme, settings), placed after their `homepage` key. The string
    must be byte-identical in all five files per D-05 lockstep — do not let plugin ranges
    drift from core. Do NOT touch `scripts`, `exports`, `devDependencies`, `version`, or
    `pnpm.overrides` in this task.
    Create a new root file `.npmrc` containing `engine-strict=true`, with a one-line
    leading `#` comment explaining it converts the advisory engines field into a hard
    install failure (per CLAUDE.md "document implicit contracts"). This is required per
    D-06 — engines alone is advisory-only under pnpm (RESEARCH Pitfall 1); the two changes
    are one atomic unit.
    Run `pnpm install` to let the lockfile/config settle on the current Node 22 shell
    (expected to succeed here). Commit as a single breaking change: subject
    `feat!: raise Node engines floor to >=22 with engine-strict enforcement`, body
    explaining the why (drops EOL Node 18/20, TS6-toolchain baseline), a `BREAKING CHANGE:`
    footer with a migration note (contributors/consumers must run Node 22 — recommend Node
    22.12+ or Node 24 because Vite 8's own floor is `^20.19.0 || >=22.12.0`, narrower than
    ours; see RESEARCH Pitfall 2), and the required `Co-Authored-By:` line.
  </action>
  <verify>
    <automated>test -f .npmrc &amp;&amp; grep -q 'engine-strict=true' .npmrc &amp;&amp; test $(grep -rl '"node": ">=22"' package.json plugins/*/package.json | wc -l) -eq 5</automated>
  </verify>
  <acceptance_criteria>
    - `.npmrc` exists at repo root and contains `engine-strict=true`.
    - Exactly five package.json files contain the identical `"node": ">=22"` engines range.
    - The commit message includes a `BREAKING CHANGE:` footer with a Node 22 migration note.
    - No unrelated keys (scripts/exports/devDependencies/version) were modified.
  </acceptance_criteria>
  <done>Five package.json declare identical node >=22; .npmrc has engine-strict=true; committed as a breaking change with migration note.</done>
</task>

<task type="auto">
  <name>Task 2: Document the expected engine-strict install failure</name>
  <files>package.json, plugins/auth/package.json, plugins/wallet/package.json, plugins/theme/package.json, plugins/settings/package.json, .npmrc</files>
  <read_first>
    - .planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md (Pattern 2 documented-failure block; Open Question 2 — capture the real error text; Environment Availability — a Node 18/20 runtime is not in the sandbox)
    - .planning/phases/06-toolchain-audit-modernization/06-VALIDATION.md (Manual-Only Verifications — TOOL-01 is documented, not a CI negative job, per D-06)
  </read_first>
  <action>
    Capture the documented expected-failure evidence for TOOL-01 per D-06 (no CI negative
    job is built this phase). If a Node 18/20 runtime is reachable (nvm/fnm/Volta, or a
    throwaway `node:20-slim` Docker container), activate it, run `pnpm install`, and record
    the VERBATIM failure output (the real `ERR_PNPM_UNSUPPORTED_ENGINE`-family message with
    the `Expected version: >=22` / `Got:` lines) into the phase SUMMARY's verification
    evidence — do not invent the exact string (RESEARCH Open Question 2). If no old-Node
    runtime is available in the execution environment, record that constraint explicitly and
    note the mechanism-level expectation (engine-strict + engines >=22 fails install on
    Node <22) as the documented check, deferring the verbatim capture to whoever runs it on
    old Node. This is a documentation/evidence task — no new source files are written.
  </action>
  <verify>
    <automated>grep -q 'engine-strict=true' .npmrc &amp;&amp; grep -q '"node": ">=22"' package.json</automated>
    <human-check>On a Node 18/20 runtime, `pnpm install` fails fast with an unsupported-engine error naming Expected version >=22; verbatim output captured in SUMMARY (or the environment constraint recorded).</human-check>
  </verify>
  <acceptance_criteria>
    - The phase SUMMARY records either the verbatim old-Node install-failure output or an explicit note that no old-Node runtime was available plus the mechanism-level expectation.
    - The documented-failure evidence references engine-strict + engines >=22 as the enforcement pair (D-06).
  </acceptance_criteria>
  <done>Expected old-Node install-failure evidence (verbatim output or recorded environment constraint) captured in the SUMMARY.</done>
</task>

</tasks>

<artifacts_produced>
- NEW `.npmrc` (root) — `engine-strict=true`.
- CHANGED `engines: { "node": ">=22" }` added to all five package.json (root + auth/wallet/theme/settings).
- Documented expected engine-strict install-failure evidence in the phase SUMMARY (TOOL-01, D-06).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime env → pnpm install | Node version of the executing environment crosses into the install gate; engine-strict decides pass/fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Tampering | `.npmrc` / `engines` fields | low | mitigate | engine-strict is the standard pnpm-native gate (not a custom preinstall script); five lockstep engines strings verified by grep gate |
| T-06-01-02 | Denial of Service | Overly strict Node range blocking legitimate contributors | low | accept | `>=22` is an open upper-bound range; Vite 8's narrower sub-range is called out in the migration note so contributors are guided to 22.12+/24 |
</threat_model>

<verification>
- `.npmrc` present with `engine-strict=true`.
- All five package.json carry the identical `"node": ">=22"` engines range.
- Breaking-change commit carries `BREAKING CHANGE:` footer + Node 22 migration note.
- `pnpm install` succeeds on the Node 22 shell; documented old-Node failure evidence recorded.
</verification>

<success_criteria>
TOOL-01 satisfied: every package.json requires Node >=22 and engine-strict makes a
wrong-Node install fail fast, with the expected failure documented (D-06 interpretation
of ROADMAP criterion 1).
</success_criteria>

<output>
Create `.planning/phases/06-toolchain-audit-modernization/06-01-SUMMARY.md` when done.
</output>
