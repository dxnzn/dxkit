---
phase: 06-toolchain-audit-modernization
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [TOOL-03]
must_haves:
  truths:
    - "tsup, vite, and vitest are bumped to current TS6-compatible versions with make test green (TOOL-03)"
    - "happy-dom is confirmed already at the latest published version (20.10.6) — no change needed (RESEARCH)"
    - "Each bump is its own bisectable commit; Vite 7→8 is verified in isolation (D-02)"
  artifacts:
    - "root package.json devDependencies at tsup ^8.5.1, vite ^8.1.4, vitest ^4.1.10"
    - "pnpm-lock.yaml regenerated for each bump"
  key_links:
    - "vitest embeds Vite for transform/resolution; Vite 8's Rolldown internals are the phase's one real risk — the 321-spec suite is the acceptance gate (RESEARCH Open Question 1)"
    - "vitest.config.ts has no build key, so the Vite 8 build.rollupOptions→rolldownOptions rename does not apply (RESEARCH Pitfall 4)"
---

<objective>
Bump the core build/test toolchain to current TS6-compatible versions, one tool per commit
per D-02: tsup 8.4.0→8.5.1, vite 7.3.6→8.1.4 (the phase's one real major-version risk),
vitest 4.1.9→4.1.10, and confirm happy-dom is already at latest (20.10.6, no bump). Keep
caret ranges (D-03); TypeScript stays at 5.8.x (D-04).

Purpose: TOOL-03 — build/test tooling on current versions with the full suite green.
Output: Root package.json devDeps updated + regenerated pnpm-lock.yaml, across per-tool
bisectable commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md
@.planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump tsup to latest (isolated commit)</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md (Standard Stack table for the current verified version; Pattern 1 per-tool bump sequence)
    - .planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md (root package.json devDependencies current shape; tsup.config.ts audit note)
  </read_first>
  <action>
    Re-confirm the current latest tsup version against the live npm registry at execution
    time (`npm view tsup version`; RESEARCH targeted 8.5.1 but is time-sensitive — see
    RESEARCH "Valid until"). Bump via `pnpm add -D tsup@^&lt;latest&gt; -w`, keeping the caret
    range (D-03). Run `make build &amp;&amp; make test` and confirm green (tsup owns all three
    output formats, so a build run is warranted here). No `tsup.config.ts` edits are
    expected (routine bump). Commit as `chore(deps): bump tsup to &lt;version&gt;` with the
    required `Co-Authored-By:` line before touching the next tool.
  </action>
  <verify>
    <automated>make build &amp;&amp; make test</automated>
  </verify>
  <acceptance_criteria>
    - tsup devDependency updated to the current caret range in root package.json.
    - `make build &amp;&amp; make test` green.
    - Change committed as its own bisectable `chore(deps):` commit.
  </acceptance_criteria>
  <done>tsup bumped to current caret range; make build && make test green; isolated commit.</done>
</task>

<task type="auto">
  <name>Task 2: Bump vite 7→8 in isolation (the one real major-version risk)</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md (Summary + Pitfall 4 + Open Question 1: Vite 8 Rolldown internals; vitest.config.ts has no build key; Pitfall 2: Vite 8's narrower Node floor)
    - .planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md (vitest.config.ts full content — confirms no build key)
  </read_first>
  <action>
    Re-confirm the current latest Vite 8.x version against the npm registry (RESEARCH
    targeted 8.1.4). Bump via `pnpm add -D vite@^&lt;latest&gt; -w` in its own commit, isolated
    from every other tool per D-02 because this is the phase's one real major-version jump
    (Rolldown-based bundler internals). Before and after the bump, confirm `vitest.config.ts`
    needs NO config-shape edits by checking it contains no build-level Vite keys (the Vite 8
    build.rollupOptions→rolldownOptions rename and CSS/CJS-interop changes apply only to a
    `build` block, which this test-only config does not have — RESEARCH Pitfall 4). Do NOT
    add any Vite `build` config or rename nonexistent keys. Run `make test` (the full 321-spec
    suite) as the acceptance gate. Commit as `chore(deps): bump vite to &lt;version&gt;` with a
    body noting the Rolldown default and that this repo's only Vite consumer is the test
    config (no build-shape impact), plus the `Co-Authored-By:` line.
  </action>
  <verify>
    <automated>make test &amp;&amp; test -z "$(grep -nE 'rollupOptions|rolldownOptions|cssMinify|commonjsOptions|build\.' vitest.config.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - vite devDependency at the current 8.x caret range in root package.json.
    - `make test` green across the full suite.
    - `vitest.config.ts` unchanged (no build-level keys added or renamed).
    - Vite bump is its own isolated commit.
  </acceptance_criteria>
  <done>vite on current 8.x caret range; full suite green; vitest.config.ts untouched; isolated commit.</done>
</task>

<task type="auto">
  <name>Task 3: Bump vitest and confirm happy-dom is already latest</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md (Standard Stack: vitest 4.1.9→4.1.10, do NOT use vitest 5 beta; happy-dom already at 20.10.6)
  </read_first>
  <action>
    Re-confirm the current latest vitest 4.x version (RESEARCH targeted 4.1.10 — stay on
    major 4; vitest 5 exists only as a `beta` dist-tag, do NOT use it). Bump via
    `pnpm add -D vitest@^&lt;latest-4.x&gt; -w`, keeping the caret range. Run `make test` green.
    Commit as `chore(deps): bump vitest to &lt;version&gt;`.
    Then check happy-dom: `npm view happy-dom version`. RESEARCH found it already at the
    latest published version (20.10.6). If it is still latest, record the no-op verification
    (no bump, no commit needed) in the SUMMARY. If a newer version has since published, bump
    it via `pnpm add -D happy-dom@^&lt;latest&gt; -w` in its own `chore(deps):` commit with
    `make test` green. Include the `Co-Authored-By:` line on any commit.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - vitest devDependency on the current major-4 caret range; vitest 5 beta NOT used.
    - happy-dom confirmed latest (no-op recorded) or bumped in its own commit.
    - `make test` green.
  </acceptance_criteria>
  <done>vitest on current major-4 caret range; happy-dom latest confirmed or bumped; make test green.</done>
</task>

</tasks>

<artifacts_produced>
- CHANGED root package.json devDependencies — tsup, vite, vitest bumped to current caret ranges (happy-dom verified latest / bumped if newer).
- CHANGED pnpm-lock.yaml — regenerated per bump.
- Per-tool bisectable `chore(deps):` commits (Vite isolated as its own commit).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | Bumped package tarballs cross into the repo at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-SC | Tampering | npm bumps (tsup/vite/vitest) | medium | mitigate | RESEARCH Package Legitimacy Audit cleared all as established, high-download, no postinstall scripts; D-02's per-tool `make test` gate blocks a bad bump from committing; caret ranges + committed lockfile keep resolution intentional |
| T-06-03-01 | Tampering | pnpm-lock.yaml drift in CI | low | mitigate | CI uses `--frozen-lockfile` (unchanged); lockfile updates are committed intentionally per bump, never regenerated silently in CI |
</threat_model>

<verification>
- `make build && make test` green after tsup bump; `make test` green after vite and vitest bumps.
- `vitest.config.ts` unchanged (no Vite build-shape edits).
- Each bump is a separate commit; Vite isolated.
</verification>

<success_criteria>
TOOL-03 (core tools): tsup/vite/vitest on current TS6-compatible versions, happy-dom
confirmed latest, full test suite green — via bisectable per-tool commits.
</success_criteria>

<output>
Create `.planning/phases/06-toolchain-audit-modernization/06-03-SUMMARY.md` when done.
</output>
