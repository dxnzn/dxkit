---
phase: 06-toolchain-audit-modernization
plan: 04
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - package.json
  - biome.json
  - pnpm-lock.yaml
  - "src/** (potential Biome reformat)"
  - "plugins/*/src/** (potential Biome reformat)"
autonomous: true
requirements: [TOOL-03, TOOL-04]
must_haves:
  truths:
    - "Biome is bumped to the current stable 2.x with make lint/test green; any reformat lands in its own separate commit (D-02)"
    - "biome.json $schema URL is bumped in lockstep with the Biome version"
    - "cz-git is the active commitizen adapter and the commitizen flow still emits conventional commits (TOOL-04, D-08)"
  artifacts:
    - "root package.json: @biomejs/biome bumped, cz-git added, cz-conventional-changelog removed, config.commitizen.path -> node_modules/cz-git"
    - "biome.json $schema URL at the bumped Biome version"
  key_links:
    - "commitizen resolves config.commitizen.path relative to project root at runtime — a pnpm install must follow both the remove and the add or npx cz throws module-not-found (RESEARCH Pitfall 5)"
---

<objective>
Finish the package.json mutations: bump Biome to current stable (2.x — NOT a major
migration; RESEARCH Pitfall 3) with a separate reformat commit if the formatter changes
files (D-02), keep the `biome.json` `$schema` URL in lockstep, and swap the commitizen
adapter from unmaintained `cz-conventional-changelog` to maintained `cz-git` at 1:1 parity
(D-08).

Purpose: TOOL-03 (Biome) + TOOL-04 (cz-git). Completes the toolchain modernization on the
lint/commit side.
Output: Root package.json + biome.json edited; cz-git installed and wired; any Biome
reformat in its own `style:` commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md
@.planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Biome (+ biome.json $schema) with a separate reformat commit</name>
  <files>package.json, biome.json, pnpm-lock.yaml, src/**, plugins/*/src/**</files>
  <read_first>
    - .planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md (Pitfall 3: latest stable is still 2.x, a patch bump, narrow diff; Pattern 1 Biome step — version commit then separate reformat commit)
    - .planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md (biome.json full content — the version-pinned $schema URL to bump; devDependencies shape)
  </read_first>
  <action>
    Re-confirm the current latest stable @biomejs/biome version (RESEARCH targeted 2.5.4;
    latest stable major is still 2.x — there is NO 3.x, so scope this as a routine patch/
    minor bump, not a schema migration). Bump via `pnpm add -D @biomejs/biome@^&lt;latest&gt; -w`,
    keeping the caret range. In the SAME commit, update the version-pinned `$schema` URL in
    `biome.json` (currently pinned to the old version) so it matches the bumped Biome version.
    Run `make lint` (and `make test`) green. Commit as `chore(deps): bump biome to &lt;version&gt;`.
    Then run `npx biome check --write .` and diff. If it changes any files, commit that
    reformat SEPARATELY as `style: apply biome &lt;version&gt; formatting` (D-02 — keep the
    reformat churn out of the version-bump commit). If the diff is empty, record that no
    reformat was needed (no second commit). Include `Co-Authored-By:` on each commit. Do NOT
    change any `biome.json` rule config beyond the `$schema` URL.
  </action>
  <verify>
    <automated>make lint &amp;&amp; make test</automated>
  </verify>
  <acceptance_criteria>
    - @biomejs/biome at the current 2.x caret range; `biome.json` `$schema` URL matches the bumped version.
    - `make lint` and `make test` green; working tree clean under `biome check` (no unformatted files).
    - Any reformat is a separate `style:` commit, distinct from the `chore(deps):` bump.
  </acceptance_criteria>
  <done>Biome bumped with $schema in lockstep; lint/test green; any reformat in a separate style: commit.</done>
</task>

<task type="auto">
  <name>Task 2: Swap cz-conventional-changelog for cz-git (1:1 parity)</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/06-toolchain-audit-modernization/06-RESEARCH.md (Pattern 3 config swap; Pitfall 5 failure mode; Package Legitimacy Audit — cz-git OK/Approved; A3 — verify default type list, no scope enforcement)
    - .planning/phases/06-toolchain-audit-modernization/06-PATTERNS.md (root package.json config.commitizen block + devDependencies)
  </read_first>
  <action>
    Swap the commitizen adapter to `cz-git` at 1:1 parity per D-08 (standard conventional-commit
    types, no scope enforcement, no custom prompts, no extra cz-git config block). Steps:
    `pnpm remove cz-conventional-changelog -w`, then `pnpm add -D cz-git@^&lt;latest&gt; -w`
    (re-confirm latest; RESEARCH targeted 1.13.1, verdict OK). Update `config.commitizen.path`
    in root package.json from the old adapter to `node_modules/cz-git` (the resolved node_modules
    path, not the bare package name — RESEARCH Pitfall 5). Run `pnpm install` after both the
    remove and the add so commitizen can resolve the new path at runtime. Do NOT touch
    `commit-and-tag-version` or `.versionrc.json` (D-08 — separate tool, untouched). Verify the
    flow by running `npx cz` (or `make commit`) once and confirming the interactive
    type-selection prompt appears and emits a valid conventional commit; if cz-git's defaults
    were to force a scope, add its `noScope`-equivalent option (RESEARCH A3) — otherwise add no
    config. Commit as `chore(deps): replace cz-conventional-changelog with cz-git` with a body
    noting the swap keeps 1:1 conventional-commit parity, plus `Co-Authored-By:`.
  </action>
  <verify>
    <automated>grep -q 'node_modules/cz-git' package.json &amp;&amp; grep -q '"cz-git"' package.json &amp;&amp; ! grep -q 'cz-conventional-changelog' package.json &amp;&amp; make test</automated>
    <human-check>`npx cz` (or `make commit`) shows the cz-git type-selection prompt and produces a valid conventional-commit message.</human-check>
  </verify>
  <acceptance_criteria>
    - `config.commitizen.path` is `node_modules/cz-git`; cz-git is a devDependency; cz-conventional-changelog fully removed.
    - `commit-and-tag-version` and `.versionrc.json` untouched.
    - The commitizen flow emits a valid conventional commit (verified via `npx cz`, evidence in SUMMARY).
  </acceptance_criteria>
  <done>cz-git is the active adapter (config.commitizen.path = node_modules/cz-git); cz-conventional-changelog removed; conventional-commit flow verified.</done>
</task>

</tasks>

<artifacts_produced>
- CHANGED root package.json — @biomejs/biome bumped; `config.commitizen.path` → `node_modules/cz-git`; cz-git added, cz-conventional-changelog removed.
- CHANGED biome.json — `$schema` URL bumped to the new Biome version.
- CHANGED pnpm-lock.yaml — regenerated.
- POSSIBLE reformatted src/tests files in a separate `style:` commit (only if `biome check --write` produced a diff).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | Biome bump + new cz-git dependency cross into the repo at install time |
| commitizen runtime → config.commitizen.path | commitizen loads the adapter module by resolved path at commit time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-SC | Tampering | new devDependency `cz-git` (typosquat / supply chain) | medium | mitigate | RESEARCH Package Legitimacy Audit verdict OK — canonical repo (github.com/Zhengqbbb/cz-git), ~64k weekly downloads, no postinstall script; stays a devDependency (zero runtime-dep posture preserved) |
| T-06-04-01 | Tampering | Biome bump postinstall / provenance | low | mitigate | Established official tooling, no postinstall; `make lint`/`make test` gate blocks a bad bump before commit |
| T-06-04-02 | Denial of Service | broken commit flow after adapter swap | low | mitigate | `pnpm install` after remove+add; `npx cz` smoke-run confirms the prompt resolves before the change is trusted (RESEARCH Pitfall 5) |
</threat_model>

<verification>
- `make lint && make test` green after Biome bump; biome.json `$schema` matches version; tree clean under `biome check`.
- `config.commitizen.path` = `node_modules/cz-git`; cz-conventional-changelog removed; commit-and-tag-version untouched.
- `npx cz` emits a valid conventional commit.
</verification>

<success_criteria>
TOOL-03 (Biome) + TOOL-04 satisfied: Biome on current stable with a clean formatted tree,
and the commitizen flow runs on maintained cz-git still emitting conventional commits.
</success_criteria>

<output>
Create `.planning/phases/06-toolchain-audit-modernization/06-04-SUMMARY.md` when done.
</output>
